# 격리된 이미지 (2026-08-29)

이 폴더의 69개 파일은 `log_assets/markdown/` 안 어떤 글의 `image:`(커버)나
`<img src>`에서도 참조하지 않는 것으로 확인된 이미지입니다.

`log_assets/images/`에 있던 812개 고아 파일(어떤 글도 안 쓰는 이미지) 중
743개는 다른 글에서 이미 쓰고 있는 이미지와 파일 내용이 100% 동일(MD5 일치)해서
바로 삭제했고, 이 69개만 이름·내용 둘 다 겹치는 곳이 없어 여기로 옮겨뒀습니다.

추정 원인: 글 본문을 나중에 편집하면서 이미지를 뺐는데(예: img_1·3·4는 빼고
img_2만 남김) 파일 자체는 안 지워진 경우로 보입니다 — 즉 한 번은 쓰였을 가능성이
있는 파일들입니다.

**확인 방법**: 각 파일명 앞부분(예: `144_Perplexity4 대화형 UX 구조와 쿼리 분기`)으로
`log_assets/markdown/`에서 같은 이름의 글을 찾아 실제로 그 이미지가 필요 없는 내용인지
확인하세요. 필요 없는 게 확실해지면 이 폴더째로 지우면 됩니다.
