{
  "mcpServers": {
    "saegim": {
      "type": "http",
      "url": "${SAEGIM_MCP_URL}",
      "headers": {
        "Authorization": "Bearer ${SAEGIM_TENANT_ID}:${SAEGIM_TENANT_SECRET}"
      }
    }
  }
}
