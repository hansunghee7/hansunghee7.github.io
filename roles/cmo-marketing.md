# cmo-marketing — CMO 겸 AI시대 마케팅 튜터 스레드

## ① 목적과 범위

사용자가 2026-08-30에 부여한 역할: **심플리파이어 CMO 겸 마케팅 튜터.**
브랜딩·마케팅을 책임지되, AI 시대 마케팅의 최신 흐름을 실험·정보공유하고
사장님을 **티칭**하는 것까지 포함한다(사장님이 명시적으로 요구). 우선순위는
Claude가 정해서 상의하는 방식.

**마케팅 목표(사장님 확정):** ① 코칭 계약 ② 강연 문의 ③ 숏폼 신사업 성공
(심플리파이어 미디어, 책영상 비즈니스 — 숏폼은 당분간 **블라인드 채널**로 진행).

**이 스레드가 맡는 층:** 측정 인프라(GA4·서치콘솔·Clarity 수집과 대시보드),
마케팅 데이터 해석·진단, 검색/AEO 전략 판단, 콘텐츠 신디케이션 정책,
숏폼 기준선 설계.

**⚠️ 인접 스레드와의 경계 — 착수 전 반드시 확인:**
- `roles/homepage-growth-ux.md` — SEO/GEO/AEO **콘텐츠 제작**과 프론트 성능
  담당. 주제(SEO/AEO, 브런치 중복 대응, 필러 페이지)가 이 스레드와 크게 겹친다.
  그쪽은 이미 "스타트업 전략" 필러 PRD를 진행 중이므로 **허브/필러 페이지를
  이 스레드에서 중복 착수하지 말 것.** 이 스레드는 전략·측정·의사결정,
  그쪽은 실제 페이지 제작으로 역할을 나누는 것이 현재 이해.
- `roles/ux-lead.md` — 같은 사용자가 부여한 UX 총괄 역할(별도 파일).

## ② 확정된 주요 결정사항

### 측정 인프라 3종 완성 (2026-08-30)
GA4 스냅샷 패턴(수집은 Actions, 화면은 정적 JSON을 fetch)을 그대로 확장.

- **서치콘솔**: `scripts/fetch_gsc.py` + `.github/workflows/fetch-search-console.yml`
  → `assets/data/search-console.json`. **GA4와 같은 서비스 계정
  (`GA4_SERVICE_ACCOUNT_JSON`)을 재사용하므로 새 Secret 불필요.** 사장님이
  ⓐ GSC 사용자에 서비스 계정 추가 ⓑ Google Cloud에서 Search Console API
  "사용 설정" 완료(ⓑ 누락이 첫 실행 실패 원인이었음).
- **Clarity**: `scripts/fetch_clarity.py` + `.github/workflows/fetch-clarity.yml`
  → `assets/data/clarity.json`. Secret `CLARITY_API_TOKEN` 등록됨.
  **API 제약이 설계를 강제한다: 최근 1~3일치만 제공 / 하루 10회 호출 한도 /
  세션 녹화 영상은 API로 못 받음.** 그래서 매일 하루치를 `history`에 누적하는
  구조이고, **하루라도 수집이 빠지면 그 날짜는 영구 공백**이다(cron 2회로 방어).
- **스튜디오 대시보드**: `insight-7b3e9f2c/search-insight.html` 신설.
  nav는 `studio.js`의 `STUDIO_NAV` 배열 한 줄 추가로 등록(중앙관리 구조).
  구성: 검색 성과 카드 → 일별 추이 → 상위 검색어(평균순위 4~15위에
  "리라이트 후보" 배지 자동) → 상위 페이지 → Clarity 마찰 신호 → 막히는 페이지.

### 내부 트래픽 필터 (2026-08-30) — 모든 수치 해석의 전제
- `_includes/site-analytics.html`: `?internal=1`로 접속하면 localStorage
  `spfr_internal` 저장 → 이후 **GA4·Clarity 스크립트 자체를 로드하지 않음**
  (해제는 `?internal=0`). 라이브에서 작동 검증 완료.
- 사장님 3기기(PC·노트북·폰) 적용 완료. **2026-08-30이 깨끗한 데이터의
  기준선**이며 그 이전 데이터는 신뢰하지 말 것.
- 계기: `analytics.json`의 `generate_lead` 3건이 전부 사장님 본인의 폼
  테스트였다. **실제 고객 리드는 0건인데 "전환율 11% 퍼널"로 오독할 뻔했다.**
  네이버 서치어드바이저 유입 12세션도 사장님 본인일 가능성이 높다.

### 진단 결과 (2026-08-30, 필터 적용 전 수치라 참고용)
- GA4 30일: 세션 90, 사용자 41, **재방문 0명**, 실제 리드 **0건**,
  AI 검색 유입(`ai_referrals`) **0건**.
- 서치콘솔: 노출 **9회**·클릭 2회가 전부. 검색어 3개뿐 —
  `simplifier`/`심플리파이어`(브랜드 검색, 이미 아는 사람) + `pl pm 차이`(11위, 클릭 0).
- **결론: 문제는 전환이 아니라 도달(색인·노출)이다.** SEO 기술 기본기는 검증
  완료 — canonical·제목·설명 정상, sitemap 604쪽 제출·성공, robots.txt 허용,
  `www` → non-www 301 정상. 즉 기술 결함이 아니라 신규 도메인의 색인 시간 +
  재발행 콘텐츠 문제. **기술을 더 만지지 말고 콘텐츠·정책으로 갈 것.**
- 서치콘솔 속성은 **URL 접두어** `https://simplifier.co.kr/`, 2026-08-26 등록
  (= 검색 데이터 측정 시작점). 도메인 속성(`sc-domain:`)은 미등록(DNS 인증 안 함).
  인증이 **GA 방식**이라 **GA 태그를 제거하면 소유권이 풀린다** — 주의.
- Clarity 첫 데이터: 분노클릭 0·스크립트에러 0(양호), 죽은클릭이 세션의 10.5%
  (글 615에서 33%, `log.html` 카테고리 필터에서도 발생).

### 콘텐츠 신디케이션 — 전략의 대전제 (사장님 제보)
로그 글 600여 편은 **브런치 등 SNS에 먼저 올라간 재발행 콘텐츠**다. 구글은
권위 높은 브런치를 원본으로 보므로 **"604편을 색인시키면 검색 유입이 온다"는
전제가 성립하지 않는다.** 대응 3축으로 합의:
1. **사이트에만 있는 콘텐츠로 공략** — 허브/필러 페이지(묶음 자체가 유일한
   콘텐츠), 서비스 페이지(코칭·강연 상업 검색어는 브런치와 경쟁 안 함),
   순위권 근처 글만 선별 증보.
2. **발행 순서 역전** — 새 글은 사이트 먼저 → 며칠 뒤 SNS 재발행. 비용 0.
3. **AEO** — AI 검색엔진은 중복 패널티 개념이 약하므로 재발행 글 604편도
   여전히 자산. 인용되기 좋은 구조가 관건. `ai_referrals`로 효과 측정 가능.

### 구조화 데이터 사고와 해결 (2026-08-30)
서치콘솔이 "aggregateRating 개체가 없는 리뷰가 여러 개" 심각 오류를 통보.
**별점을 추가하는 대신 `review` 마크업 자체를 제거**하는 것으로 해결.
근거 ① 고객이 별점을 준 적이 없어 평점을 지어내야 하는데 이는 수동 조치
(manual action) 위험 ② **자사 사이트에 올린 자사 후기(self-serving review)는
LocalBusiness/Organization 계열(ProfessionalService 포함)에서 애초에 별점
리치결과 대상이 아니다** = 얻을 것이 0인데 위험만 있는 마크업이었다.
후기 본문은 화면에 그대로 유지. `_includes/home-body.html`에 이유를 주석으로
남겼다. 실시간 테스트 "감지된 항목 없음" 확인 + 색인 생성 요청 완료.
**→ 앞으로 누가 "별점을 넣자"고 해도 다시 넣지 말 것.**

### ⚠️ 프로세스 위반 기록 (반드시 시정)
CLAUDE.md에 2026-08-29 도입된 **"세션은 main에 직접 push 금지 → 작업 브랜치 +
PR + auto-merge"** 규칙을 이 스레드가 모른 채 main에 직접 4회 push했다
(`85d3b5aa` 수집 파이프라인, `04082a4d` 내부 트래픽 필터, `692bf3e4` 스튜디오
검색 페이지, `a5b118ea` JSON-LD 리뷰 제거). 세션 시작 시점의 CLAUDE.md
스냅샷에 해당 조항이 없었고 세션 도중 갱신된 것이 원인.
**다음 코드 변경부터는 반드시 브랜치 + PR로 진행할 것.**

## ③ 진행 중이거나 남아있는 작업

- [ ] **발행 정책 전환(사이트 선공개 → SNS 재발행) 사장님 확정 대기** — 비용 0,
      현재 1순위. "진행하죠" 한마디만 받으면 정책 확정.
- [ ] 허브 페이지 파일럿 — **착수 전 `roles/homepage-growth-ux.md` 확인 필수**
      (그쪽 "스타트업 전략" PRD와 중복 위험).
- [ ] 숏폼 블라인드 채널 기준선 미착수. 유튜브 애널리틱스 API는 **서비스 계정
      미지원** → 채널 소유자의 OAuth 1회 동의 + 리프레시 토큰을 Secret에 저장하는
      방식이 필요(사장님 작업 15~20분). 채널이 초기이므로 당분간은 Claude in
      Chrome으로 유튜브 스튜디오를 직접 열람하는 것으로 대체 가능.
- [ ] Clarity 세션 녹화는 API로 못 받으므로, 필요할 때 Claude in Chrome으로
      대시보드 직접 열람(건별 사장님 허락 필요). 프로젝트 ID `y8afhrh0tc`.
- [ ] 죽은 클릭 원인 규명 — 글 615, `log.html` 카테고리 필터. 녹화 확인 필요.
- [ ] 스튜디오 ⓘ 툴팁 가로 오버플로 버그(좁은 창에서 화면 밖으로 나가 가로
      스크롤 발생). 별도 작업 칩 `task_cf685175`로 등록만 해둠, 미착수.
- [ ] 재방문 0% 문제(뉴스레터 등 리텐션 자산) — 의도적으로 우선순위 뒤로 미룸.
      도달 문제가 먼저.
- [ ] 서치콘솔 "페이지"(색인 현황) 보고서가 신규 속성이라 "데이터 처리 중"
      이었음 — 며칠 뒤 604쪽 중 실제 색인된 수를 확인해야 처방이 확정된다.

## ④ 다음에 이어서 할 일

1. **발행 정책 전환 확정 받기** — 가장 싸고 확실한 조치. 이것부터.
2. `roles/homepage-growth-ux.md`를 읽고 **허브/필러 페이지 역할 분담을 먼저
   정리**한 뒤에야 콘텐츠 착수. 중복 작업 방지.
3. 내부 트래픽 필터 적용 후 **1주일치 깨끗한 데이터(2026-09-06경)로 재진단** —
   진짜 방문자 수, 진짜 유입 경로, 재방문율을 이때 처음으로 정확히 볼 수 있다.
4. 서치콘솔 색인 현황(페이지 보고서) 확인 → 색인 자체가 안 된 것인지, 색인은
   됐는데 순위가 안 나오는 것인지 확정 → 그에 맞는 처방 선택.
5. 코드 변경은 **브랜치 + PR + auto-merge**로. main 직접 push 금지.
