# 진행상황

> 홈페이지 작업이 어디까지 됐는지 기록해두는 파일입니다.
> 새 대화(스레드)를 시작하면 **"진행상황.md 보고 이어서 해줘"** 라고 말씀해주세요.
> git에 저장되는 파일이라 클로드 앱이 죽어도 사라지지 않습니다.

마지막 업데이트: 2026-08-26

> ⚠️ 여러 세션이 동시에 이 저장소를 작업한 적이 있습니다. 이어받을 때는
> 먼저 `git log --oneline -20`과 `git status`로 실제 상태를 확인하세요.

---

## 🌐 도메인 — ✅ 완료

- **simplifier.co.kr로 이전 완료, 정상 동작 중.**
- `hansunghee7.github.io` 접속 시 `simplifier.co.kr`로 301 리다이렉트됩니다.
- 예전에 A레코드 없이 CNAME만 바꿔 장애 났던 문제는 해소됨.
- ⚠️ 앞으로 사이트를 확인할 땐 **simplifier.co.kr** 로 볼 것.
  `curl https://hansunghee7.github.io` 는 리다이렉트만 반환하므로 `-L` 옵션이 필요합니다.

## ⚙️ CMS (관리자 글쓰기 화면, /admin/) — ✅ 로그인까지 완료

- Decap CMS 파이프라인 완료 — 13개 카테고리, 글쓰기 필드 구성 완료
- 새 글 저장 시 log.html + posts.json 자동 반영
- **GitHub OAuth 로그인 연결 완료** — Cloudflare Worker(`simplifier-cms-auth.simon-8be.workers.dev`) 배포,
  `admin/config.yml`의 `base_url` 실제 주소로 교체, `/admin/` 로그인 테스트 성공 확인
- 글쓰기 폼 순서 정리 + SEO 프롬프트 복사 버튼 추가됨

**남은 것**
- 링크드인/페이스북/인스타그램/스레드 자동 발행 — 미착수 (글 몇 번 써보고 안정된 뒤 진행 권장)
- 브런치/네이버블로그/리멤버 커넥트/로켓펀치는 자동 발행 통로 자체가 없음 → "수동 발행 채널용 복사 텍스트" 필드로 수동 게시

## 🏠 홈 (index.html) — ✅ 최근 작업 완료

- 데스크톱 섹션 간격 130px 통일, 모바일 섹션 간격 +10px
- ORIGIN / VOICES IN THE FIELD 라벨 12px로 통일 (PC·모바일 동일)
- GNB 새창 아이콘을 사각틀 없는 대각선 화살표(↗)로 단순화
- FAQ "왜 회사명이 심플리파이어인가요?" 답변을 히어로 슬로건과 맞춰 재작성 (화면 텍스트 + JSON-LD 스키마 동시 반영)
- **모바일 좌우 여백 전 섹션 16px 통일** (YC 실측 기준)
  - 기존엔 0 / 11 / 15 / 24 / 32px 이 뒤섞여 있었음
  - 원인: `.story-section`·`.yc-sections`의 `padding` 단축 지정이 `.wrap`의 좌우값을 덮어씀 +
    로고 그리드(92vw)·스토리 갤러리(94vw)가 vw로 자체 폭을 잡아 섹션 여백을 무시
  - 헤더 nav는 **의도적으로 32px 유지** (YC도 내비게이션은 본문보다 넓게 씀)
  - 카드 안쪽 글자(37px)는 카드 디자인상 자연스러운 여백이라 **그대로 두기로 결정**

## 📝 블로그 (log.html + 포스트 페이지)

- AEO/SEO 완료: canonical, BlogPosting/FAQPage 스키마, sitemap.xml(585 URL), robots.txt, FAQ 6문항, 파비콘, OG 이미지
- 포스트 본문: 흰 시트를 **화면 전체 폭**으로, 글은 그 안 중앙 **700px 컬럼**에 정렬 (브런치 실측 기준)
- **포스트 커버 연출: 스크롤 시 커버가 접히고 제목이 페이드아웃되는 원래 방식으로 복원**
  (중간에 브런치식 고정(fixed) 방식으로 바뀌었다가 되돌림 — 이 방식을 선호함)
- 로그 목록/포스트도 모바일 좌우 16px로 통일
- 브런치 신규 글 동기화 파이프라인 정상 동작 중

**남은 것**
- ❌ **Google Search Console / 네이버 서치어드바이저 등록 — 미착수**
- ❌ **애널리틱스 설치 — 미착수** (현재 전무)
- 이전글/다음글 영역은 화면 전체폭 스타일 유지 중 (본문 컬럼 정렬로 바꾸는 안은 보류)

---

## 다음 우선순위 후보

1. **Search Console / 네이버 서치어드바이저 등록 + 애널리틱스 설치** ← 가장 큰 미착수 항목
2. CMS로 실제 글 한 번 써보고 파이프라인 검증
3. 검증 후 SNS 자동 발행 연동
