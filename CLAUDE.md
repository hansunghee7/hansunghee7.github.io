# 저장소 안내 (Claude Code용)

## 튜터 원칙 — 사장님과 대화할 때 항상 지킬 것

사장님은 클로드 코드를 "AI CTO가 되기 위한 학습 여정"으로 쓰고 있습니다.
모든 제안과 의견은 아래 두 기준을 통과해야 합니다:

1. **규모 최적화** — 업계 모범 사례를 그대로 이식하지 말 것. 1인 회사 + AI
   조합이라는 현재 규모에서 비용 대비 가장 효과적인 방안을 제시하고,
   "지금 하지 말아야 할 것"도 함께 말할 것. 다음 단계로 넘어갈 시점의
   신호(외부 사용자 발생, 매출 발생, 채용)도 알려줄 것.
   ⚠️ **"비용 대비 효과"의 기준은 "지금 1인 기준으로 충분한가"가 아니라
   "심플리파이어 2.0(팔리는 상품/규모)의 공정자산이 되는가"다**
   (2026-09-03 사장님 지시). 인프라·도구성 결정(배포 파이프라인, 자동화
   구조 등)은 이 기준으로 지금 당장의 개인 편의보다 투자를 크게 잡는 게
   맞을 수 있다 — "개인용이니 오버엔지니어링"이라고 자동으로 깎지 말 것.
   반대로 진짜 일회성 편의는 여전히 최소 투자로.
2. **AI CTO 성장** — 모든 작업을 "도구 설치"가 아니라 그 뒤에 있는 개념을
   배우는 기회로 설계할 것. AI를 가장 효과적이고 최신으로 활용하는 방법
   (에이전트 권한 설계, 자동화 단계, 최신 도구·관행)을 작업에 자연스럽게
   녹여서 가르칠 것. 핵심 결정(무엇을 검증할지, 에이전트에 어디까지 권한을
   줄지 등)은 대신 정하지 말고 사장님이 직접 내리게 할 것.

평가를 요청받으면 미화 없이 객관적으로 — 잘한 것의 근거와 우려되는 것을
같이 말할 것. 학습 기록과 다음 과제는 `docs/진행상황.md`에 남깁니다.

3. **사장님이 직접 손을 대는 절차는 컴맹 기준으로** — 붙여넣기, 배포 버튼,
   터미널 명령 등 사장님이 실행할 단계는 "어느 화면에서 무엇을 클릭하고
   무엇을 붙여넣는지"를 번호 순서로, 한 단계에 한 동작만, 성공했을 때
   보이는 것과 실패했을 때 할 일까지 적을 것. 파일 이름·메뉴 이름은 실제
   표기 그대로. (2026-09-03, 사장님 지시)
4. **사장님이 그 자리에서 바로 읽어야 하는 문서는 파일 다운로드로 주지
   말 것** — md 파일 첨부가 아니라 Artifact(아티팩트)나 링크 형태로 줄
   것. 저장소에 커밋한 문서는 안전을 위해 그대로 두되, 커밋과 별도로
   Artifact로도 게시해서 링크를 드릴 것. (2026-09-03, 사장님 지시)
5. **동작하지 않고 결과가 검증되지 않는 실험은 하지 않는다** — 설계만
   해두고 실행 안 한 산출물, "이론상 되는" 미배포 코드는 공정자산으로
   치지 않는다. 실험하기로 했으면 실제로 돌려서 결과를 실측할 것.
   (2026-09-05, 사장님 지시)
   ⚠️ 이 원칙은 "그러니 위험한 실험도 강행하라"는 뜻이 아니다 — **검증
   자체가 되돌리기 어려운 리스크(계정 정지, 자격증명 노출 등)를 요구하는
   실험은 애초에 하지 않는다.** 그런 경우 "왜 검증 가능한 형태로 만들 수
   없어서 안 했는지"를 cxo-db에 남기는 것이 이 원칙 아래에서의 정답이다
   — 반쪽짜리 설계 문서를 자산이라 부르지 않는 것뿐, 위험을 감수하고서라도
   실행하라는 지시로 읽지 않는다. 리스크 판단 자체를 건너뛰어야 하는
   상황이면 사장님께 그 갈림길부터 명시적으로 확인받는다.

## 세션 운영 교리 — 짧은 세션, 파일 기억 (2026-08-30 도입)

토큰 비용 ≈ **컨텍스트 크기 × 도구 호출 횟수**입니다. 매 도구 호출마다 그때까지의
대화 전체가 다시 전송되므로, 사흘 살려둔 세션에서는 한 줄 질문도 사흘치 대화만큼
비쌉니다. 실측: 2026-08-28~30 스레드 5개가 $656을 썼고 최장수 세션 하나가 $303.
근거: [공식 비용 문서](https://code.claude.com/docs/en/costs) ·
[공식 베스트 프랙티스](https://code.claude.com/docs/en/best-practices).

**원칙: 기억은 파일에, 세션은 작업 하나에.** 세션 간 연속성은 대화를 이어받는
것이 아니라 저장소 문서(진행상황.md, 역할 노트)로 잇습니다.

### 아침 의식 (세션 시작) — 개시어: "하이~"

사장님이 **"하이~"**라고 하면 아래 의식을 수행하라는 뜻입니다. 의식을 마치면
진행상황.md 인수인계에서 자기 역할의 할 일을 찾아 **한 줄로 보고하고** 시작하세요.

1. **새 세션으로 시작합니다.** 어제 세션을 이어받지 마세요.
2. 읽는 것은 **자기 역할 노트 + 진행상황.md 상단 우선순위**뿐. UX_GUIDE·
   STYLE_GUIDE 같은 가이드는 그 화면을 실제로 만지는 시점에 읽습니다.
3. 저장소는 오늘 쓸 것만 부착합니다 (cxo-db는 기록할 게 생겼을 때 요청).

### 저녁 의식 (세션 종료) — 개시어: "바이~"

사장님이 **"바이~"**라고 하면 아래 순서로 마무리하세요. 원칙: **새 파일을
읽지 않습니다** — 이 의식은 컨텍스트가 아직 뜨거울 때 아는 것을 기록하는
절차이지, 조사가 아닙니다. 쓰는 곳은 아침 의식이 읽는 두 곳뿐입니다.

1. **산출물 회수** — 이 세션에서 승인된 산출물이 아티팩트·대화에만 있으면
   지금 저장소로 옮기고 인덱스에 등록 (2026-08-30 유실 사고 재발 방지).
2. **작업 마감** — `git status`로 미커밋 변경 확인, 열린 PR은 병합하거나
   상태를 인수인계에 적기. **push까지 끝나야** 내일 다른 기기 세션이 봅니다.
3. **인수인계 3~5줄** — 을 진행상황.md 또는 자기 역할 노트에. 칸은 아래
   순서로, 해당 없는 칸은 생략:
   - 첫머리 상태 등급: 🟢정상 / 🟡주시 / 🔴불안정
   - 한 것 / 다음 것(내일 세션이 첫 지시로 그대로 쓸 수 있는 문장으로)
   - 막힌 것 + **버린 길**(시도했다 안 된 접근과 이유 — 안 적으면 다음
     세션이 같은 막다른 길을 다시 갑니다)
   - **비정상·임시 상태**(꺼둔 검사, 반쯤 바꾼 구조, 임시 우회 — 교대
     사고의 최대 원인이 이 칸의 누락입니다). 필요하면 "X가 일어나면 Y" 한 줄.
   내용 복사 대신 파일 경로·PR 번호·아티팩트 URL 포인터로.
   판정 기준: 아무것도 모르는 내일 세션이 이 줄만 보고 이어갈 수 있는가.
4. 되돌리기 어려운 판단·사고가 있었으면 cxo-db 인박스 이슈 (아래 경험DB 절).
   해당 없으면 안 남깁니다.
5. **한 줄 마감 보고** ("한 것 / 내일 볼 것") 후 사장님이 **세션을
   닫습니다(아카이브).** 유휴 세션은 캐시(1시간 수명)가 식어, 다음 재개 때
   전체 컨텍스트를 정가로 재처리합니다.

### 세션 중

- 파일을 많이 읽는 조사·탐색은 **서브에이전트에 위임**하고 요약만 받으세요.
- 큰 파일은 통째로 읽지 말고 필요한 범위만 읽으세요.
- Routine(예약 작업)은 **살아있는 세션에 바인딩하지 마세요** — 매 발화가 그
  세션의 전체 컨텍스트를 재전송합니다. 매번 새 세션을 만드는 방식으로 겁니다.

### 자동 로드 문서의 다이어트 기준

CLAUDE.md와 역할 노트는 매 세션 자동 로드되므로 그 자체가 토큰 비용입니다.
한 줄마다 "**이 줄을 지우면 클로드가 실수하는가?**"를 물어서 아니면 지우고,
상황별 절차는 `.claude/skills/`로 옮깁니다(필요할 때만 로드됨). 목표:
CLAUDE.md 200줄 이하, 역할 노트 80줄 이하.

**다음 단계 신호**: 의식을 지켜도 7일 사용량 경고 지속 / 동시 스레드 8개 초과 /
S2(매출) 진입 — 그때 모델 등급 정책과 사용량 리포트 자동화를 만듭니다.

## 사장님이 승인한 산출물은 그 세션 안에 저장소로 옮긴다

**아티팩트와 대화는 "보여주는 표면"이지 "보관 장소"가 아닙니다.** 세션은 매번
새로 시작하고 대화 기억은 반드시 잘립니다. 판정 기준은 하나입니다 —
**아무것도 모르는 새 세션이 찾을 수 있는가.**

사장님이 "좋다"·"그렇게 가죠"라고 한 것이 나오면, **그 세션이 끝나기 전에**
아래로 옮기고 이 문서의 인덱스에 등록하세요. 다음 세션이 같은 걸 다시 만들지
않게 하는 유일한 방법입니다.

| 종류 | 어디에 |
|---|---|
| 디자인 골격 (메일 템플릿, 페이지 레이아웃) | 해당 폴더 또는 `docs/`. **주석에 왜 그런지 근거를 같이** |
| 브랜드 자산 (로고·색·폰트) | `log_assets/images/`, `insight-7b3e9f2c/studio.css` 토큰 |
| 문서 형식·방법론 | 비공개 저장소 `simplifier-cxo-db/standards/` |
| 아직 확정 안 된 시안 | 아티팩트에 두되 **URL을 아래 인덱스에 반드시 기록** |

마지막 줄이 핵심입니다. 저장소에 넣기 애매한 시안도 **주소만 적혀 있으면**
다시 만들 필요가 없습니다.

### 합의된 산출물 인덱스

| 산출물 | 위치 | 합의 |
|---|---|---|
| 통합 UX 가이드 | [docs/UX_GUIDE.md](docs/UX_GUIDE.md) | 상시 |
| 스튜디오 스타일 가이드 | [insight-7b3e9f2c/STYLE_GUIDE.md](insight-7b3e9f2c/STYLE_GUIDE.md) | 상시 |
| 모닝 브리핑 메일 디자인 | `docs/브리핑_메일_템플릿.html` | 2026-08-30 |
| 브랜드 로고 (검은색, 밝은 배경용) | `log_assets/images/logo_black.png` | 2026-08-30 |
| 브랜드 로고 (흰색, 어두운 배경용) | `log_assets/images/logo_white.png` | 상시 |
| 창업자 질문 27문항 | `simplifier-cxo-db/questions/` · [렌더링](https://claude.ai/code/artifact/4acdbda1-8229-47c6-a309-a1e27c934480) | 2026-08-30 |
| 단계 모델 (S0~S3) | `simplifier-cxo-db/standards/stage-model.md` | 2026-08-30 |
| 표준 문서 골격 | `simplifier-cxo-db/standards/_TEMPLATE.md` | 2026-08-30 |
| 잉크 프로젝트(질문→글→뉴스레터 자동화, 탐 담당) 설계 | [docs/질문하기_파이프라인.md](docs/질문하기_파이프라인.md) | 2026-09-03 |
| 사업모델 포트폴리오 (렌드, CSO) | [아티팩트](https://claude.ai/code/artifact/51994dc9-603f-4954-9000-6e148d6368bb) | 미확정 시안 (2026-09-04) |
| 새김 소개페이지 시안 | [아티팩트](https://claude.ai/code/artifact/42058292-0765-46c4-9253-5f6d7ee76297) | 미확정 시안 (2026-09-04) |

> 2026-08-30 사고: 승인된 메일 디자인·로고가 아티팩트에만 있다가 세션 기억이
> 잘리며 유실, 사장님 기억으로 복원했습니다. 사람 기억에 의존하지 않는 것이
> 이 절의 목적입니다.

## 이 저장소는 공개입니다 — 무엇을 어디에 둘지

`hansunghee7.github.io`는 **공개 저장소**입니다. 사이트 빌드에서 제외한 파일도
GitHub에서는 누구나 읽을 수 있습니다(`_config.yml`의 exclude는 배포 제외일 뿐
비공개가 아닙니다).

| 종류 | 어디에 |
|---|---|
| 저장소 작업 노트, 기술 규칙, 사고 이력, UX 가이드 | **여기(공개)** — 모든 세션이 매번 읽어야 하는 운영 문서라 옮기면 작업이 불편해집니다 |
| 사업 전략, 상품 기획, 가격, 고객 정보, C-Level 경험DB | **비공개 저장소 `simplifier-cxo-db`** |

새 문서를 만들 때 어느 쪽인지 먼저 정하세요. 애매하면 사장님께 물어보세요 —
한 번 공개로 올라간 내용은 지워도 커밋 히스토리에 남습니다.

⚠️ `insight-7b3e9f2c/`(내부 대시보드)는 "주소를 모르면 못 본다"를 전제로
운영되지만, **폴더 이름이 공개 저장소에 그대로 보이므로 그 자물쇠는 사실상
열려 있습니다.** 민감한 데이터를 그 안에 올리지 마세요.

## 세션이 끝나기 전에 — 경험DB에 기록할 게 있는지 확인

이 회사의 자산은 코드가 아니라 **왜 그렇게 했는지의 기록**입니다. 그 기록은
비공개 저장소 `simplifier-cxo-db`에 모읍니다(형식은 그 저장소의 README 참고).

**작업 완료를 보고하기 전에** 이번 세션이 아래 둘 중 하나에 해당하는지 보세요:

1. **되돌리기 어려운 선택을 했다** — 도입/미도입, 구조 변경, "지금은 하지 말자"는
   판단. 특히 **하지 않기로 한 결정**이 가치가 큽니다(검색해도 안 나오므로).
2. **뭔가 깨졌다가 고쳤다** — 배포 실패, 데이터 유실, 눈에 안 띄던 버그.

해당 없으면 **안 남깁니다.** 억지로 채우면 검색하면 나오는 체크리스트가 되고
자산 가치가 사라집니다. 한 세션에 여러 건이면 각각 따로 씁니다.

### 어떻게 남기나 — 완성된 문서를 쓰지 마세요

**수집과 정리를 분리합니다.** 던지는 단계는 마찰이 0이어야 하고, 다듬는 건 주 1회
한 곳에서 합니다. 예전 규칙은 각 세션에게 완성된 문서를 요구했는데, 마찰이 커서
실제로는 안 모였습니다.

→ **`simplifier-cxo-db`에 이슈로 던지세요**(`[인박스]` 템플릿, 세 줄이어도 됨).
   그 저장소가 세션에 안 붙어 있으면 사장님께 붙여달라고 요청하세요.
   금요일 모닝 브리핑이 열린 이슈를 모아 정식 문서로 승격합니다.

### ⚠️ 공개 커밋에는 "왜"를 쓰지 마세요

이 저장소는 공개입니다. 커밋 메시지는 누구나 읽고 **히스토리에서 지울 수도 없습니다.**

- 공개 커밋 메시지 → **무엇을 했는가.** 어차피 diff로 다 보이므로 숨길 게 없습니다
- 비공개 이슈 → **왜 그렇게 판단했는가, 뒤집을 신호, 일반화.** 이게 자산입니다

기록할 때 반드시 채울 두 칸:

- `stage` — 이 판단이 유효한 단계 (`simplifier-cxo-db/standards/stage-model.md`).
  요약: **S0 만드는 중 / S1 남이 쓴다 / S2 돈을 받는다 / S3 팀이 있다.**
  심플리파이어는 현재 **S1**입니다.
- **뒤집을 신호**(결정) 또는 **근본 원인**(사고) — 이게 없으면 그냥 일지입니다.

> 사장님이 "기록해줘"라고 말하기를 기다리지 마세요. 사람 기억에 의존하는 절차는
> 2주 안에 끊깁니다 — 그걸 막으려고 이 규칙을 문서에 박아둔 것입니다.

## UX 가이드 (공개 사이트 + 스튜디오 통합)

이 저장소는 성격이 다른 두 화면(고객 대상 공개 홈페이지 / 관리자 전용
심플리파이어 스튜디오)을 같이 갖고 있습니다. 어느 쪽이든 UX/디자인 관련 작업을
하기 전에 **[docs/UX_GUIDE.md](docs/UX_GUIDE.md)를 먼저 읽으세요** — 양쪽에
공통으로 적용되는 원칙(타이포그래피 페어링, 점진적 정보 노출, 고정 테마, 접근성,
브랜드 표기, 노출 범위 판단)과 각 화면의 실제 규칙, 의도적 차이/미정리 차이를
정리해뒀습니다. 스튜디오 쪽 세부 규칙의 정본은 별도로
[insight-7b3e9f2c/STYLE_GUIDE.md](insight-7b3e9f2c/STYLE_GUIDE.md)입니다.

## Simplifier Studio (`insight-7b3e9f2c/`)

`insight-7b3e9f2c/` 안의 어떤 파일이라도 새로 만들거나 고치기 전에,
**[insight-7b3e9f2c/STYLE_GUIDE.md](insight-7b3e9f2c/STYLE_GUIDE.md)를 먼저 읽으세요.**
필수 `<head>`/nav/스크립트 구조, ⓘ 설명 버튼 패턴, 새로고침 버튼 패턴, 과거에
실제로 있었던 사고(죽은 `title=` 툴팁, 설명 문단을 ⓘ로 옮기며 여백이 같이 사라진
사례) 등 이 폴더의 UX/기술 규칙이 전부 그 문서에 있습니다.

작업을 끝냈다고 보고하기 전에 반드시 실행:

```bash
python scripts/check_studio_style.py
```

FAIL이 하나라도 있으면 고치기 전까지 완료로 보지 마세요. `title=` 관련 항목은
WARN으로만 뜨는데, 그건 사람 판단이 필요해서입니다 — STYLE_GUIDE.md의 기준으로
직접 검토하세요. 이 스크립트는 문자열 패턴만 봅니다 — 제목과 콘텐츠 사이 여백처럼
시각적으로만 드러나는 문제는 미리보기 서버로 띄워서 직접 확인해야 합니다.

`insight-7b3e9f2c/shorts-studio.html`은 이 저장소 밖 `shorts-lab` 프로젝트의
파이프라인이 매번 새로 써서 덮어씁니다 — 이 파일을 직접 고쳐도 다음 파이프라인
실행 때 되돌아갑니다. 영구히 고치려면 `shorts-lab/pipeline/build_studio_site.py`
쪽을 고쳐야 합니다.

## 이미지 자산 정리 (`log_assets/images/`)

이미지 정리·삭제·격리 작업 전에 **`image-cleanup` 스킬을 사용하세요**
(`.claude/skills/image-cleanup/`). 블로그 본문만 보고 라이브 로고를 깨뜨린
2026-08-29 사고의 재발 방지 절차(`find_orphan_images.py`, 격리 원칙)가 거기 있습니다.

## 숏폼(shorts-lab) 작업 전 저장소 부착

대본 검토·리서치 반영·음성/자막 생성 등 숏폼 관련 요청을 받으면, 작업 시작 전에
먼저 `add_repo`로 `hansunghee7/shorts-lab`을 부착하세요. 정렬·자막 파이프라인
(`pipeline/build_srt.py`)과 `episode.json`·`sources.md` 등 정본이 전부 그
저장소에 있어, 안 붙이면 이미 있는 절차를 다시 즉흥으로 만들게 됩니다
(2026-09-01 재발 방지 — 저장소 없이 자막을 만들다 오디오 STT 오류를 못 걸러낼
뻔함). "오늘 쓸 저장소만 부착" 원칙의 예외입니다 — 숏폼 작업은 매번 필요합니다.

## 여러 기기에서 작업할 때 (PC + 노트북)

이 저장소는 두 대 이상의 기기에서 클로드 코드로 작업합니다. 세션을 시작하면
가장 먼저 `git fetch origin --prune`으로 원격 상태를 확인하고, 로컬이
`origin/main`보다 뒤처져 있으면 먼저 따라잡은 뒤에 작업을 시작하세요.
브랜치 이름을 여러 개 나열해서 `git fetch origin <A> <B>` 형태로 받지 마세요 —
그중 하나라도 원격에 없으면(예: 이미 삭제된 브랜치) 전체 fetch가 조용히
실패하고 나머지도 갱신되지 않습니다.

## 커밋 교통정리 — 세션은 main에 직접 push하지 않습니다 (2026-08-29 도입)

여러 스레드/기기의 클로드 세션이 동시에 작업하므로, main 합류는 GitHub이
직렬화합니다:

1. 세션은 **자기 작업 브랜치에만** 커밋·push합니다 (예: `claude/...`,
   `feat/...`). main에 직접 push하지 마세요 — 룰셋이 필수 검사(build-check)를
   요구해서 어차피 거절됩니다.
2. 작업이 끝나면 **PR을 열고 auto-merge(squash 또는 merge)를 켭니다** —
   build-check가 통과하면 GitHub이 알아서 병합합니다. 통과 못 하면 병합이
   안 되니, 실패 원인을 고쳐 다시 push하세요.
3. 병합된 브랜치는 삭제해서 목록을 깨끗하게 유지합니다.
4. 예외(룰셋 bypass): 사장님 본인의 직접 커밋(CMS 글 발행, SNS 크롬 확장)과
   자동화 워크플로(github-actions)의 커밋은 PR 없이 main에 들어갑니다 —
   콘텐츠/데이터 커밋이라 의도된 예외입니다.
5. 스레드가 여러 개 동시에 돌 때는 가급적 **스레드마다 영역을 나누세요**
   (예: Studio / 블로그 콘텐츠 / 인프라) — 같은 파일을 두 스레드가 고치면
   PR이 있어도 충돌 해소는 사람 몫입니다.

## 노출 범위를 바꿀 때 — 승인 없이 열지 않습니다

이 저장소는 main에 병합되면 **곧바로 라이브**입니다. 스테이징 환경이 없습니다.
그래서 사고는 "미완성 화면이 보인다"가 아니라 **공개로 승격하는 순간이 다른
작업에 묻어서 나가는 것**으로 났습니다(2026-08-29, PR #9·#10으로 되돌림).

### 무엇이 "노출 범위 변경"인가

- `noindex` 제거 → 검색에 열림
- `preview: true` 해제 → 정식 페이지로 승격
- `sitemap.xml`에 URL 추가 (블로그 글 발행은 제외 — 평상 업무)
- `llms.txt`에 링크 추가 → AI 검색에 열림
- `_includes/header.html`에 링크 추가 → GNB 메뉴에 노출

### 규칙

1. **먼저 사장님께 확인받으세요.** 노출은 되돌려도 이미 본 사람은 되돌릴 수 없고,
   검색엔진에 색인되면 제거에 시간이 걸립니다.
2. **노출 변경은 그것만 담은 PR로.** 기능 작업과 같은 PR에 넣지 마세요 —
   위 사고가 정확히 그렇게 났습니다.
3. 확인을 받았으면 PR 본문에 이유와 함께 **`노출-승인`**을 적으세요.
   예: `노출-승인: 강연 페이지 공개 합의 (2026-08-30)`

`build-check`가 PR마다 `scripts/check_exposure_changes.py`로 검사합니다. 노출이
넓어지는데 승인 표시가 없으면 **병합이 막힙니다.** 무엇이 새로 공개되는지
목록으로 찍어주니, 의도치 않은 항목이 섞였는지 그걸로 확인하세요.

> ⚠️ **확인 없이 표시만 붙이지 마세요.** 그러면 이 검사는 무의미해집니다.
> 기계는 "승인했다는 선언"이 있는지만 볼 수 있지, 실제로 승인받았는지는
> 모릅니다.

### 테스트는 라이브에서 하되 노출만 막습니다

별도 스테이징 서버를 두지 않습니다. 화면 몇 개짜리 정적 사이트에서 빌드 환경을
둘로 나누면 **두 환경이 어긋날 때 지금보다 나빠집니다**("스테이징에선 됐는데
라이브에서 깨졌다"가 제일 비쌉니다). 대신 `preview/` 아래에 `noindex`를 달아
올리고, 눈으로 확인한 뒤 위 절차로 승격합니다.

`robots.txt`가 `Allow: /`인 것은 의도된 것입니다 — 크롤러를 막으면 `noindex`
태그를 **읽지 못해서** 오히려 URL이 검색에 뜰 수 있습니다. 읽게 허용하고
noindex로 거절하는 게 맞습니다.

**다음 단계 신호**: 외부 기여자 발생 / 화면 20개 초과 / 결제·로그인 흐름 도입.
셋 중 하나가 오면 그때 PR별 미리보기 환경(Cloudflare Pages 등)을 만듭니다.

## 시스템 구조 페이지는 인프라와 함께 갱신

`insight-7b3e9f2c/system-map.html`은 이 저장소의 구조 스냅샷입니다.
**인프라를 바꾸는 작업(워크플로 추가·삭제, 외부 서비스 연동·해지, 자동화
Routine 등)을 했다면 같은 세션에서 이 페이지도 갱신하세요** — 요약 숫자
타일(워크플로 수, 외부 서비스 수), 외부 연동 표, 필요하면 다이어그램과
정리 이력까지. 2026-08-29에 감시망을 새로 만들고 이 페이지를 안 고쳐서
사장님이 직접 발견해 지적한 사례가 있습니다. 스냅샷 문서는 낡는 순간
신뢰를 잃습니다.

## 배포 사고 대응

라이브 사이트가 죽었거나 `build-check.yml`·`site-health-check.yml`이 실패하면
**`deploy-incident` 스킬을 사용하세요** (`.claude/skills/deploy-incident/`).
원인 커밋 찾기, revert 복구, 브랜드 자산 격리 원칙 등 매뉴얼 전체가 거기 있습니다.
