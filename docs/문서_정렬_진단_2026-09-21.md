# 문서 정렬 진단 (백로그 5번 갈래 C, 2026-09-21, 판정: 탐)

입력: 소넷이 읽기 전용으로 만든 인벤토리(`C:\work\_ops\inventory-2026-09-21.md`, 저장소 밖). 아래는 그 표를 탐이 원본과 대조해 판정한 것이다. 산출물 1(계층도)의 초안이지 확정이 아니다.

## 1. 확인된 것 (원본 대조)
- 항상 로드되는 줄 수는 CLAUDE.md 179줄로 목표(200줄 이하) 안이다. rules 2개(67줄)는 해당 경로를 읽을 때만, 스킬 10개(630줄)는 부를 때만 로드된다.
- 인벤토리가 측정한 `docs/` 상위 15개 중 가장 큰 것은 `진행상황.md`다. **인벤토리 시점 2,076줄, 지금 2,547줄, `##` 절 76개.** 인수인계·📥·📢가 쌓이기만 하고 접히지 않는다.

## 2. 인벤토리의 오류 (그대로 쓰면 안 되는 부분)
- 스킬 5개를 "상위 문서 연결 없음"으로 적었으나 **4개는 연결돼 있다.** `deploy-incident`, `image-cleanup`, `exposure-change`, `ink-homepage`는 CLAUDE.md 진입점 표나 `ink-desk` 스킬이 이름으로 부른다. 경로 문자열로만 검색해서 틀렸다. 진짜 미연결은 `dom-selector-debug` 하나다(CLAUDE.md에 이름 0회).
- "역할 노트가 세션 시작 때 자동 로드"는 부정확하다. 아침 의식 스킬이 읽으라고 하는 것이지 시스템이 자동으로 넣는 것이 아니다.
- 인벤토리 자신이 밝힌 한계: `docs/` 나머지 31개 파일 미측정, 실제 열람률 미측정, 같은 뜻의 다른 표현 중복 미검출.

## 3. 정렬 진단 (어디가 어긋났나)
1. **최상위(회사 목표) 문서가 이 공개 저장소에 없다.** 목표·OKR 문서를 이름으로 찾아도 없다(비공개 `simplifier-cxo-db`는 이번에 확인하지 않음). 그래서 페르소나 활동이 무엇에 연결되는지 문서로는 추적되지 않는다. 최상위 결정은 사장님 몫(백로그 5번 산출물 2).
2. **`진행상황.md`가 계층 밖의 병목이다.** 아침 의식마다 자기 블록을 찾으려고 2,500줄대 파일을 뒤진다. 지난 인수인계를 "이전 인수인계"로 옮기기만 하고 지우거나 아카이브하지 않는다. 1순위 정리 대상.
3. **스킬과 CLAUDE.md의 연결 검사가 없다.** 이름 언급 여부를 기계가 확인하지 않아 `dom-selector-debug`처럼 고아 스킬이 생긴다.
4. **중복 3쌍(산출물 원칙, 세션 운영, 커밋 공개성)은 의도된 근거 문서 관계**라 정리 대상이 아니다.

## 4. 다음 단계 제안 (사장님 결정 필요)
| 순서 | 할 일 | 결정자 |
|---|---|---|
| 1 | `진행상황.md` 이전 인수인계·종결된 📥를 아카이브 폴더로 옮기는 규칙과 1회 정리(서브에이전트 실행, 탐 판정) | 사장님 승인 |
| 2 | 고아 스킬 판정(`dom-selector-debug`를 CLAUDE.md에 잇거나 삭제) | 탐 |
| 3 | 갈래 A(클로드 문서 구조 모범 사례)·B(OKR) 리서치 착수. `_ops/research/`의 A·B 파일은 다른 주제(유튜브 분석, 헤르메스 승인)라 이 갈래의 결과가 **아니다** | 사장님 (검증 방식은 2026-09-21 지시: 핏의 브라우저 컨트롤 + 제미나이) |
| 4 | 계층도·OKR 초안 종합은 오퍼스 사용이 필요하며 **쓰기 전에 사장님 컨펌**(CLAUDE.md 모델 규칙) | 사장님 |
