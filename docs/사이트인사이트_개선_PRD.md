# 사이트 인사이트 대시보드 — 개선 PRD (초안 v1)

작성 2026-08-26 · 승인 전 · **이 문서에 합의된 뒤 제작 시작**

대상 파일: `insight-7b3e9f2c/index.html`, `scripts/fetch_ga4.py`, `.github/workflows/fetch-analytics.yml`

---

## 1. 왜 하는가

`insight-7b3e9f2c/`는 이미 잘 만들어져 있다 — 목적 선언, AARRR 순서(최초방문→재방문→문의)의 상단 KPI,
"진지한 검토자"를 행동으로 추정하는 관여 이벤트, 홈페이지 DOM을 직접 읽어 항상 최신인 신뢰자산 카운트까지.
**이 PRD는 없는 걸 새로 만드는 게 아니라, 있는 걸 더 정확하고 더 진단적으로 만드는 작업이다.**

지금 상태의 한계:
- 숫자가 **스냅샷**이라 방향(좋아지는 중인지 나빠지는 중인지)을 모른다.
- **기준선(목표)**이 없어서 숫자를 봐도 잘하고 있는지 판단이 안 된다.
- 퍼널이 **설명(어디서 보는지)**일 뿐, 실제 단계별 낙폭을 보여주는 **진단 도구**가 아니다.
- AI 유입 측정의 **구조적 한계**(리퍼러 없는 트래픽)가 화면 어디에도 적혀 있지 않다.

---

## 2. 목표와 성공 판단

**목표.** 대표(비개발자, 매일 이 페이지를 확인)가 숫자를 보자마자 "잘되고 있다 / 확인이 필요하다"를
스스로 판단할 수 있게 한다. 지금은 숫자를 읽어주지만 판단은 못 해준다.

**성공 판단**

| 지표 | 판단 방법 |
|---|---|
| 증감 표시 추가 후 | 대표가 "이번 주 재방문 늘었어요/줄었어요"를 대시보드만 보고 말할 수 있는가 |
| 목표선 추가 후 | 숫자 옆에 "부족/달성" 같은 상태가 색으로 보이는가 |
| 퍼널 차트 전환 후 | "어느 단계에서 제일 많이 이탈하는가"에 바로 답할 수 있는가 |

**성공이 아닌 것**: 차트를 더 화려하게 만드는 것. 판단 시간을 줄이는 것 외의 시각적 개선은 우선순위 낮음.

---

## 3. 개선 항목

### P0 — 지금 결정하면 바로 착수 가능

| # | 문제 | 제안 | 완료 기준 |
|---|---|---|---|
| 1 | ① KPI 카드가 스냅샷만 보여줌, 증감 없음 | 각 카드에 "지난 30일 대비 ▲/▼ N%" 배지 추가. `fetch_ga4.py`가 이전 30일 구간도 같이 가져와 계산 | 최초방문·재방문·문의 카드 3곳 모두에 증감 배지가 뜨고, 이전 구간 데이터가 없으면(측정 시작 초기) 배지 대신 "비교 불가" 표기 |
| 2 | 문의 카드가 측정시작일에 0→값으로 계단처럼 튐, 오독 가능 | 문의 스파크라인에 측정시작일 세로 마커 + "측정 시작" 라벨 | 차트 위에 마커가 보이고, hover/title에 날짜 텍스트 노출 |
| 3 | AI 유입 측정의 한계가 화면에 없음 | "유입 상세" 소스 표 아래에 1줄 안내 추가: "AI 챗봇에서 링크를 복사해 들어오면 리퍼러가 안 남아 이 표에 잡히지 않을 수 있습니다" | 문구가 실제 화면에 노출됨 |

### P1 — 방향엔 동의하지만 설계가 필요

| # | 문제 | 제안 | 완료 기준 |
|---|---|---|---|
| 4 | 목표(goal)선이 없어 숫자만으론 잘하는지 모름 | ① KPI 카드에 목표값을 입력할 수 있는 상수(예: `GOALS = { newUsers: N, returningUsers: N, leads: N }`)를 코드에 두고, 달성/부족을 색으로 표시 | 대표가 목표 숫자를 알려주면 코드 상수 하나만 바꿔서 반영 가능. 목표 미설정 상태에선 지금처럼 중립 색 |
| 5 | 퍼널(②)이 설명 표일 뿐, 실측 낙폭이 없음 | 관여 이벤트(로고월 도달 → FAQ 펼침 → 문의)를 **단계별 인원 수 + 낙폭 %**로 보여주는 실제 퍼널 차트로 전환 | "로고월 도달 100명 → FAQ 펼침 12명(12%) → 문의 2명(17%)" 형태로 표시 |
| 6 | ③ 신뢰자산(재고)과 관여신호(실제 도달)가 다른 섹션이라 대비가 안 됨 | 두 섹션을 "자산 개수 vs 실제로 본 사람" 한 표로 병합 | 로고·후기·FAQ 각 항목 옆에 "보유 개수"와 "도달 방문자 수"가 나란히 표시 |

### P2 — 있으면 좋지만 급하지 않음

| # | 문제 | 제안 |
|---|---|---|
| 7 | ①에만 이상 감지 색상이 없음 (콘텐츠 신선도엔 있음) | ① 카드도 staleness 테이블과 같은 규칙(예: 재방문 0인 날이 N일 이상 연속되면 경고색)으로 통일 |

---

## 4. 기술 요구사항

- **증감 계산(#1)**: `fetch_ga4.py`에 `60daysAgo~31daysAgo`(이전 구간) 리포트를 하나 더 호출해 `totals_prev` 형태로 `analytics.json`에 추가. GA4 API 호출 1회 늘어남(할당량 문제 없는 수준)
- **목표선(#4)**: 비밀값 아님 — 코드 상수로 저장. `insight-7b3e9f2c/index.html` 상단 JS의 `MEASURE_START` 옆에 나란히 둘 것
- **퍼널 실측(#5)**: 이미 있는 `section_view_proven → faq_open → generate_lead` 이벤트 순서를 그대로 쓰되, 사용자 단위 유니크 카운트가 필요하면 GA4 API의 `eventCount`가 아니라 `sessions` 기준으로 재집계해야 함(현재 `engagement_events`는 이벤트 발생 횟수이지 세션 수가 아님 — 낙폭 계산 전에 확인 필요)
- 기존 파이프라인(매일 06:00 GitHub Actions → `assets/data/analytics.json` → 대시보드가 fetch)은 그대로 재사용, 스키마만 확장

---

## 5. 범위 밖 (이번에 안 함)

- 차트 라이브러리 도입 (지금처럼 순수 CSS/JS 막대로 충분)
- 실시간(분 단위) 업데이트 — 일 1회 배치로 충분한 용도
- 알림(이메일/슬랙) 발송 — 대표가 매일 페이지를 직접 확인하는 방식 유지
- 목표값 자체를 정하는 것 — 이건 사장님이 결정할 몫 (아래 Q1)

---

## 6. 확인이 필요한 것 (사장님 결정)

| # | 질문 | 내가 제안하는 기본값 |
|---|---|---|
| **Q1** | 목표(goal) 숫자를 지금 정할까, 데이터 더 쌓인 뒤 정할까? | **더 쌓인 뒤** — 최소 2~4주치 실측 없이 목표를 정하면 근거 없는 숫자가 됨. 그때까지 #4는 보류 |
| **Q2** | P0(1~3번)부터 먼저 반영하고 P1은 나중에? | **예** — P0는 오늘 바로 가능, P1은 설계 논의(특히 #5 이벤트→세션 재집계)가 더 필요 |
| **Q3** | #6(자산 vs 관여신호 병합)이 지금 구조를 크게 바꾸는데, 진행할까? | 우선 보류 — P0 반영 후 실측 데이터를 보고 정말 필요한지 재판단 |

---

## 7. 진행 순서

1. 이 PRD 합의 (Q1~Q3)
2. P0 3건 구현 (증감 배지, 측정시작 마커, AI 유입 한계 안내)
3. `fetch_ga4.py` 확장 → Actions 수동 실행으로 검증
4. 2~4주 데이터 축적 후 목표값(Q1) 결정 → P1 착수
