# SNS 등록대장

> 무엇이 어느 채널에 발행됐고, 무엇이 예약 중이고, 무엇이 막혔는지 한 표로 관리한다(2026-09-24 사장님 지시). 정본은 이 표다. AItoEarn 자체 대시보드에도 기록이 남지만, 마야가 직접 관리하는 곳은 여기다.

> ⚠️ **이 프로젝트(SNS 등록 자동화)의 KPI는 "사장님이 손 하나도 안 대는 등록 프로세스"다**(2026-09-24 사장님 원문: "이 프로젝트의 목표는 최초에 밝혔듯이 사장 손이 하나도 안 타는 등록프로세스를 만드는게 마야의 KPI입니다"). 어딘가 막혀서 "사장님이 대신 클릭해 주세요"로 넘기면 그 순간 기존 스튜디오 멀티퍼블리싱(복붙 도구)과 다를 게 없어져서 이 프로젝트를 할 이유가 사라진다. 막히면 사장님께 대신 시키지 말고 다른 실행 경로부터 찾는다.

## 채널 현황 (2026-09-24 확인)

| 채널 | 방식 | 상태 | 비고 |
|---|---|---|---|
| Facebook (Simplifier 페이지) | AItoEarn API | **실전 등록 성공** | `accountId: facebook_1276868818845114`. 텍스트 게시는 확인됨, 이미지 포함은 추가 작업 필요(아래) |
| Instagram (신기한 아파트사전) | AItoEarn API | 연결 확인됨(팔로워 194) | 핏의 영상 채널, 마야 텍스트 게시 대상 아님 |
| YouTube (한성희) | AItoEarn API | 연결 확인됨(팔로워 0) | 핏 담당 |
| TikTok (신기한 아파트사전) | AItoEarn API | 연결 확인됨(팔로워 13) | 핏 담당, 실전 예약 성공(핏 우편 2026-09-24) |
| Twitter/X | AItoEarn(대시보드엔 있으나 API 조회에는 안 나옴) | 미확인 | 재인증 필요할 수 있음, 다음에 확인 |
| LinkedIn (한성희, 팔로워 18,571명) | AItoEarn 플랫폼 지원 목록엔 있으나 이 계정 미연결 | 수동(스튜디오 멀티퍼블리싱) | 실제 독자가 있는 채널이라 자동화 전 신중 검토 |
| 브런치·네이버블로그·리멤버·로켓펀치·네이버클립 | AItoEarn 미지원 | 수동(스튜디오 멀티퍼블리싱) | 계속 복붙, 핏과 벤치마킹 중 |

## 등록 기록

| 날짜 | 글(원문) | 채널 | 방식 | 상태 | 예약/발행 시각 | 참고 |
|---|---|---|---|---|---|---|
| 2026-09-24 | 「토큰을 아끼려다 기술이 늘었다」(631번) | Facebook(Simplifier) | AItoEarn 발행 흐름 API(`scripts/publish_sns_queue.py`) | **등록 성공(DONE=VERIFIED)** | 2026-09-25 09:00 KST 예약 | `flowId: 72fe23ba-7b9e-4d5e-9452-ee12c88de493`. 첫 시도 2건은 실패(엔드포인트 오타 `/publish-flows`→`/publish/flows`, Facebook `option.content_category:"post"` 누락, 이미지 URL이 우리 도메인이라 `disallowed_domain` 거부). 세 번째 시도(텍스트만, 이미지 없이)에서 성공 |

## 다음 것

- 2026-09-25 09:00 KST 이후 실제로 페이스북에 올라갔는지 라이브 확인(또는 `_sns-extension` 다음 수집 결과와 대조).
- 이미지 포함 게시는 AItoEarn 업로드 API로 이미지를 먼저 올려 AItoEarn 자체 URL을 받아야 한다(우리 사이트 도메인은 `disallowed_domain`으로 거부됨). 다음 과제.
- 앞으로는 `AITOEARN_API_KEY` 저장소 시크릿을 사장님이 추가하면, GitHub Actions(`publish-sns.yml`, 하루 2회 + 수동 실행)가 큐(`assets/data/sns_publish_queue.json`)의 승인된 항목을 이 세션 개입 없이 자동 등록한다.
- Twitter/X가 API 목록에 안 나오는 이유 확인.
- LinkedIn(진짜 독자 18,571명)은 자동화 전에 시험 한 번 더 신중하게 설계(팔로워 0인 채널에서 먼저 검증한 뒤에).
- 브런치·네이버블로그·리멤버·로켓펀치·네이버클립(AItoEarn 미지원)은 핏과 벤치마킹 중([설계안](SNS_자동게시_설계안.md) 3절).
