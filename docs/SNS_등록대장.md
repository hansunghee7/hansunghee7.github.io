# SNS 등록대장

> 무엇이 어느 채널에 발행됐고, 무엇이 예약 중이고, 무엇이 막혔는지 한 표로 관리한다(2026-09-24 사장님 지시). 정본은 이 표다. AItoEarn 자체 대시보드에도 기록이 남지만, 마야가 직접 관리하는 곳은 여기다.

> ⚠️ **이 프로젝트(SNS 등록 자동화)의 KPI는 "사장님이 손 하나도 안 대는 등록 프로세스"다**(2026-09-24 사장님 원문: "이 프로젝트의 목표는 최초에 밝혔듯이 사장 손이 하나도 안 타는 등록프로세스를 만드는게 마야의 KPI입니다"). 막히면 사장님께 대신 시키지 말고 다른 실행 경로부터 찾는다. **안 되면 뚫을 방안을 찾는다, 검증된 실측이 완료다**(심플리파이어 웨이 17번, 사장님 지시 2026-09-24) — "다음에", "당분간 이대로"로 닫지 않는다.

## 채널 우선순위 (2026-09-24 사장님 확정)

- **사장님 주력 채널**(우선 자동화): 링크드인, 페이스북, 네이버블로그, 브런치
- **육성 중 채널**(후순위): 인스타그램, 리멤버 커넥트, 로켓펀치

## 채널 현황

| 채널 | 우선순위 | 방식 | 상태 | 비고 |
|---|---|---|---|---|
| Facebook (Simplifier 페이지) | 주력 | AItoEarn API | **실전 등록 성공(이미지 포함)** | `accountId: facebook_1276868818845114` |
| LinkedIn (한성희, 팔로워 18,571명) | 주력 | AItoEarn 지원 목록엔 있으나 이 계정 미연결 | 대기 | 연결 자체는 리스크 없음, 지금 바로 연결 가능(사장님이 AItoEarn "채널 연결" 화면에서 클릭). 실제 게시는 팔로워 0인 채널에서 먼저 검증 후 |
| 네이버블로그 | 주력 | AItoEarn 미지원, **네이버 공식 오픈API(로그인 API)로 글쓰기 가능 확인**(2026-09-24) | 대기 | AItoEarn 없이 공식 API + GitHub Actions 구조로 별도 설계 가능. 다음 과제 |
| 브런치 | 주력 | AItoEarn 미지원, 공식 API 유무 미확인 | 확인필요 | 계속 조사(포기 아님) |
| Instagram (신기한 아파트사전) | 육성중(핏 채널) | AItoEarn API | 연결 확인됨(팔로워 194) | 핏 담당, 마야 텍스트 게시 대상 아님 |
| Instagram (마야용, simplifier_seoul) | 육성중 | AItoEarn 미연결 | 확인필요 | 메타 비즈니스 스위트 경유 연결 시도 중 서버 30분+ 무응답. 다음엔 AItoEarn "채널 연결" 화면에서 직접 시도(우회 경로 안 씀) |
| 리멤버 커넥트 | 육성중 | AItoEarn 미지원 | 수동(스튜디오 멀티퍼블리싱) | 공식 API 유무 확인 필요 |
| 로켓펀치 | 육성중 | AItoEarn 미지원 | 수동(스튜디오 멀티퍼블리싱) | 공식 API 유무 확인 필요 |
| 네이버클립 | 핏 담당 | 공식 API 없음(확인됨) | 수동 | 핏 영역, 마야 소관 아님 |
| YouTube·Facebook 릴스(영상) | 핏 담당 | AItoEarn API | 핏이 별도 보고 | 마야 소관 아님 |
| Twitter/X | 해당 없음 | - | **미사용 채널**(사장님이 원래 안 씀) | 자동화 불필요 |

## 등록 기록

| 날짜 | 글(원문) | 채널 | 방식 | 상태 | 예약/발행 시각 | 참고 |
|---|---|---|---|---|---|---|
| 2026-09-24 | 「토큰을 아끼려다 기술이 늘었다」(631번) | Facebook(Simplifier) | AItoEarn 발행 흐름 API | **등록 성공, 이미지 포함(DONE=VERIFIED)** | 2026-09-25 09:00 KST 예약 | 최종 `flowId: 75cbf6ec-040d-4eb3-8ec7-658a9ddddb46`. 1차(텍스트만, flowId 72fe23ba)를 취소하고 이미지 포함으로 재등록. 이미지는 AItoEarn 업로드 API(`POST /api/assets/uploadSign`→PUT)로 올려 자체 도메인(`assets.aitoearn.ai`) URL 확보, `disallowed_domain` 문제 해결 |

## 다음 것

- 2026-09-25 09:00 KST 이후 실제로 이미지 포함 게시가 올라갔는지 라이브 확인(또는 `_sns-extension` 다음 수집 결과와 대조).
- `AITOEARN_API_KEY` 저장소 시크릿을 사장님이 추가하면, GitHub Actions(`publish-sns.yml`)가 큐(`assets/data/sns_publish_queue.json`)의 승인된 항목(이미지 업로드 포함)을 이 세션 개입 없이 자동 등록한다. 이미지 업로드도 스크립트에 넣을지는 다음 개선 과제(지금은 세션이 먼저 업로드해서 URL을 큐에 채워 넣음).
- 네이버블로그 공식 API로 자동 게시 설계(주력 채널, 다음 우선순위).
- LinkedIn 연결(사장님 클릭 1회) → 팔로워 0 채널에서 게시 검증 → 실전.
- Instagram(simplifier_seoul) AItoEarn 직접 연결 재시도.
- 브런치·리멤버·로켓펀치 공식 API 유무 계속 조사.
