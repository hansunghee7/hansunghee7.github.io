# 구솔라 다음 지시서: Task 07~08 초기 셋업 (2026-09-17)

> GU-SOLAR-LINUX-MIGRATION Epic Task 07(Git/GitHub CLI)·08(Python/Node
> runtime)까지만 다룬다. **Task 03·04·06(설치·네트워크·SSH)이 EVIDENCE로
> 확인되기 전엔 쓰지 않는다** — 한 task의 검증이 끝나지 않으면 다음 단계로
> 넘어가지 않는다는 Epic 10절 원칙 그대로.

## 실행 주체

SSH가 뚫리는 순간부터는 물리 접근이 필요 없다. **로컬 탐(신PC 헤르메스)이
SSH로 구PC에 접속해 아래 명령을 대신 실행할 수 있다** (Hermes 위임 우선
규칙 §b7f1 "SSH/원격 PC에서 실행해야 하는 작업"에 해당 — 사장님이 직접
타이핑할 필요 없음). 사장님이 직접 하고 싶으면 SSH 세션에 그대로 붙여넣어도
된다.

## Task 07. Git / GitHub CLI

```bash
sudo apt update
sudo apt install -y git gh
git --version
gh --version
```

**검증**: 두 명령 다 버전 문자열이 출력되면 성공.

## Task 08. Python / Node 런타임

```bash
sudo apt install -y python3 python3-pip python3-venv
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt install -y nodejs
python3 --version
node --version
```

**검증**: 두 명령 다 버전 문자열이 출력되면 성공.

## Task 09~10 (헤르메스 설치·headless 실행) — 이 지시서 범위 밖

새로 설계하지 않는다. **로컬 탐이 신PC에 헤르메스를 설치·운영한 절차를
그대로 재사용**(Epic 1-1절 "Inspect → Reuse → Extend")한다. 이 클라우드
세션은 그 설치 절차 자체를 모르므로 지어내지 않는다 — 로컬 탐이 Task
07~08 EVIDENCE를 확인한 뒤 자기 지식으로 이어서 진행한다.

## EVIDENCE

Task 07·08 각각 버전 출력 화면(또는 로그)을 진행상황.md에 남기면
DONE=VERIFIED. Task 09부터는 로컬 탐이 이어서 기록.
