# 노트북에서 신PC로 이동 지시서 (탐과 마야, 2026-09-20)

> 사장님 지시(2026-09-20): 이 스레드를 닫고 노트북에서 신PC로 옮긴다. 노트북에 있는 로컬 저장 내용이 못 옮겨지므로 맥락이 끊길까 걱정된다.
> 원칙: 기억은 파일에(CLAUDE.md). 이동에 필요한 것은 저장소에 이미 있고, **저장소에 없는 것은 아래 "노트북에만 있는 것" 표뿐**이다.
> 사실 확인은 2026-09-20 읽기 전용 실측이다. 확인하지 못한 것은 "미확인"이다.

## 1. 이동 전 점검 결과 (노트북)
| 확인 | 결과 |
|---|---|
| 탐 작업 폴더의 미커밋 변경, stash | 0개 |
| 탐이 만든 PR | 전부 병합됨(남은 것은 자동 병합 대기 1건) |
| 마야 작업 폴더(`hansunghee7.github.io-maya`) | 미커밋 0개, 브랜치는 병합됨 |
| PR 없이 노트북에만 있던 커밋 | 2개 브랜치(`claude/progress-notes-2026-08-30`, `feat/thread-protocol-menu`, 8/30~8/31 작업). **원격에 백업 완료**(삭제하지 않음). 병합 여부는 사장님 판단 |
| 스크래치패드 복제본 | 재생성 가능(원격에 이미 푸시됨) |

## 2. 노트북에만 있는 것 (저장소에 없음)
| 항목 | 대체 방법 |
|---|---|
| SSH 개인 키와 별칭 `shinpc` | 신PC에서는 헤르메스가 로컬이라 **필요 없음**. 노트북에서 계속 마야가 쓰려면 유지 |
| Claude 메모리, 대화 기록 | 옮겨지지 않음. 필요한 것은 이미 저장소 문서에 있음(진행상황, 역할 노트, 백로그) |
| `~/ruleset_backup_0920.json` | GitHub에서 다시 받을 수 있음 |
| 앱 설정(Remote Control 기본값, 동적 워크플로우 끔, 비활성 세션 보관 등) | **앱 설정은 PC별이라 신PC 앱에서 다시 맞춰야 함**(미확인, 사장님 확인) |

## 3. 탐: 신PC에서 이어가기
**신PC 상태(읽기 전용 확인):** 저장소 복사본 `C:\Users\PC\work\hansunghee7.github.io`가 있음. 브랜치 main, 마지막 커밋 9/19, 미커밋 0, 미푸시 0(뒤처져 있을 뿐이라 `git pull`만 하면 됨). git 2.55와 python 있음. `gh` 명령은 있으나 로그인 여부는 미확인. Claude 앱 설치 여부는 확인하지 못함.

**사장님이 할 일**
1. 신PC의 Claude 앱에서 폴더 `C:\Users\PC\work\hansunghee7.github.io`를 열어 새 세션을 시작한다.
2. 앱 설정에서 "새 세션을 Remote Control에 연결"을 켠다(폰에서 보이게).
3. 새 세션에 **"하이~"**라고만 말한다. 세션이 `git fetch`, CLAUDE.md, 역할 노트, 진행상황 상단을 읽고 한 줄로 보고한다.
4. 이어서 "진행상황.md 탐(로컬) 블록의 '다음 것' ①부터 진행해 줘"라고 말하면 된다.

**노트북과 달라지는 명령 (신PC는 헤르메스가 로컬)**
| 노트북에서 | 신PC에서 |
|---|---|
| `hx.sh cron` | `hermes cron list` |
| `hx.sh ask < 지시` | `hermes chat --query-file <파일> --oneshot -Q` (긴 일은 백그라운드로 실행해 완료 알림을 받는다) |
| `hx.sh read/ls <경로>` | 직접 읽기. 단 `.env`, 키, 토큰은 읽지 않는다 |
| `hx.sh kick` | `python C:\Users\PC\AppData\Local\hermes\scripts\mailbox_courier.py` |
| `hx.sh put` | 스크립트를 직접 수정하되 먼저 백업하고 `python -m py_compile`로 문법 검사 |
| SSH 별칭 `shinpc` | 필요 없음 |

**지켜야 할 것:** 신PC는 **운영 헤르메스가 있는 PC**다. `.env` 직접 편집 금지, 크론 정지·재개·등록은 사장님 컨펌, 설정 변경 금지 규칙은 노트북과 같다(역할 노트 2절).

**세션 간 소통이 바뀐다:** 탐(신PC)과 마야(노트북)는 다른 PC다. 다른 PC 세션끼리 `SendMessage`가 되는지는 **미확인**이다(문서상 Remote Control과 서버 경유가 필요). 되지 않으면 우편(`mailbox.py send`)과 즉시 배달을 쓴다(신PC에서는 위 표의 `kick` 대체 명령).

## 4. 마야에게 전달할 지시서 (붙여넣기용)
> 마야에게. 사장님 결정(2026-09-20)으로 탐이 노트북에서 신PC로 옮깁니다. 마야가 함께 옮길지 노트북에 남을지는 사장님이 정합니다. 어느 쪽이든 아래를 먼저 끝내고 사장님께 한 줄로 보고하세요.
>
> 1. **미커밋·미푸시 확인.** 탐이 확인한 마야 작업 폴더는 변경 0개였습니다. 마야가 그 뒤에 만든 파일이 있으면 지금 커밋해 PR로 올리세요. 카드에만 있고 저장소에 없는 산출물도 옮기세요.
> 2. **`진행상황.md`의 [마야] 블록을 인수인계 형식으로 갱신.** 첫머리 상태 등급, 한 것, 다음 것(내일 세션이 첫 지시로 그대로 쓸 수 있는 문장), 막힌 것과 버린 길, 비정상·임시 상태(꺼둔 검사, 반쯤 바꾼 구조). 파일 경로와 PR 번호는 포인터로.
> 3. **노트북 전용 도구 표시.** `hx_lite.sh`와 SSH 별칭 `shinpc`는 노트북에서만 됩니다. 신PC에서는 헤르메스가 로컬이니 `hermes ...`를 직접 쓰세요(위 대응표). `scripts/ask_dialog.py`는 신PC에서도 되는지 확인해 결과를 적으세요.
> 4. **비공개 사본 경로를 인수인계에 적으세요.** 예: `simplifier-cxo-db/ink/리서치/2026-09-20-PM과-PL-딥리서치-헤르메스원본사본.md`. 신PC에만 있는 헤르메스 보고서(PM/PL 등)도 위치를 적으세요. 공개 저장소 push는 내용이 공개 가능한지 마야가 판단합니다.
> 5. **Claude 메모리는 옮겨지지 않습니다.** 마야가 세션 기억에만 두고 있던 규칙이 있으면 `역할노트-마야.md`나 `진행상황.md`로 옮기세요(역할 노트 80줄 이하 유지).
> 6. **세션 소통 변경.** 탐이 신PC로 가면 같은 PC 소켓 메시징이 안 됩니다. 급한 일은 우편(`--ask`) 뒤 즉시 배달로, 사장님 컨펌은 클로드 모바일에서 해당 세션에 직접 받으세요.
> 7. 끝나면 **새 세션에서 "하이~"**로 시작하고, 역할 노트와 진행상황 [마야] 블록만 읽으세요.

## 5. 이동 뒤 첫 확인 (탐)
1. 신PC에서 `hermes cron list`로 크론 9개가 보이는지, `pending-detector`와 `solar-bible-long-poller`가 정상인지.
2. 신PC 저장소 `git pull` 뒤 `docs/진행상황.md`의 탐(로컬) 블록이 2026-09-20 판인지.
3. `C:\Users\PC\work\hansunghee7.github.io`의 `.claude` 설정(훅)이 신PC에서 동작하는지(경로가 `$CLAUDE_PROJECT_DIR` 기준이라 문제 없을 것으로 보이나 **미확인**).
