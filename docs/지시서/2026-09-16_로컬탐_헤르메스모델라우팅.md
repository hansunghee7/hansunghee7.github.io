# 헤르메스 모델 자동 라우팅 조사·1차 구현 지시서 (2026-09-16)

> ✅ 0~10절 완료 (2026-09-17). 최종 보고(원안 14절 13개 항목)는
> [진행상황.md](../진행상황.md)의 "탐(로컬) (2026-09-17): 헤르메스 모델
> 라우팅 지시서 최종 보고" 절 참고. 10.1(Main fallback)·10.2(Ollama
> 배정)·10.3(2차 Router) 전부 실측 완료, 결과와 남은 판단거리는
> 진행상황.md의 해당 세 절(2026-09-17, 탐(로컬)) 참고. 10.2는 품질
> 미달, 10.3은 자동(무지시) 위임 신뢰도 낮음 — 둘 다 사장님 판단 대기
> 중이라 "완료"가 "문제없음"을 뜻하지 않는다. **11절 추가(2026-09-17,
> 사장님 결정 3건 반영)**: saegim 값 재확인, qwen2.5vl:7b 재검증, 전처리
> 래퍼 착수 승인. 11절부터 이어서 진행할 것.

발신: 탐(클라우드 세션) / 수신: 탐(로컬, 신PC) / 전달·승인: 사장님

목표: 헤르메스가 작업 난이도에 따라 비용이 다른 모델을 자동으로 골라 쓰게
한다. 사장님이 원안을 직접 작성했고(아래 원문 그대로 보존), 이 문서는 거기에
이 저장소가 이미 확보한 실측값과 이 환경의 제약(권한 분류기)을 얹은 것이다.
**공식 문서에 없는 기능을 있는 것으로 가정하지 않는다**(원안의 핵심 전제와
동일하다).

## 0. 이 세션이 이미 확보한 사실 (재조사하지 말 것)

- **전략 방향(2026-09-16, 사장님 결정): 헤르메스는 상시 가동으로
  포지셔닝한다.** "필요할 때 여는 도구"가 아니라 "항상 켜져 있는 서비스"
  전제로 이후 모든 인프라 결정(LiteLLM 프록시 등)을 내린다. 이 전제
  때문에 헤르메스에 의존적인 구조(예: 프록시를 헤르메스 기동 스크립트에
  결합)를 채택해도 괜찮다는 판단이 성립한다.
- 🚨 **최우선(2026-09-16, 사장님 확인): openai-codex(ChatGPT) provider를
  헤르메스에서 뺀다.** OAuth가 사장님 계정이 아니라 **와이프 계정**에
  걸려 있었고, 와이프가 오늘 챗지피티 플러스를 새로 시작했는데 헤르메스의
  자동 호출만으로 **당일 주간 사용량 80% 소진**을 확인함(모델은 Sol 등급).
  **조치 방향 변경(2026-09-16, 사장님 지시)**: 챗지피티 쪽에서 연결 앱을
  찾아 해제하는 방법은 메뉴 위치가 불확실해 느리다고 판단, **헤르메스
  설정에서 openai-codex provider 자체를 모델 선택에서 빼는 쪽으로 바로
  간다.**
  1. **로컬 탐**: `config.yaml`의 `model.provider: openai-codex`를 다른
     provider(이미 사장님 계정으로 인증된 `nous` 등, 신솔라 폴링 크론과
     동일)로 바꾸는 정확한 변경값을 확인·준비한다. 가능하면 직접 적용,
     이전에 config.yaml 직접 수정이 권한 분류기에 두 번 막힌 전례가
     있으니 강행하지 말고 막히면 변경값을 스크래치패드에 남겨 "사장님
     직접 실행" 항목으로 넘긴다.
  2. 적용 후 `hermes config get model --json`으로 provider가 실제로
     바뀌었는지 확인(DONE 기준).
  3. 챗지피티 쪽 연결 앱 해제는 보류(급하면 나중에 별도로). 이 항목은
     이 지시서의 다른 모든 항목(제미나이 풀 포함)보다 먼저 처리한다.
  4. **실행 경로 추가(2026-09-16, 사장님이 터미널에서 직접 실행)**: 위
     0절 규칙 5("`/model`은 이미 설정된 provider/model 사이를 전환할 수
     있다")를 이용한 방법. config.yaml 직접 수정 대신 이 방법을 먼저
     시도한다.
     1. 헤르메스가 실행 중인 터미널 창을 연다(꺼져 있으면 새로 `hermes`
        실행).
     2. 입력창에 `/model` 입력 후 Enter.
     3. 화면에 뜨는 provider 목록에서 원하는 provider/model을 선택
        (화살표+Enter 또는 번호 입력, 실제 화면에 나오는 방식 그대로
        따른다).
     4. **확인됨(2026-09-16, 사장님 실측)**: 선택하면 "Model switched:
        ... (session only - add --global to persist)"라는 메시지가 뜬다.
        즉 **`/model`은 기본적으로 세션 한정 적용**이고, 영구 반영하려면
        `--global` 옵션을 붙여야 한다(정확한 붙이는 위치는 CLI 도움말로
        확인 - `/model --global`처럼 명령에 붙이거나, picker 안에 별도
        옵션이 있을 수 있음).
     5. **검증(중요)**: `--global`로 적용한 뒤 같은 또는 새 터미널에서
        `hermes config get model --json` 실행 → 출력의 `provider`/`model`이
        의도한 값으로 바뀌었는지 확인. 세션만 바꾸고 검증을 건너뛰면
        재시작 시 원래 openai-codex로 되돌아간다.
- 신PC `config.yaml`의 현재 `model:` 섹션: `provider: openai-codex`,
  `default: gpt-5.6-sol-900k`, `reasoning_effort: medium`, `thinking: false`.
  `fallback_model:`은 파일에 예시 주석만 있고 실제 설정은 비어 있음(2026-09-15
  사장님 확인).
- `llm:` 섹션에 별도로 `provider: ollama`, `model: qwen2.5-coder:14b`,
  `endpoint: http://127.0.0.1:11434/v1`가 이미 설정돼 있음. **로컬 무료
  모델이 이미 하나 붙어 있다.** 어떤 하위 기능(STT? 다른 용도?)이 이걸
  쓰는지는 미확인이니 이번 조사 대상에 포함할 것. 저비용 delegation 후보로
  가장 먼저 검증 가치가 있음(API 키·과금 없음).
- **재현된 문제(2026-09-16 야간 2차 실측)**: 기본 설정 그대로 ad-hoc 직접
  호출(`hermes chat --query-file -`)을 하면 **HTTP 400**으로 실패함
  (`hermes doctor` 이슈 1번과 일치). 반면 신솔라 폴링 크론은 생성 시점의
  provider 스냅샷(`provider: nous`)을 그대로 쓰기 때문에 이 문제와 무관하게
  계속 정상 동작 중. 즉 **현재 config.yaml의 `model.provider: openai-codex`
  자체가 ad-hoc 호출 경로에서 깨져 있을 가능성이 있다.** 원안 8절이 요구하는
  "provider가 openai-codex로 설정됐는가/OAuth가 유효한가" 조사는 이 재현부터
  출발할 것.
  **재조정(2026-09-16, ChatGPT 사고로 provider를 nous로 교체)**: 위 8절
  Codex OAuth/모델 목록 조사는 provider 자체가 바뀌면서 의미가 없어졌다.
  2절의 Codex 관련 조사 항목은 재검토 후 skip하거나 nous 기준으로
  다시 잡을 것 (재조사하지 말라는 뜻이 아니라, 대상이 바뀌었다는 뜻).
- **인코딩 환경 이슈(2026-09-16, 로컬 탐 실측)**: 이 PC의 기본 코드페이지
  (cp949)가 한글 주석이 있는 YAML(예: 위 litellm 설정 파일)을 그대로
  실행하면 `UnicodeDecodeError`가 난다. `PYTHONUTF8=1
  PYTHONIOENCODING=utf-8` 환경변수를 붙여서 실행하면 해결된다. 이 PC에서
  한글 주석이 있는 파일을 다루는 Python 실행에는 앞으로 이 두 환경변수를
  기본으로 붙일 것.
- **환경 제약(중요, 원안에 없던 항목)**: 이 PC에서 Claude Code 세션이
  `hermes cron edit`이나 `config.yaml` 직접 수정을 시도하면 권한 분류기가
  "미승인 영속 변경/공유 자원 수정"으로 **두 번 연속 차단**했다(살아있는
  크론이 참조하는 파일이라는 판단으로 보임). **이번 작업도 config.yaml에
  손대는 단계(auxiliary/delegation 설정 추가 등)에서 같은 차단을 만날
  가능성이 높다.** 차단되면 강행하지 말고, 적용할 정확한 YAML 조각과 실행할
  명령을 스크래치패드 파일로 준비해 "사장님 직접 실행" 항목으로 넘긴다
  (아래 크론 프롬프트 사례와 동일한 패턴).
- **무료 모델 후보 추가(2026-09-16, 클라우드 세션 실측)**: 공식 Gemini API를
  인증 없이 직접 호출해 확인함. `generativelanguage.googleapis.com/v1beta/models`가
  살아있고, `gemini-flash-latest`/`gemini-2.5-flash-lite`/`gemini-pro-latest`
  전부 입력 1,048,576 토큰, 출력 65,536 토큰. OpenAI 호환 엔드포인트
  (`/v1beta/openai/chat/completions`)도 존재 확인(인증 없이 호출 시 정상적인
  "Missing Authorization header" 오류 응답). 즉 API 키만 있으면 Hermes의
  "커스텀 OpenAI 호환 엔드포인트" 방식(config.yaml 주석의 `base_url`+`key_env`)
  으로 붙일 수 있을 가능성이 높다. **미확인**: 무료 티어의 실제 분당/일일
  한도(`ai.google.dev`가 이 클라우드 세션에서 네트워크 차단이라 못 봄, 사장님이
  API 키 발급 화면에서 직접 확인 필요). `multi-llm-handoff` 규약의 "계정 간
  자동 라우팅·할당량 우회 금지" 조항은 세션 쿠키 추출 등 약관 우회 기술에만
  적용되고, 이 공식 API 경로는 해당 없음(같은 날 규약 갱신, 근거는
  `.claude/skills/multi-llm-handoff/SKILL.md` 참고).
- **계정 3개, 단일 공용 풀(2026-09-16, 최종 확정, v7)**: v6에서 잉크·
  쇼츠랩에 계정을 전담 배정했던 구조를 되돌린다(사장님 지적: 이 풀은
  프로젝트 2개가 아니라 **전 에이전트(탐·마야·시안·노트·페이브·핏·
  헤르메스)가 쓰는 공용 자원**이라 프로젝트별 배정은 범위를 잘못 잡은
  것). 계정 3개를 **같은 `model_name` 하나(`gemini-pool`)로** LiteLLM에
  등록하고, 누구든 이 이름 하나만 호출한다. `routing_strategy:
  simple-shuffle`, `cooldown_time: 60`, `num_retries: 3`. 키 하나가
  한도에 걸리면 자동으로 다음 키로 재시도(LiteLLM 기본 기능). 검증
  전용으로 호출량이 낮아진 상태라 이걸로 충분하다고 판단(부족하면
  재검토). `api_base: https://generativelanguage.googleapis.com/v1beta/openai/`,
  키는 `GEMINI_API_KEY_1`/`_2`/`_3`. 설정 예시와 배경은
  아티팩트(https://claude.ai/artifact/FM86igrvHaA6H5ztKM6g2G) 1절 참고.
  키 3개는 사장님이 로컬 탐에게 직접 전달(채팅에 남기지 않음, 이번엔
  용도 구분 없이 그냥 3개).
- **키 1개는 이미 있음, 먼저 착수(2026-09-16, 사장님 지시)**: "심플리파이어
  다이얼로그"(`insight-7b3e9f2c/ask.html` → Cloudflare Worker
  `simplifier-ask.simon-8be.workers.dev`, 소스는 `simplifier-agent`
  저장소 `worker-ask.js`)가 이미 `GEMINI_API_KEY`라는 이름의 무료 티어
  키로 `gemini-flash-lite-latest`를 실서비스에 쓰고 있다(무료 티어
  503 과부하 응답을 재시도로 흡수하는 코드까지 이미 있음. 딥리서치
  수치보다 신뢰도 높은 실측 증거). 이 키는 Cloudflare Worker Secret이라
  값을 다시 못 읽으므로(write-only), **사장님이 Google AI Studio 키
  목록에서 같은 키를 찾아 값을 로컬 탐에게 전달**하면 `GEMINI_API_KEY_1`
  로 바로 쓴다. 3개를 다 모을 때까지 기다리지 말고, 이 키 1개로 먼저
  `model_list`에 1개 항목만 등록해 실제로 되는지부터 검증한다(DONE
  기준: 이 항목으로 실제 chat completion 1건 성공). 나머지 2개는
  사장님이 오늘 중 추가 전달 예정, 도착하는 대로 `model_list`에 항목
  추가. 모델명은 기존 실서비스와 맞춰 `gemini-flash-lite-latest`를
  기본으로 검토(원래 검토했던 `gemini-flash-latest`와 별도 모델이니
  둘 다 후보로 두고 실측 비교해도 됨).
- **할당량은 키 단위가 아니라 계정(프로젝트) 단위(2026-09-16, 사장님 확인)**:
  Google AI Studio "프로젝트 세부정보" 화면 확인 결과, 이 키는 프로젝트 ID
  `gen-lang-client-0966583500`("Simplifier Project")에 속해 있고, **같은
  계정 안에서 키를 몇 개 더 만들어도 처리량은 늘지 않는다**(할당량이
  계정/프로젝트 단위로 공유되기 때문). 즉 이 키 1개 = 3개 슬롯 중 1개
  몫이다. 나머지 2개 슬롯은 이 계정에서 키를 추가 발급하는 방식이 아니라,
  **서로 다른 구글 계정 2개**에서 각각 키를 새로 발급받아야 실제 처리량이
  늘어난다. 원래 설계(계정 3개)와 방향은 같으므로 구조 변경은 없다.
  "같은 계정에서 편하게 키만 늘리기"를 시도하지 말라는 확인일 뿐이다.
- **LiteLLM 설정 파일 완성(2026-09-16, 클라우드 탐이 구축)**: 사장님 지시로
  풀 구축은 클라우드 탐이 먼저 끝내고, 로컬 탐은 비밀키 등록만 맡는다.
  완성된 설정 파일: `docs/지시서/litellm_config.gemini-pool.yaml`(값 없이
  `os.environ/GEMINI_API_KEY_1`/`_2`/`_3` 참조만, 모델은 실서비스와 동일한
  `gemini/gemini-flash-lite-latest`, `routing_strategy: simple-shuffle`,
  `num_retries: 3`, `cooldown_time: 60`). LiteLLM 네이티브 `gemini/` 프로바이더를
  써서 별도 `api_base` 없이 키만 넣으면 동작하는 구조다(OpenAI 호환 베타
  엔드포인트보다 검증된 경로).

  **로컬 탐이 할 일(비밀키 쪽만)**:
  1. 이 저장소를 pull해서 `docs/지시서/litellm_config.gemini-pool.yaml`을
     그대로 사용(수정 불필요).
  2. 사장님이 전달한 키 3개를 로컬 환경변수로 등록:
     `export GEMINI_API_KEY_1=...`, `_2`, `_3` (또는 로컬 `.env`, 이 저장소
     밖에 저장 - 커밋 금지).
  3. `pip install 'litellm[proxy]'` 후, 아래 두 환경변수를 먼저 설정한다:
     - `GEMINI_API_KEY_1`/`_2`/`_3` (기존과 동일)
     - `LITELLM_MASTER_KEY`: 임의의 긴 랜덤 문자열(사장님이 직접 생성,
       **이 값은 어디에도 커밋하지 않는다** - 로컬 `.env`에만).
     그 다음
     `litellm --config docs/지시서/litellm_config.gemini-pool.yaml --port 4000 --host 127.0.0.1`
     으로 프록시 실행(`--host 127.0.0.1`은 이 PC 밖에서 이 포트에 접근 못
     하게 막는 것 - 실제로 이 플래그가 지원되는지는 `litellm --help`로
     먼저 확인, 안 되면 OS 방화벽에서 4000번 포트 인바운드를 로컬로만
     제한).
  4. 검증(DONE 기준, `master_key` 적용 후에는 헤더 필요):
     `curl http://localhost:4000/chat/completions -H "Content-Type: application/json" -H "Authorization: Bearer $LITELLM_MASTER_KEY" -d '{"model": "gemini-pool", "messages": [{"role": "user", "content": "hi"}]}'`
     가 실제 응답을 반환하는지 확인. 성공 시 진행상황.md에 실제 응답 일부
     또는 HTTP 200 캡처를 증거로 남긴다.
  5. 이후 Hermes `config.yaml`에서 이 프록시(`http://localhost:4000`)를
     커스텀 OpenAI 호환 엔드포인트로 연결하는 작업은 별도 단계(위 "환경
     제약" 항목 참고, 권한 분류기 차단 가능성 있음). 이때도
     `Authorization: Bearer <LITELLM_MASTER_KEY>` 헤더를 같이 설정해야
     한다.
  6. 키가 1개만 먼저 도착했다면 `model_list`에서 `_2`/`_3` 항목을 주석
     처리하고 1개로 먼저 검증, 나머지 도착 시 주석 해제.
  7. **상시화, 헤르메스 기동 스크립트 편입으로 확정(2026-09-16, 사장님
     결정)**: **헤르메스 자체를 상시 가동으로 포지셔닝**하기로 했으므로
     ("헤르메스가 항상 켜져 있어야 우리 퍼포먼스가 극대화된다"), 이
     프록시를 헤르메스 자체 기동 스크립트에 끼워 넣는다. Windows 작업
     스케줄러 방식은 채택하지 않는다. 상태 모니터링·자동 재시작 같은
     추가 인프라는 지금 단계에서 만들지 않는다(과설계).
  8. **외부 유출 안전장치(2026-09-16, 사장님 지시)**: 위 `--host
     127.0.0.1` 바인딩 + `LITELLM_MASTER_KEY` 두 가지가 안전장치다. 주소가
     외부에 알려져도 마스터 키를 모르면 호출이 거부되고, 애초에 이 PC
     밖에서는 포트 자체에 도달하지 못한다. 마스터 키 값은 공개 저장소
     문서(이 지시서·진행상황.md 포함)에 절대 적지 않는다 - 이 PC의 로컬
     `.env`에만 두고, 같은 PC에서 도는 에이전트(로컬 탐, 로컬 마야 등)는
     그 파일을 직접 읽어 쓴다.
- **역할 재배치(2026-09-16, 사장님 제안)**: 제미나이를 1차 지식 정제
  계층으로 쓰지 않는다. **1차 조사는 헤르메스+클로드 코드가 자체적으로
  하고, 제미나이는 그 결과를 검증하는 용도로만 쓴다**(비용·호출 낮춤).
  아티팩트 1·3절 참고. 헤르메스 delegation/auxiliary 설정 조사(2절)를 할
  때, 제미나이는 "delegation 저비용 실행" 자리가 아니라 "검증 단계" 자리에
  배치할 것을 우선 검토한다.

## 1. 공통 규칙 (전 지시서와 동일)

- 과제 하나 끝날 때마다 진행상황.md 탐 절에 결과 기록 후 즉시 커밋·PR.
- 검토·수집은 하이쿠·소넷 서브에이전트에 위임, 세션은 판정만.
- 컨텍스트 70% 넘으면 그 자리에서 저녁 의식.
- 이 저장소(공개)에는 실제 IP·호스트명·토큰·계정명을 쓰지 않는다.
  자리표시자만. 기술 상세(모델 ID 목록, 실제 provider 값)는 문제없지만,
  인증 정보·URL·개인 식별자는 절대 적지 않는다.
- 막히면 최대 3회 재시도 후 "막힌 것"에 원인 기록, 다음 과제로.

**금지 목록**: 존재 확인 안 된 provider/model을 config에 입력, 매 턴
`/model` 강제 전환, 검증 안 된 라우팅을 운영 크론에 바로 적용, 기존 Solar
provider 제거, OAuth credential 노출, 지금 도는 Gateway/Telegram 설정을
이 작업과 무관하게 변경, 전체 config.yaml 리팩터링.

## 2. 조사 (원안 2절, 위 0절과 중복되는 항목은 건너뛰고 나머지만)

- `hermes status`, `hermes config get model --json`, `hermes doctor` 전체
  출력 확보.
- OpenAI Codex OAuth 연결 상태 확인 (원안 8절 그대로).
- 실제 사용 가능한 Codex 모델 목록 확인. Luna/Terra/Sol/Astra라는 이름이
  실제 어떤 모델 ID에 대응하는지, 추측하지 말고 CLI 출력 근거로 기록.
- 사장님이 이미 골라둔 무료 모델 provider/모델명 확인(사장님께 물어서 확보,
  없으면 이 항목은 "미확보"로 남기고 진행).
- `agent.personalities`(현재 `{}`로 비어 있음)가 모델별 역할 분리 기능인지
  공식 문서로 확인한다. 없으면 근거 없이 이 필드를 쓰지 않는다.
- Auxiliary(compression/vision/approval/skill search) 설정 현황과
  `provider:auto` 기본 동작 확인.
- Delegation 설정(`delegation.model`/`provider` 지정 가능 여부, 현재
  `delegation.max_iterations: 250`만 있고 모델 지정 필드는 미확인) 확인.

DONE 기준: 위 각 항목의 실제 출력값 또는 "미확보/확인 불가"가 진행상황.md에
한 줄씩.

## 3. 설계 원칙 (원안 3절 그대로 적용)

Main model 자동교체(매 턴 `/model` 전환)를 먼저 만들지 않는다. 1차는
**delegation 기반 위임**으로 시작한다:

```
Main model (판단·복잡한 작업)
    ↓ 위임
간단한 독립 작업 → 저비용 delegation 모델(위 0절 ollama 후보 우선 검토)
```

## 4. 1차 구현: Delegation 기반 (원안 4절)

- delegation 대상으로 위 0절의 로컬 Ollama 모델(`qwen2.5-coder:14b`)을 먼저
  시도한다. 이미 붙어 있고 무료이므로 새 provider/키 없이 검증 가능하다.
  안 되면(품질·속도 문제) 원안대로 사장님이 고른 무료 모델로 대체.
- 위임 대상 작업 예시(원안 그대로): 파일 목록 확인, 간단한 코드 탐색, 형식
  변환, 짧은 요약, 단순 반복 작업.
- config.yaml에 delegation 모델을 지정하는 단계에서 권한 분류기가 막히면,
  위 0절 안내대로 정확한 설정값을 스크래치패드에 남기고 "사장님 직접 실행"
  으로 넘긴다. 강행하지 않는다.

## 5. 2차 구현: 난이도 Router (원안 5~7절)

1차가 안정 동작을 확인한 뒤에만 검토한다. 원안의 EASY/NORMAL/HARD/CRITICAL
분류 기준, 매핑, 안전장치(EASY는 상위 모델 호출 안 함, CRITICAL은 자동 실행
대신 승인 여부 판단, 판단 불가 시 Main model로 fallback)를 그대로 따른다.
A안(delegation 대상을 Router가 선택) vs B안(새 세션 시작 시 Main model
선택) 비교 후 A안부터 시도한다. 세션 중 매 턴 강제 전환(원안의 3순위)은 이번
지시서 범위 밖으로 둔다.

## 6. 테스트 (원안 11절, 순서 그대로)

Test 1(단순 조회) → Test 2(3~5개 파일 수정) → Test 3(복잡한 버그) →
Test 4(아키텍처 변경, 자동 실행하지 말고 판단만) → Test 5(저비용 모델 실패
시 fallback). 각 테스트에서 실제 사용된 provider/model을 로그로 남긴다.
Test 4는 실제로 위험한 변경을 실행하지 말고 "이 경우 CRITICAL로 분류되고
승인이 필요하다"는 판단까지만 확인한다.

## 7. 보고 형식 (원안 14절)

완료 후 진행상황.md에 원안 14절의 13개 항목을 채워서 보고한다. 그중
"실제 사용 가능한 모델 목록"과 "실제 사용된 모델 증거"는 추측이 아니라 이번
조사·테스트에서 나온 CLI 출력/로그를 근거로 남긴다.

## 8. 마감

- 진행상황.md 탐 절에 CLAUDE.md 템플릿대로 인수인계.
- 아래 기존 대기열과 순서 조율(이 작업이 그것들을 밀어내지 않음):
  1. 크론 프롬프트 댓글 단계 반영: 사장님 실행 대기 중(별건)
  2. 과제 4 웨이크 검증, 페이브 PROCESS.md 게이트 편입: 탐(클라우드) 담당
  3. F0-3 나머지 실측: 사장님 승인 대기 중(별건)
  4. **이 작업(헤르메스 모델 라우팅)**: 신규, 위 순서와 병행 가능하다(서로
     다른 파일·크론을 건드림)
- push까지 끝낸 뒤 종료.

## 9. 후속 지시: fallback_providers 설정 (2026-09-17, 사장님 결정)

배경: 6절 Test 5에서 delegation 대상(gemini-pool)이 죽으면 자동 fallback이
없고 3회 재시도 후 사람에게 되묻는다는 것이 확인됐다(헤르메스 공식 문서상
`fallback_providers` 미설정 시의 기본 동작 — 버그 아님). 사장님 결정:
"무료면 Main 모델 추가 좋습니다" — 현재 Main model(provider: opencode-free,
무료 확인됨)을 `delegation.fallback_providers`에 추가한다.

할 일:
1. `config.yaml`의 delegation 설정에 `fallback_providers`를 추가해 저비용
   모델(gemini-pool) 장애 시 Main model(현재 provider: opencode-free)로
   자동으로 넘어가게 한다. 정확한 필드명·문법은 추측하지 말고 공식 문서로
   재확인 후 적용(이 지시서 0절 전제와 동일한 원칙).
2. 권한 분류기가 `config.yaml` 직접 수정을 막으면(0절에서 이미 겪은 전례와
   동일), 강행하지 말고 정확한 변경값을 스크래치패드에 남겨 "사장님 직접
   실행" 항목으로 넘긴다.
3. 검증(DONE 기준): gemini-pool을 되돌리기 쉬운 방법으로 일부러 실패시킨
   뒤(예: 키를 임시로 잘못된 값으로 교체 후 원복), delegation 호출이 실제로
   Main model로 넘어가는지 로그로 확인한다. 설정만 하고 "이론상 될 것"으로
   끝내지 않는다.
4. 완료 후 진행상황.md 탐 절에 CLAUDE.md 인수인계 템플릿대로 기록.

Test 2에서 발견한 "raw bash에 Windows 백슬래시 경로를 넘기면 이스케이프로
깨지는" 버그는 cxo-db [#110](https://github.com/hansunghee7/simplifier-cxo-db/issues/110)에
기록 완료, 이미 파일을 즉시 삭제해 조치도 끝났으므로 추가 작업은 없다.
앞으로 헤르메스에 Windows 경로를 지시할 때는 raw bash 대신 `read_file`/
`patch` 같은 전용 파일 도구를 우선 쓴다.

---

## 10. 2차 확장 지시 (2026-09-17, 사장님 결정: 세 건 모두 진행)

로컬 탐의 최종 보고에서 올라온 세 가지 다음 우선순위 질문에 사장님이 전부
"진행"으로 답했다. 순서는 아래 번호대로(①이 ②·③의 전제는 아니니 막히면
다음 걸로 넘어가도 됨).

### 10.1 Main model(opencode-free) 자체 fallback 추가

지금까지는 delegation(gemini-pool → Main) fallback만 있고, **Main model
자체가 죽었을 때의 대비가 없다.** 사장님 결정: 지금 추가한다.

1. 조사부터: `fallback_providers`가 delegation 레벨과 별개로 Main model
   레벨에도 있는 필드인지 헤르메스 공식 문서로 확인한다(9절과 같은 원칙 —
   문서에 없는 기능 가정 금지). Main 레벨 fallback이 아예 없는 기능이면
   "미지원"으로 결론짓고 대안(예: 헬스체크 후 `/model --global` 수동 전환
   안내 등)을 제안만 하고 구현은 보류.
2. fallback 대상 후보: 이전에 이미 인증됐던 `nous`가 1순위 후보(0절
   참고, ChatGPT 사고 전 실제 사용 이력 있음). 다른 후보가 있으면 실제
   인증 상태를 확인하고 고른다. 무료 Ollama(`qwen2.5-coder:14b`)는 §10.2
   결정으로 다른 용도(마야 리서치 delegation)에 배정됐으니 여기 후보에서
   제외.
3. 권한 분류기 차단 시 0절 패턴대로 처리(스크래치패드에 값 남기고 "사장님
   직접 실행"으로 이관).
4. 검증(DONE 기준): opencode-free를 되돌리기 쉬운 방법으로 일부러
   실패시킨 뒤(예: 잘못된 인증값으로 임시 교체 후 원복), 실제 요청이
   fallback 대상으로 넘어가는지 로그로 확인. 9절 검증과 같은 수준.

### 10.2 로컬 Ollama(qwen2.5-coder:14b)를 로컬 마야의 리서치 delegation으로 배정

사장님 결정: 이 모델은 **로컬 마야의 "얇은 글"(195편 관련) 적용 가능한
리서치 용도**로 쓴다. 조사가 아니라 배정이 확정된 상태이니 바로 연결
작업으로 들어간다.

1. 마야의 리서치 워크플로 지시서(이미 작성돼 있음, 진행상황.md에서
   "로컬 마야 리서치 워크플로" 절 확인)에 이 provider(`llm:` 섹션의
   `provider: ollama`, `model: qwen2.5-coder:14b`,
   `endpoint: http://127.0.0.1:11434/v1`)를 delegation 후보로 명시한다.
   기존 델리게이션 설정과 충돌하지 않는 별도 경로인지 확인.
2. 실제 로컬 마야 세션(또는 로컬 탐이 마야 역할로 대신 검증해도 됨,
   단 최종 검증은 마야 쪽 결과로 다시 한번 확인)이 이 모델로 delegation
   호출 1건을 성공시켜 실제 응답을 받는다(DONE=VERIFIED).
3. 코드 특화 모델이라 리서치·요약 품질이 기대에 못 미칠 수 있다 — 실제
   출력을 사람이 한 번 눈으로 보고 "얇은 글 리서치에 쓸 만한지" 판단
   기록을 남긴다. 품질이 안 나오면 이 배정을 되돌릴 수 있다고 진행상황.md에
   명시(뒤집을 신호로).
4. 완료 후 진행상황.md에 마야 앞으로 인수인계(어떻게 호출하는지, 검증
   결과 포함).

### 10.3 2차 난이도 Router, ASAP 착수

원안 3절의 게이팅 원칙("1차가 안정 동작을 확인한 뒤에만 2차 검토")을
사장님이 이번 결정으로 명시적으로 해제했다 — 1차(delegation)가 5/5
테스트를 통과했으니 곧바로 2차로 넘어간다.

1. 원안 5~7절 그대로 따른다: EASY/NORMAL/HARD/CRITICAL 분류 기준,
   Luna/Terra/Sol/Astra 매핑(단 실제 모델명은 이 지시서 0절 실측값 기준으로
   재해석 — 원안 이름이 아니라 실제 provider/model ID로 적용), 안전장치
   (EASY는 상위 모델 호출 안 함, CRITICAL은 자동 실행 대신 승인 여부
   판단).
2. A안(delegation 대상을 Router가 선택)부터 시도, 매 턴 강제 `/model`
   전환(3순위, 원안 6절)은 여전히 범위 밖.
3. 최소 4개 케이스(EASY/NORMAL/HARD/CRITICAL 각 1개)로 실측, 실제
   분류 결과와 선택된 provider/model을 로그로 남긴다. 6절 Test 1~5와
   같은 형식으로 진행상황.md에 기록.
4. CRITICAL 케이스는 6절 Test 4와 동일하게 실제 위험한 작업을 실행하지
   않고 "승인 필요" 판단까지만 확인한다.

완료 후 진행상황.md 탐 절에 CLAUDE.md 인수인계 템플릿대로 기록, 세
항목(10.1~10.3) 중 못 끝낸 게 있으면 이유와 함께 다음 세션으로 이월.

---

## 11. 후속 지시 (2026-09-17, 사장님 결정)

### 11.1 saegim 연결 재확인: 이름 문제 아님, 값 문제로 좁혀짐

클라우드 세션이 `.mcp.json`(simplifier-saegim 저장소)과 사장님이 실제
등록한 환경변수 이름을 대조했다: `SAEGIM_MCP_URL`/`SAEGIM_TENANT_ID`/
`SAEGIM_TENANT_SECRET` 전부 정확히 일치한다. 즉 "환경변수 이름이 틀렸다"는
가설은 틀렸다. 그런데도 401(`AUTH_HEADER_REJECTED`, invalid_token)이
계속 나므로, 남은 원인은 **값 자체**다.

할 일:
1. simplifier-saegim 쪽에서 Supabase `tenants` 테이블(또는 해당 프로젝트의
   테넌트 관리 화면)에 `tenant_id = 'dogfood'`로 등록된 실제
   `tenant_secret` 값을 확인한다.
2. "새김 도그푸딩" 환경변수의 `SAEGIM_TENANT_SECRET` 값과 일치하는지
   대조한다(2026-09-14엔 이 조합으로 실제 성공했었으니, 그 뒤 서버 쪽
   비밀값이 재발급됐거나 복사 과정에서 값이 잘렸을 가능성이 유력하다).
3. 불일치하면 dogfood 테넌트 비밀값을 다시 발급(또는 기존 값 재확인)해서
   환경변수를 갱신한다. 값 자체는 이 저장소(공개)에 절대 적지 않는다.
4. 검증(DONE 기준): 그 환경에서 뜬 세션이 saegim MCP 도구로 실제
   lookup 1건을 성공시킨다.

### 11.2 마야 리서치용 로컬 모델을 qwen2.5vl:7b로 재검증

사장님 의견: "qwen2.5vl:7b 쓰면 어떤가요, 로컬 마야 평이 좋습니다." (이
모델은 195편 웹툰 OCR에서 이미 검증된 비전 모델이지, 10.2에서 테스트한
코드 특화 모델 qwen2.5-coder:14b와는 다른 모델이다.) 코드 전용이 아니라
일반 지시 이해력이 더 나을 가능성이 있어 타당한 제안이다.

할 일:
1. 10.2에서 실패했던 같은 두 과제(SEO 얇은 콘텐츠 판단, 한국어 텍스트
   사실 3개 추출)를 qwen2.5vl:7b로 그대로 재실행한다(같은 엔드포인트
   `http://127.0.0.1:11434/v1/chat/completions`, 모델명만 교체).
2. 한국어 지시 준수 여부, 숫자 환각 여부를 10.2와 같은 기준으로 사람이
   직접 눈으로 판단한다.
3. 통과하면 마야에게 이 모델로 인수인계(호출법 포함). 여전히 미달이면
   그 결과를 그대로 기록하고 다른 로컬 모델(`hermes3:8b` 등)로 넘어간다.

### 11.3 자동(무지시) EASY 위임 전처리 래퍼 착수 승인

사장님 결정: "알아서 위임 진행" — 10.3에서 보류를 추천했던 전처리
래퍼를 새 과제로 시작한다.

할 일:
1. 요청이 신솔라에 도달하기 전에 난이도(EASY/NORMAL/HARD/CRITICAL)를
   먼저 판정하고, EASY로 판정되면 `delegate_task` 호출을 대신 넣어주는
   래퍼를 설계한다. 어디에 끼워 넣을지(헤르메스 게이트웨이 레벨인지,
   별도 프록시인지)는 헤르메스 공식 아키텍처 문서로 가능한 지점부터
   확인 — 문서에 없는 훅 포인트를 있는 것으로 가정하지 않는다.
2. 오탐(원래 NORMAL 이상인데 EASY로 잘못 분류돼 위임됨) 위험이 가장 크므로,
   분류 기준은 18절 정책 문서 그대로 쓰되 **애매하면 위임하지 않고
   신솔라가 직접 처리하는 쪽으로 보수적으로** 짠다.
3. 검증: 10.3에서 실패했던 것과 같은 자연스러운 EASY 질문("현재 디렉터리에
   파일 몇 개야?" 등)을 명시적 위임 요청 없이 던졌을 때 실제로
   `delegate_task`가 자동으로 호출되는지 확인. 동시에 NORMAL/HARD/CRITICAL
   케이스가 여전히 정책대로 동작하는지(오분류 없는지) 같이 재확인한다.
4. 권한 분류기 차단 시 기존 패턴대로 처리.

완료 후 진행상황.md에 CLAUDE.md 템플릿대로 인수인계.

---

## 부록: 사장님 원안 전문 (그대로 보존, 위 절들이 이걸 요약·보강한 것)

> ※ 이 부록은 원본 지시서 텍스트를 그대로 옮긴 것이다. 위 0~7절과 내용이
> 겹치면 위 절의 실측값이 우선한다.

# Hermes 모델 자동 라우팅 시스템 구축 지시서

## 0. 절대 전제

이번 작업의 목표는 Hermes가 작업 난이도/성격에 따라 적절한 모델을 선택하도록
만드는 것이다.

단, Hermes 기본 기능에 없는 것을 있는 것처럼 가정하지 마라.

현재 공식 Hermes 구조를 기준으로 다음을 사실로 취급한다.

1. Hermes에는 Main model과 Auxiliary model이 있다.
2. Auxiliary model은 compression, vision, approval, skill search 등
   작업별로 분리할 수 있다.
3. Auxiliary의 provider:auto는 기본적으로 Main model을 사용하고, 설정된
   fallback chain을 따른다.
4. Hermes에는 Subagent Delegation이 있으며, 부모 모델과 별도로
   delegation.model/provider를 지정할 수 있다.
5. /model은 이미 설정된 provider/model 사이를 전환할 수 있다.
6. /model로 모델을 바꾸면 해당 세션의 prompt cache가 리셋될 수 있다.
7. OpenAI Codex는 ChatGPT Plus/Pro OAuth로 Hermes에 인증할 수 있다.
8. ChatGPT Plus/Pro의 Hermes 사용량 차감 방식은 공식 문서상 명확히
   문서화되어 있지 않다.

따라서 "Hermes가 기본적으로 Main model을 자동으로 Luna/Terra/Sol/Astra 중
난이도별로 선택한다"고 가정하지 마라.

## 1. 목표

- Luna = 저비용/빠른 작업
- Terra = 일반 실무
- Sol = 고난도 작업
- Astra = 최고난도 작업

단순 작업은 가능한 한 저렴한 모델을 사용하고, 복잡한 작업만 상위 모델을
사용하도록 한다. 단, 실제 모델명/제공 여부/성능 특성은 현재 Hermes
설치환경과 provider가 제공하는 모델 목록을 확인한 뒤 확정한다.

## 2. 먼저 해야 할 일: 현황 조사

코드 수정 전에 반드시 현재 Hermes 환경을 조사한다. 확인 대상: Hermes 버전,
현재 main provider/model, OpenAI Codex OAuth 연결 상태, 현재
fallback_providers, auxiliary 설정, delegation 설정, 사용 가능한 OpenAI
Codex 모델 목록, 현재 config.yaml 실제 위치, 현재 세션/Gateway 실행 방식.
`hermes status`, `hermes config get model --json`, 현재 config.yaml, 현재
auth 상태, 현재 사용 가능한 provider/model 목록을 사실 기반으로 확보한다.
추정해서 쓰지 말고 실제 출력값을 근거로 기록한다.

## 3. 중요한 설계 원칙

Main model 자동교체를 먼저 구현하지 마라. `/model` 전환은 기존 세션의
prompt cache를 리셋할 수 있기 때문에, 매 턴마다 Main model을 변경하는
방식은 피한다. 대신 "작업을 적절한 모델에 위임하는 방식"을 우선한다.

## 4. 1차 구현: Delegation 기반

Hermes 공식 delegation 기능을 우선 활용한다. 부모 모델은 전체 목표와
판단을 담당하고, 간단한 독립 작업(파일 목록 확인, 간단한 코드 탐색, 형식
변환, 짧은 요약, 단순 반복 작업)은 저비용 delegation 모델에게 위임한다.
복잡한 아키텍처 판단, 대규모 코드 수정, 장애 분석, 중요한 의사결정은 Main
model이 맡는다.

## 5. 2차 구현: 난이도 기반 Router 검토

1차 구조가 안정적으로 동작한 후, 작업 복잡도를 사전에 분류하는 Router를
검토한다. 상태: EASY/NORMAL/HARD/CRITICAL → Luna/Terra/Sol/Astra. 이
Router가 Main model을 무조건 세션 중간에 교체해서는 안 된다. A. delegation
대상 모델을 Router가 선택 vs B. 새 세션 시작 시 Main model 선택, 두 방식을
비교한다. 매 턴 강제 `/model` 전환은 3순위.

## 6. Router 판단 기준

작업 단계 수, 필요한 추론 깊이, 코드 수정 규모, 여러 파일 간 의존성, 외부
도구 사용 수, 실패 비용, 보안/운영 위험, 결과 검증 난이도.

- EASY: 단순 조회, 상태 확인, 단일 파일의 단순 수정
- NORMAL: 여러 파일의 일반적인 수정, 테스트 추가, 일반적인 분석
- HARD: 복잡한 버그, 여러 컴포넌트의 연쇄 수정, 아키텍처 변경
- CRITICAL: 운영환경 변경, 데이터 손실 가능 작업, 인증/보안, 대규모
  마이그레이션, 복잡한 시스템 장애

## 7. 안전장치

EASY 작업은 상위 모델을 호출하지 않는다. NORMAL 작업은 Terra 수준을
우선한다. HARD 이상에서만 Sol/Astra를 고려한다. CRITICAL은 자동 실행보다
인간 승인 여부를 함께 판단한다. Router가 판단하지 못하면 임의로 최고
모델을 선택하지 말고 Main model로 fallback한다. 모델 호출 실패 시 Hermes
공식 fallback chain을 활용한다. 존재하지 않는 provider/model을 설정하지
않는다.

## 8. ChatGPT Plus OAuth 관련

provider가 `openai-codex`로 설정됐는가, OAuth 인증이 실제 유효한가, 선택
가능한 Codex 모델 목록이 무엇인가, 현재 설치된 Hermes에서
Luna/Terra/Sol/Astra라는 이름이 실제 어떤 모델 ID에 대응하는가를 검증한다.
모델명은 추측하지 말고 실제 provider model 목록을 확인한다. ChatGPT
Plus의 사용량이 Hermes에서 어떻게 차감되는지는 공식 문서가 명확히
정의하지 않았으므로, "Plus 사용량이 무제한"이라고 결론 내리지 마라.

## 9. 무료 모델 fallback

무료 모델을 추가하고 싶다면 우선 현재 Hermes가 실제로 지원하는
provider/model을 조사한다. 무료 모델 후보를 임의로 고정하지 마라. 확인 후
Main/Delegation/Auxiliary/Fallback 각 영역에 어떤 모델을 넣을 수 있는지
제안한다. 무료 모델은 단순 요약, 간단한 분류, 보조 분석, 실패 시
fallback에 우선 사용한다. 중요한 코드 작업의 기본 모델로 무료 모델을
강제하지 않는다.

## 10. 실제 구현 전에 반드시 할 비교

현재 Hermes의 기본 기능만으로 가능한 것과 추가 Router가 필요한 것을
구분해서 보고하라. 표 형식: 기능 | Hermes 기본 제공 | 추가 구현 필요 | 추천.
대상 항목: Main model 자동 난이도 전환, Auxiliary 자동 선택, Fallback, Delegation
model 선택, 난이도 Router, 작업별 비용 제어, 위험도 기반 상위 모델 승급.

## 11. 테스트 순서

한 번에 전체 자동화를 만들지 마라.

- Test 1: 단순 파일 조회 → EASY → 저비용 모델
- Test 2: 3~5개 파일 수정 → NORMAL → Terra 수준
- Test 3: 복잡한 버그 수정 → HARD → Sol 수준
- Test 4: 복잡한 아키텍처 변경 → CRITICAL → Astra/Sol 고려 + 승인 정책 검토
- Test 5: 저비용 모델 실패 → fallback 정상 동작

각 테스트에서 실제 사용된 provider/model을 증거로 남긴다.

## 12. 완료 조건

1. 현재 Hermes 버전과 실제 설정을 확인했다.
2. OpenAI Codex OAuth 연결 상태를 확인했다.
3. 실제 사용 가능한 모델 ID를 확인했다.
4. Hermes 기본 기능과 추가 Router의 역할을 분리했다.
5. Delegation을 이용한 1차 비용 최적화가 동작한다.
6. 최소 4개의 난이도 테스트를 통과했다.
7. 실제 어떤 모델이 사용됐는지 로그/증거가 있다.
8. 실패 시 fallback이 작동한다.
9. 기존 Hermes 기능과 충돌하지 않는다.
10. 현재 신솔라/구솔라 운영환경을 망가뜨리지 않는다.

## 13. 절대 하지 말 것

- 공식 문서에 없는 Hermes 기능을 있는 것으로 가정
- 존재하지 않는 모델 ID를 config에 입력
- ChatGPT Plus 사용량을 무제한으로 가정
- 매 턴마다 /model을 강제로 변경
- 검증되지 않은 자동 라우팅을 운영환경에 바로 적용
- 기존 Solar provider 제거
- OAuth credential 노출
- 현재 동작하는 Gateway/Telegram 설정을 작업과 무관하게 변경
- 전체 구조를 한꺼번에 리팩터링

## 14. 최종 산출물

1. 현재 Hermes 모델 구조
2. 실제 연결된 provider
3. 실제 OAuth 상태
4. 실제 사용 가능한 모델 목록
5. Hermes 기본 제공 기능
6. 추가 구현한 Router
7. 모델별 역할
8. 자동 라우팅 규칙
9. fallback 구조
10. 테스트 결과
11. 실제 사용된 모델 증거
12. 남은 위험/제약
13. 추천 운영 기본값

핵심은 "모델이 많아졌다"가 아니라, "같은 작업을 필요 이상으로 비싼 모델에
보내지 않으면서, 어려운 작업에는 충분한 추론 능력을 자동으로 배정한다"를
달성하는 것이다.
