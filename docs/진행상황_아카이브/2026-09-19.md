# 진행상황 아카이브 (2026-09-19)

## 🟢 탐(로컬) 상태: 🟢정상 (2026-09-19, 백로그 갱신 2차)

한 것:
- 헤르메스 보고서·크론 실측 감사. `relay-watcher` 정지 원인은 삭제된 폴더를
  가리키던 경로(`relay_watcher.py` REPO)였고, 잘못 만든 LLM 크론은 헤르메스가
  기존 스크립트 크론 재개로 정정. H1-1·H1-2·H1-4는 커밋 해시·크론 상태로
  실측 확인, H1-3(`stagnation-detector`)은 등록만 됨(실행 증거는 내일 08:00).
- solar-bible §10 보강 3건: 두 번째 사본 조건(`e2ec4aa`), 백업 불필요 범주
  (`e58c15a`), 헤르메스의 홈페이지 저장소 쓰기 금지와 정본·읽기용 클론 표
  (`0394f61`, 사장님 승인). 모두 solar-bible 저장소 커밋.
- 릴레이 Routine 이름을 "(API /fire)"로 정정(설정·프롬프트는 그대로).
- 헤르메스 발주서 v4(사장님 승인 대기): https://claude.ai/artifact/6nUP3X3PwYyjvgLKPTuV6i
- 발주 ①(ComfyUI 기동 테스트) 실측 대조: 기동, GPU 인식(RTX 4090)은 확인.
  서버 종료(5번)는 보고 시점에 실제로는 실행 중이었고 디스크 속도(6번)는 캐시 값이라 미완. 탐 실측: D(HDD)
  약 185MB/s, C(SSD) 약 1.9GB/s(약 10배). 21.6GiB 모델 1개 로딩이 HDD 약 2분,
  SSD 약 12초 [추정].
- 사장님이 준 원문(고도화 4항목)을 공식 문서(code.claude.com/docs)와 대조해
  판정을 고침: 훅은 `FileChanged`로 외부 파일 변경을 감지할 수 있고, Auto-Memory는
  기기별 로컬 저장.

막힌 것 / 비정상 상태:
- ComfyUI 서버: 16:05 실측으로 종료 확인(8188 무응답, ComfyUI 프로세스 없음).
  다만 헤르메스 보고 시점(종료 완료)과 실제 종료 시점이 달랐으므로 발주 ①은
  보고서 정정 대기.
- 구솔라 Linux 전환 "완결" 보고 vs 구PC 오프라인 1일(실측) → 미확인.
- 발주 ②~⑤ 사장님 승인 대기. 헤르메스 `max_turns 15`로 긴 작업 보고가 잘림.
- 버린 길: 감시 크론을 LLM 프롬프트 작업으로 등록하는 방식(토큰 소모 + 인코딩
  오류). 스크립트 크론이 맞음.

다음 것:
* 사장님: (1) 발주 ②~⑤ 승인·범위, (2) `max_turns` 상향 여부, (3) 숏츠에 쓰는
  모델만 SSD로 옮길지(아래 6번), (4) 진행상황.md 교통정리안(아카이브 분리) 승인,
  (5) 이 PC의 옛 스냅샷 2개(`진행상황.md.local`, `CLAUDE.md.local`) 삭제 승인. 헤르메스 보고서를 붙여 넣을 때 키 원문 줄은 지울 것.
* 탐(다음 세션): 내일 08:00 이후 `stagnation-detector` 첫 실행 로그·모델명 확인,
  발주 결과를 실측 대조해 판정.

우선순위 작업 (탐 백로그, 담당 O, 합의됐으나 미착수)
1. 발주 ②~⑤ 발송·판정 (①은 정정 반영: 종료 원문 필요, 속도는 탐이 실측 완료)
2. LiteLLM 프록시가 죽는 원인 조사: 재시작 15회, 종료 메시지 없음, Ctrl+C 흔적
   1건, 오늘 08:25 키 없는 `chat/completions` 401 호출자 추적. 종료 흔적 계측(로그
   파일 리다이렉트, 종료 코드)이 먼저이고, 범위가 좁은 조사라 effort를 임시 상향한 뒤
   원복. Windows 서비스화 검토.
3. `relay-watcher` 기록에 지연시간(cloud→local) 추가 + 양방향 20건 실측
4. 컴맹 가이드 초안 검증·확정 (여러 세션째 이월)
5. 핏(로컬)에게 `COMFYUI_DIR` 알림: `shorts-lab` 기본 경로
   `C:\Users\PC\Documents\ComfyUI`가 9/18 이관으로 삭제됨. 발주 ① 정정 후.
6. [신규] ComfyUI Active/Archive(시트 S1-2): 숏츠 파이프라인이 실제 쓰는 모델만
   SSD로 옮기는 방안, 사장님 결정. 복사와 검증만 하고 삭제는 하지 않는다.
7. [신규] 가드레일용 짧은 `CLAUDE.md`: `solar-bible`, `simplifier-agent`에는 없음
   (실측). 그 폴더에서 열린 세션이 §10을 읽도록 5줄로 안내. 전역
   `~/.claude/CLAUDE.md`는 기기별이라 최소만(선택).
8. [신규] Auto-Memory 소규모 시험: 폴더는 있으나 비어 있음. 이 PC 특이사항만
   저장하고, 저장소 문서에 이미 있는 내용은 저장하지 않는다. 로컬 한정(다른
   기기·클라우드에 안 감).
9. [신규] 훅 시험: `FileChanged` 훅으로 `solar-bible/tasks/done/` 변경을 감지해
   증거 요약을 붙이는 방안. 공식 문서에 이벤트는 있으나 폴더 단위 감시가 되는지,
   세션이 켜져 있을 때만 발동하는지는 시험으로 확인. 기존 SessionStart 훅 확장
   (헤르메스 크론 상태 요약, 발주 ④⑤ 뒤)과 함께 진행.
10. CLAUDE.md 다이어트: 현재 614줄(목표 200). 경로 지정 `.claude/rules/`는 해당
    파일을 Read할 때만 로드된다(공식 문서)이므로 셸 명령 위주 규칙에는 맞지 않고,
    상황별 절은 skills로 옮기는 쪽이 낫다. 시작 신호: 세션 시작 토큰 비용이 문제가
    되거나 새 규칙을 또 추가해야 할 때.
11. [사장님 승인 필요] 진행상황.md 교통정리: 살아 있는 파일(최신 블록·열린 공지)과
    날짜별 아카이브 분리, 이동 스크립트 시험 실행과 검증(절·줄 보존, 해시 1회 존재).
12. [조건부] 구PC 판정: 발주 ② 8번 조회 결과가 나오면 Linux 전환 상태 확정.

운영 관행(백로그 작업 아님): 감사·발주 초안을 쓸 때는 Plan Mode(편집·쓰기 차단)로
시작한다.

보류·기각 항목 (제 검토 판단, 2026-09-19):
- Auto-Memory를 저장소 공유 기억으로 쓰는 것: 공식 문서상 기기별 로컬 저장이라
  "기억은 GitHub" 원칙에 맞지 않는다. 기기 편의로는 위 8번으로 채택.
- `--fork-session` 기반 인프라 베이스 세션: 복제한 세션도 큰 컨텍스트를 호출마다
  다시 보내 비용 교리(컨텍스트 x 호출 수)와 맞지 않는다 [이력을 이어받는지는 문서
  인용을 못 받아 확인 필요]. 대체: 인프라 맥락을 문서로 고정(시트 F0-4). 재검토
  신호: 세션 시작 로딩이 비용의 큰 부분이라는 사용량 데이터가 나올 때.
- 훅 결과로 헤르메스에게 자동 재발주(사장님 개입 없이): 발주는 사장님 승인 게이트를
  유지한다. 재검토 신호: 자동 검증이 몇 주간 오탐 없이 돈 뒤.
- worktree 격리 신규 도입: 9/17 적용 완료. 9/19 사고는 헤르메스가 작업 트리를 직접
  수정한 것이라 worktree가 아니라 §10 쓰기 금지 규칙으로 처리.
- effort xhigh 상시 표준: 세션 비용 교리와 맞지 않아 범위가 좁은 조사에만 임시 사용.
- D:\Downloads·ComfyUI "정리": ComfyUI는 지금 쓰는 설치본이라 정리 지시를 내지
  않고, 사장님 명시 승인이 있을 때만 다룬다.
- SSH 포트포워딩 신규 통로: 노트북은 이미 Tailscale로 접속(9/15 검증). 22번 포트
  노출은 발주 ②로 감사부터.
- "릴레이 인수인계 8건 순차 처리": 8건은 9/15~17에 대부분 처리됨(worktree,
  fallback_providers, Agent OS, 모델 라우팅 등). 남은 것은 컴맹 가이드뿐(위 4번).

손대지 말 것: D 드라이브 안의 어떤 것도 삭제·이동 금지(솔라바이블 §10),
`trig_019zUkyhmv8ndyHY1G4n33qD`(비활성 그대로).
