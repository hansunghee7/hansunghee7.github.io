# 에이전트 퍼포먼스 극대화 백로그 (2026-09-19)

사장님 지시 3가지가 기준입니다.
1. 사장님이 PowerShell을 직접 실행하는 일, 답답한 폴링 대기, 헤르메스에게 복사·붙여넣기를 최소화한다.
2. 에이전트가 쉬는 사이 헤르메스를 풀로 돌린다. 헤르메스를 적극 쓰고 Claude 토큰 사용을 최소화한다.
3. SSH, 헤르메스 고도화, 구PC Linux를 활용해 에이전트 퍼포먼스를 끌어올린다. 탐 업무 고도화 4항목 포함.

역할 분담 원칙: 오래 걸리는 실행, 초안 작성, 장비를 다루는 일은 헤르메스. 판정, 설계, 검증은 탐.
탐이 헤르메스를 부를 때는 대기열 대신 직접 호출을 기본으로 한다
(`hermes chat --oneshot`, 2026-09-19 실측 왕복 12초). 대기열(pending)은 야간·대량 작업용이다.

## 1. 사장님 손을 덜 타게 (지시 1)
| # | 안건 | 담당 | 검증 기준 |
|---|---|---|---|
| 1 | 직접 호출 래퍼 `hx`: 지시문을 넣으면 헤르메스를 바로 부르고 결과를 tasks/done에 저장, 증거 스냅샷 자동 첨부 | 헤르메스 작성, 탐 검증 | 왕복 15초 이내, done 파일 생성 |
| 2 | 결과 알림 훅: 헤르메스가 tasks/done에 결과를 남기면 새 것만 요약(`hermes_results.py`, 헤르메스가 작성 중). SessionStart 훅으로 연결하고 `FileChanged` 훅은 시험 후 채택 | 탐 | 새 세션 시작 때 새 결과가 자동 출력됨 |
| 3 | 폴러 개선: 지시서를 요약하지 않고 원문 그대로 헤르메스에 전달, 간격 단축 검토 | 헤르메스 | 원문 전달 확인, 처리 지연 측정 |
| 4 | 관리자 권한 작업을 모아서 1회 스크립트로 제시(사장님 PowerShell 실행 횟수 축소). 신호: 관리자 작업이 주 3회 이상 나오면 최소 권한 자동화 검토 | 탐 | 관리자 창 실행이 작업당 1회 이내 |
| 5 | 헤르메스 보고서 자동 저장 규칙 유지(지시서 공통 규칙 반영 완료). 복사·붙여넣기 없이 탐이 파일을 직접 읽는다 | 헤르메스 | 붙여넣기 요청 0건 |

## 2. 헤르메스를 풀로 (지시 2)
| # | 안건 | 담당 | 검증 기준 |
|---|---|---|---|
| 6 | 야간 대량 큐: 사장님 취침 또는 Claude 토큰 저잔량 때 pending을 일괄 처리. `max_turns` 30 반영 확인, 실패 시 재시도 3회 | 헤르메스 | 다음 날 아침 done에 결과와 증거 |
| 7 | 토큰 저잔량 모드 규칙: 세션 초기에 사용량을 확인하고 잔량이 낮으면 탐은 판정만 하고 실행은 전부 헤르메스가 한다 | 탐 | 규칙 문서화, 저잔량일 때 탐의 도구 호출 감소 |
| 8 | 4090 로컬 모델 라우팅(시트 L1-4): 비용 0원 작업(로그 정리, 표 만들기, 초안)을 로컬 모델로 | 헤르메스 | 무료 모델 처리 비율 측정 |
| 9 | 헬스체크 알림 연결: 실패했을 때만 텔레그램(사장님 확정 채널, 카카오톡은 쓰지 않음)으로 알림 | 헤르메스 | 실패를 일부러 만들어 알림 수신 확인 |
| 10 | 증거 자동화(시트 A0-6): 모든 완료 보고에 evidence_snapshot 출력 첨부 | 헤르메스 | 증거 없는 DONE 0건 |

## 3. SSH, 구PC Linux, 헤르메스 고도화 (지시 3)
| # | 안건 | 담당 | 검증 기준 |
|---|---|---|---|
| 11 | 구PC Linux를 Tailscale에 다시 가입(사장님 1회 작업 후 헤르메스가 SSH로 이어서 세팅) | 사장님, 헤르메스 | Tailscale 목록에서 Linux 표시, SSH 접속 |
| 12 | 구PC를 상시 가동 서버로: 폴러, relay-watcher, health-check 크론을 systemd로 이전 후보. 재부팅 영향과 신PC 부담을 줄인다. 신호: 구PC가 1주 안정 가동일 때 | 헤르메스 | 1주 무중단, 재부팅 후 자동 복구 |
| 13 | SSH 병렬 실행: 신PC(4090), 구PC(Linux), 노트북을 Tailscale+SSH로 묶고 헤르메스가 허용 목록 명령만 원격 실행 | 헤르메스, 탐 검증 | 원격 명령 왕복 성공, 허용 목록 밖 명령 차단 |
| 14 | SSH 키 전용 전환(`PasswordAuthentication no`). 키 접속을 먼저 확인한 뒤 적용, 노트북 접속 시험 필수 | 탐, 사장님 승인 | 비밀번호 로그인 거부, 키 로그인 성공 |
| 15 | 숏츠 모델 SSD 활성 세트: 이미지 12개 복사 완료(검증 일치). LTX 영상 모델 추가 복사, `extra_model_paths.yaml`, 핏에게 `COMFYUI_DIR` 알림 | 탐, 핏 | 모델 로딩 시간 실측 비교(HDD 약 2분 대 SSD 수 초) |

## 4. 탐 업무 고도화 4항목 (원문 기준, 공식 문서 대조 반영)
| # | 안건 | 판정과 시작 조건 |
|---|---|---|
| 16 | 가드레일용 짧은 `CLAUDE.md`: `solar-bible`, `simplifier-agent`에 5줄, 전역 `~/.claude/CLAUDE.md`는 최소만 | 채택, 즉시 |
| 17 | Auto-Memory 소규모 시험: 이 PC 특이사항만 저장, 저장소 문서에 있는 내용은 저장하지 않음. 로컬 한정 | 채택, 시험 |
| 18 | 훅 확장: 위 2번과 통합. `FileChanged`는 폴더 단위 감시 가능 여부를 시험 | 채택, 시험 |
| 19 | Plan Mode는 감사·발주 초안에 사용, `effort` 상향은 범위가 좁은 조사에만 임시 사용 | 운영 관행 |
| 20 | **[종결 2026-09-21: PR #816, #843, #860 병합, CLAUDE.md 179줄]** CLAUDE.md 다이어트(614줄에서 목표 200줄), 진행상황.md 교통정리(사장님 승인 완료) | 채택, 리셋 후 |

## 5. 문서 계층 정렬과 OKR (사장님 지시 4, 2026-09-19 밤, 갈래 C 완료(2026-09-21, 진단 문서 [문서_정렬_진단_2026-09-21.md](문서_정렬_진단_2026-09-21.md)), 갈래 A·B와 종합은 대기)

**목표**: 최상위 문서(회사 목표)부터 최하위 문서(개별 역할 노트, 스킬)까지 문서 간 정렬과 역할을 다시 정의한다. 각 문서는 에이전트의 로드 부하를 최소로 하면서, 각 역할이 자기 일을 충실히 하게 만들어야 한다. 회사 목표와 개별 조직원(페르소나)의 활동을 잇는 OKR을 벤치마킹해서 우리 규모에 맞게 고도화한다.

**방식**: 딥리서치 파이프라인(`solar-bible/docs/딥리서치_파이프라인.md`) 그대로. 헤르메스가 수집, 제미나이가 출처 대조, 탐이 판정한다. 내부고객은 사장님과 각 페르소나다.

**리서치 3갈래**
| 갈래 | 질문 | 근거 기준 |
|---|---|---|
| A. 클로드 문서 구조 모범 사례 | CLAUDE.md 계층(전역, 프로젝트, 하위 폴더), rules, skills, memory, 서브에이전트 정의는 각각 무엇을 담고 언제 로드되는가. 고수들의 구조는 무엇이며 로드 부하는 어떻게 줄이는가 | 공식 문서(code.claude.com/docs) 우선. 커뮤니티 사례는 `[커뮤니티]`로 표기하고 원문 인용 |
| B. OKR 벤치마킹 | 회사 목표와 개인 활동을 잇는 방법(OKR 원전, 소규모 팀과 1인 기업 적용 사례). 1인 + AI 조직에서 무엇이 남고 무엇이 과한가 | 원전 저서·공식 가이드·사례 글의 원문 인용 |
| C. 우리 문서 인벤토리와 정렬 진단 | 우리 문서를 계층별로 세고(줄 수, 로드 시점, 중복, 상위 목표와의 연결 유무), 어디가 어긋났는지 | 탐이 서브에이전트로 읽기 전용 실측. 표로 제출 |

**산출물 3개**
1. 문서 계층도: 최상위부터 최하위까지, 각 문서의 역할, 로드 시점, 줄 수 상한, 누가 고치는가.
2. OKR 축소판 초안: 회사 목표(O) 1~2개와 핵심 결과(KR), 페르소나별 목표 1개씩과의 연결표. 최상위(회사 목표) 결정은 사장님이 한다.
3. 이전 계획: 위 재정의에 맞춰 CLAUDE.md 다이어트 PR과 역할 노트 작성을 정렬하는 순서.

**지금 하지 않는 것 (규모 최적화)**: OKR 소프트웨어, 분기 회고 자동화, 개인 평가 점수화. 1인 + AI 규모에서는 목표 문서 한 장과 주간 점검이면 충분하다. **넘어갈 신호**: 외부 사용자, 매출, 채용이 생기면 분기 OKR을 정례화한다.

**다른 작업과의 순서**: CLAUDE.md 다이어트 PR2~4는 저위험이라 그대로 진행한다. **PR5(보호 6절 압축)와 아침·저녁 의식 스킬화는 이 리서치 결과 뒤로 미룬다**(문서 구조 자체가 바뀔 수 있음). 역할 노트는 얇게(80줄) 먼저 만들고 결과에 맞춰 조정한다.

**착수 조건과 모델**: 제미나이 3.6-flash 하루 한도가 초기화된 뒤(2026-09-20 이후 권장, 오늘 19일 검증 키 소진. 초기화 시각은 미확인이라 20일 아침에 실제로 확인). 수집은 헤르메스와 하이쿠, 갈래 C 실측은 소넷, 종합(계층도와 OKR 초안 설계)은 판단이 필요해 오퍼스를 쓰며 **쓰기 전에 사장님께 컨펌**을 받는다.

## 6. 추가 안건과 우선순위 (2026-09-20 사장님 확정)

> **확정(2026-09-20)**: 아래 순위를 승인. 단, **0순위에 "딥리서치 제미나이 검증 프로세스 작동 확인"을 추가**한다. 이때 마야는 자기 결과에 검증이 없다고 인지한 상태로 둔다(검증은 마야 몰래 독립 관문으로 돈다). 나머지는 계획대로 진행. 새김 글로벌화(첫 시장은 한국 PMF, 해외 확장을 고려한 네이밍·사용성 리포지셔닝)는 별도 딥리서치 안건으로 진행상황.md에 둔다.

**새로 생긴 안건**
| # | 안건 | 근거 |
|---|---|---|
| 21 | **텔레그램 회신 경로**: 사장님이 텔레그램으로 답하면(`확인` 등) 헤르메스가 마지막으로 알림을 보낸 사람 앞 우편함 메시지로 넘긴다. 어느 알림에 대한 답인지 매핑과 오작동 시험 설계가 핵심 | 지금은 텔레그램 답장이 헤르메스와의 대화로만 끝나고 탐·마야에게 닿지 않음(`solar-bible/reports/2026-09-19-telegram-inbound.md`, 결론 "없음"). 사장님 확정 2026-09-19 |
| 22 | **[종결 2026-09-21: docs/역할노트-탐·마야·노트.md 존재]** **역할 노트 작성**: 모든 페르소나가 `docs/역할노트-<이름>.md`(80줄 이하, 핵심 지침, UX 가이드·스킬 문서는 링크)를 만든다. 탐(로컬)은 내일 | 사장님 확정 2026-09-19 |
| 23 | 텔레그램 통일 재작업(헤르메스, 지시서 `2026-09-19-telegram-unify-fix.md`)과 카카오톡 잔재 정리. 위 9번은 카카오톡이 아니라 **텔레그램**으로 읽는다 | 사장님 확정 2026-09-19 |
| 24 | **새김 GCP 예산 알림 대응**: 프로젝트 `saegim-mcp` 월 지출 한도 ₩10,000의 80%(₩8,000)에 9/19 도달(구글 알림 메일, 사장님 폰 캡처 2026-09-20). 무엇이 비용을 내는지 SKU별로 확인하고, 100%에서 서비스가 자동 일시중지되는지 확인한 뒤 한도 조정 또는 원인 제거를 사장님이 결정 | 9/12 기록에 "예산 ₩10,000, Cloud Run 초과 시 자동 일시중지"와 "월 0원에 가깝다"는 설계 문서가 있어 실제와 어긋남. 배포 설정은 min-instances 0, max 2, 1CPU 512Mi. 9/12~9/15 main 푸시·수동 실행 배포 워크플로가 약 34회 돌았음(실패 6회 포함, 9/16 이후 없음). 원인은 미확인. **결제 화면 실측(2026-09-20 사장님 캡처)**: 9/1~20 비용 ₩8.11천 − 절감액(크레딧) ₩8.11천 = **실부담 ₩0**. 예산 알림은 크레딧을 뺀 뒤가 아니라 총액 기준으로 보임(추정). 원인은 Cloud Run(asia-northeast3) 요청 기반 청구, 9/13 이후 매일 약 ₩200~2,100, 배포가 없던 9/16~19에도 계속돼 배포 때문이 아님. 배포 시작(9/12)이 비용 시작과 겹쳐 지투시그마 사용 시점과는 구분 못 함. **미확인**: 예산 100%에서 실제로 서비스가 멈추는지(구글 예산은 기본적으로 알림만이고 자동 중지는 별도 연결이 필요하다고 알고 있으나 이 프로젝트 설정은 못 봄), 요청의 정체(고객, 우리 시험, 스캔). **대안 조사(2026-09-20, 서브에이전트가 공식 페이지 직접 확인)**: Cloud Run 무료량은 월 vCPU 18만 초, GiB 36만 초, 요청 200만 건이나 서울(asia-northeast3)은 Tier 2라 무료 혜택이 Tier 1 가격 기준 금액 할인으로 적용돼 덜 쓰임. 열린 스트림이 청구되는지는 공식 문서에 명시 없음(미확인). 코드는 MCP 기본 설정 그대로(stateless_http, json_response 미사용). 무료 플랫폼(Render, Koyeb, Oracle, GCE e2-micro, HF, Cloudflare)은 절전, 프로덕션 비권장, 회수, 미국 리전 한정, 재작성 비용 중 하나 이상에 걸려 외부 고객용으로 안전한 곳을 못 찾음. 다음: (1) Cloud Run 측정항목으로 청구 시간과 SKU 확인 (2) 코드 옵션 2개와 Tier 1 리전 실험은 사장님 결정 후 (3) 무료 플랫폼 이전은 지금 하지 않음 **SDK 2.2.0 시험(2026-09-20 탐, 로컬)**: 기본(stateful)은 클라이언트의 GET 스트림이 계속 열려 있음(4초 이상). `stateless_http`+`json_response`를 켜도 GET 요청은 여전히 열려 있음(POST 응답만 JSON으로 바뀜). 즉 옵션 두 개만으로는 부족하고, GET을 405로 거절하는 방법(MCP 사양이 허용)이 후보. 이 서버는 `requirements.txt`가 `mcp` 버전을 고정하지 않아 배포마다 SDK가 바뀔 수 있음. 아직 병합·배포하지 않음(Cloud Run 측정항목 캡처 뒤 결정) **[미검증 표기, 2026-09-20 사장님 지시]** (1) "열린 GET 스트림이 Cloud Run 요청 기반 청구에서 시간으로 과금된다"는 공식 문서에 명시가 없는 추론이라 **미검증**이다(GET 스트림이 열려 있는 것 자체는 SDK 2.2.0 시험으로 확인). (2) 외부 보고서의 "서울 리전 단가가 Tier 1보다 15~20% 높다"는 출처를 확인하지 못해 **미검증**이다(서울이 Tier 2라는 사실만 공식 페이지로 확인). **청구 실측(사장님 캡처)**: 9/1~18 프로젝트 saegim-mcp ₩8,265 중 SKU `Services CPU Tier 2 (Request-based billing)`이 ₩7,855로 대부분이고, 일 약 1,000~2,000원(9/12 시작, 9/16 최고 약 2,150원), 9월 예상 ₩11,923(크레딧 제외 기준)이라 한도 ₩10,000을 넘을 전망. 서비스는 살아 있음(9/20 홈 200, 첫 요청 4.6초는 인스턴스 0에서 깨어난 콜드 스타트로 보임). **진행**: saegim PR #95(GET 405 + 롤백 스위치 `SAEGIM_REJECT_GET=0` + `mcp==2.2.0` 등 고정). 병합 금지, 조건은 Cloud Run 7일 기준선 캡처 뒤. 병합 후 3~4일 청구 추이로 효과 검증. 보류: 타임아웃 단축, vCPU·메모리 축소, max-instances 축소. 결정은 cxo-db 인박스 #122. **[배포 완료 2026-09-20 09:50 KST, 사장님 즉시 병합 승인]** saegim PR #95 병합(커밋 0c23b60), 배포 워크플로 성공. 사장님이 같은 날 Cloud Run 콘솔에서 요청 제한 시간을 300초에서 15초로 먼저 바꿨음(코드 변경과 동시에 적용되어 **효과를 따로 분리해 볼 수 없음**). **운영 확인(탐, 읽기 전용 요청)**: GET /mcp는 405와 `Allow: POST, DELETE`(0.19초), 토큰 없는 POST와 틀린 토큰 POST는 401, 홈과 가입 화면은 200. **미완료**: (a) 제품 클라이언트(Claude Code) E2E는 이 노트북에 새김 MCP 인증 정보가 없어 못 함, 사장님 PC에서 확인 필요. (b) 병합 전 7일 측정항목 기준선은 캡처 없이 병합됨, 대신 청구 기준선은 9/14~19 일별 약 730~2,150원(평균 약 1,300원). (c) 3~4일 청구 추이 검증은 9/23~24에 사장님 캡처로 확인. 롤백은 환경변수 `SAEGIM_REJECT_GET=0`. |
| 26 | **[과제 변경 2026-09-22 사장님: 일회성 결정이 아니라 헤르메스가 매일 수익경로 관련 정보를 리서치해 오는 상시 과제. 탐은 리포트 형식과 원문 인용 대조만 맡는다]** **크레딧 소멸(2026-12-10) 전에 새김의 수익 경로 확정**: 유료 고객을 만들거나 지원금을 받는 것 중 어느 경로로 갈지, 그 전에 월 원가 허용선을 정한다. 사장님 지시 2026-09-20. 사업 전략 세부는 비공개 저장소에 둔다 | 크레딧 ₩414,984는 2026-12-10까지(9/12 기록과 사장님 확인이 일치). 소멸 뒤 청구는 등록된 결제 수단으로 나간다. 현재 총액은 하루 약 ₩1,000~2,000이고 405와 타임아웃 조치의 효과는 아직 측정 전이라, 12/10 이후 월 원가는 미확정(9월 예상 총액 ₩11,923은 부분 달 기준 예측). 외부 보고서의 "과금이 즉시 0원에 가깝게 차단됨", "카드 결제 확률 매우 낮음", "안심해도 됨"은 근거 없이 단정한 문장이라 채택하지 않음 **결정(2026-09-20 사장님)**: (1) 지원금 후보 조사 발주 승인. (2) 결정 시점은 **11월 말**(한 달은 촉박). (3) 지원금은 사업 기간, 사업 아이템, 고객 확보 여부, 서비스 매출, 글로벌 진출 등 조건이 있어 **전략 정리가 먼저**. **발주 완료**: solar-bible `tasks/pending/2026-09-20-support-grants-research.md`(커밋 7d9f169), 회사 정보 없이 지원사업 요건 매트릭스와 접수 캘린더만 조사, 결과 `reports/2026-09-20-support-grants.md`, 제미나이 검증 실행 여부를 보고서에 명시하게 함(딥리서치 검증 첫 실사용 사례). 결과 수신 뒤 탐이 원문 표본 3건 대조로 판정. **다음**: 요건 5축(사업 기간, 사업 아이템, 고객 확보, 서비스 매출, 글로벌)에 대한 회사 현황과 격차 정리는 비공개 저장소 `simplifier-cxo-db`에 한다. |
| 27 | **노트북 로컬 헤르메스의 기본 모델이 종료됨**: 이 노트북의 헤르메스는 기본 모델이 `gemini-2.5-flash`(공급자 gemini)로 설정돼 있고 마야가 딥리서치를 발주했을 때 404로 보고서 생성이 실패함(마야 피드백 2026-09-20). 마야는 이 PC 설정을 건드리지 않고 신PC 경로(`tasks/pending`)로 다시 발주함 | 확인: `hermes config show`의 기본 모델이 gemini-2.5-flash. 미확인: 종료 시점과 이 PC에서 그 모델을 쓰는 다른 호출이 있는지. solar-bible에는 2.5 참조가 없고 이 저장소는 9/16 지시서에 역사 기록으로만 남아 있음. 선택지: (a) 이 PC에서는 헤르메스를 쓰지 않고 `hx`(신PC)만 쓴다고 선언(추천, 설정 변경 없음), (b) 이 PC 기본 모델을 바꾼다(모델 로스터의 결정 범위라 사장님 결정) **종결(2026-09-20 사장님)**: 노트북 헤르메스는 고려하지 않는다(발열이 심해 테스트 용도로만 쓰거나 지울 예정). 대신 구PC 활용안은 46번. |
| 28 | **[종결 2026-09-21: PR #770, #779, #806, #808 병합, 감지기 구현]** **헤르메스 폴러 3분 지연 단축**(사장님 지시 2026-09-20): 지금 폴러(`solar-bible-tasks-poller`, 3분 주기)는 **매번 AI 에이전트를 부르는 방식**이라(실행 기록에 hermes-agent 스킬 전문이 프롬프트로 실림) 간격을 줄이면 호출과 무료 모델 한도 소모가 그대로 늘어난다. 후보 3개 | **A. 직접 호출 확대(추천, 즉시)**: 탐은 이미 `hx ask`로 왕복 약 12초. 마야 세션도 같은 방식으로 쓰게 하려면 `hx.sh`의 쓰기 통로(put)를 분리해 읽기와 발주만 열어야 함(에이전트 권한 설계, 사장님 결정). **B. 폴러를 스크립트로 바꾸기(추천, 작음)**: `--no-agent` 스크립트가 1분마다 `git fetch`로 `tasks/pending`의 새 파일만 확인하고(AI 호출 0), 있을 때만 `hermes cron run <폴러 id>`로 에이전트를 부름. 기존 폴러는 30분 안전망으로 남김. 효과: 평상시 AI 호출이 하루 약 480회에서 필요할 때만으로 줄고 지연이 수십 초~1분대. 미검증: 최소 주기(`every 1m`이 되는지), `cron run`이 말하는 "다음 스케줄러 틱"의 길이, 그 사이 지연. 시험으로 실측 필요. **C. 푸시 수신(보류)**: GitHub 웹훅을 받으려면 신PC에 공개 진입로(Tailscale Funnel 등)가 필요해 "외부 접속은 Tailscale만, 공유기 포트 개방 안 함" 원칙과 충돌. 공개 저장소 대상 자체 호스팅 러너도 위험(외부 코드 실행 소지)이라 지금 하지 않음. 신호: B 적용 뒤에도 지연이 실제 문제일 때 **[진행 2026-09-20 사장님 승인: B 시험, A 마야에게 열기]** **B 구현**: `scripts/hermes_ops/pending_detector.py`(시험 6개 통과), 신PC 크론 `pending-detector`(ID 63f1b256e212, `every 1m`, no-agent, deliver local) 등록. 기존 폴러는 아직 3분 주기 그대로(안전망 전환은 측정 뒤). **측정(표본 작음)**: 푸시 완료 10:14:27 뒤 감지 10:15:23으로 **56초**(1회). 감지기 실행 기록은 10:15:23, 10:17:22, 10:19:22로 **실제 간격이 약 2분**이라 `every 1m`이 2분 단위로 도는 것으로 보임(원인 미확인, 스케줄러 틱 가능성). 기존 폴러 실행 기록은 09:55:35, 09:59:43으로 약 4분 간격(2회). **발견**: 폴러는 한 번에 하나씩 실행하는 구조라 긴 딥리서치를 처리하는 동안 새 지시서를 못 받음(측정 시각에 폴러가 지원사업 조사를 실행 중이었고 시험 지시서는 대기). 감지기가 폴러를 깨워도 폴러가 바쁘면 소용없어서, **긴 작업을 별도 프로세스로 분리하는 안(28-B2)**이 필요할 수 있음. 미검증. **A 구현**: `scripts/hx_lite.sh`(업무지시, 읽기, 크론 조회만, put 차단). 기술적 강제가 아니라 세션 권한 규칙과 함께 쓰는 안전장치. **[시험 결과 2026-09-20, 사장님 승인 두 방법 모두]** **줄 분리(성공)**: 긴 작업 전용 폴러 `solar-bible-long-poller`(ID 938439795638, 지시문은 solar-bible `docs/long-poller-prompt.md`)와 폴더 `tasks/pending-long/` 추가, 감지기가 줄마다 폴러를 따로 깨움(시험 8개 통과). 긴 작업(4분 대기)과 짧은 작업을 10:42:22에 동시에 푸시하자 **짧은 작업은 10:44:18에 완료(1분 56초)**, 긴 작업은 10:47:22에 완료(약 5분), 헤르메스가 10:48:01에 발주자에게 완료 우편. 같은 상황에서 이전에는 짧은 지시서가 약 19분 대기. **긴 작업 직접 호출(성공)**: 백그라운드 `hx_lite.sh ask`로 7분 대기 작업이 끝까지 돌고 완료 알림이 자동 도착(10:36:54 발주, 10:44:30 답변, 7분 36초, SSH keepalive 추가). 30분 이상은 미시험. **결정 필요**: 짧은/긴 작업을 어느 줄에 올릴지 기준(제안: 5분 이내 예상은 `pending`, 그 이상은 `pending-long`, 사람이 기다리는 급한 일은 `hx ask`). 보고서 파일은 지시서에 `PUBLISH: yes`가 없으면 원격에 push하지 않음(긴 줄 지시문 규칙). |
| 29 | **탐 KPI: 사장님 손 전달과 에이전트·사장님 대기 시간 줄이기**(사장님 지시 2026-09-20). 지표와 기준선은 [탐_KPI.md](탐_KPI.md). 추가 방안: (1) `hx kick` 우편 즉시 배달(완료, 수 초), (2) 배달원 크론 주기 단축(사장님 컨펌 필요), (3) 긴 딥리서치를 폴러와 분리해 실행(28-B2, 구현 전), (4) 딥리서치 완료 때 사장님 앞 우편과 즉시 배달을 하도록 지시서 표준화, (5) 텔레그램 답장을 에이전트에 넘기는 경로(백로그 21). 외부 딥리서치 의뢰서는 [의뢰서](의뢰서/2026-09-20_대기시간_단축_외부딥리서치.md) | 사장님이 외부 도구에 직접 의뢰하기로 함(내부 절차가 답답하다는 판단). 결과가 오면 규약 3원칙으로 검토해 채택/보류 확정 |
| 25 | **main 룰셋이 자동화 워크플로 push를 막음(데이터 파이프라인 중단)**: GA4, 서치콘솔, 클래리티, 북 인사이트, SNS 등 12개 워크플로가 main에 직접 push하는데 2026-09-17 12:18 KST부터 `GH013: Required status check "build" is expected`로 거절됨(약 66시간, 예약 발행 파이프라인 #726도 가동 불가). 사장님과 마야가 룰셋 화면을 확인했으나 우회 목록에 github-actions 항목이 없음 | 룰셋 `main-protect`(id 21809387)를 API로 읽은 결과 우회 대상은 저장소 관리자 역할 하나뿐. 마지막 수정 시각이 2026-09-17 12:18:42로 실패 시작과 같은 분. 추정: 그 시각의 수정이 워크플로 예외를 지웠을 가능성(확인 못 함). 변경자는 감사 로그가 없어 확인 불가. **시도 결과(2026-09-20 사장님이 직접 실행)**: 우회 목록에 GitHub Actions 앱(15368)을 API로 추가하는 방법은 `422 Actor GitHub Actions integration must be part of the ruleset source or owner organization`로 거절됨. 개인 소유 저장소에서는 이 방법이 불가능하므로 앞의 '예외가 지워졌다'는 추정은 약해졌고, 그 시각에 검사 규칙이 추가·변경됐을 가능성이 남음(미확인). 다음 방법: 사장님 소유의 저장소 한정 토큰(PAT, Contents 쓰기)을 Secret `PUSH_TOKEN`으로 등록하고 워크플로가 그 토큰으로 push(사장님은 관리자라 우회됨). 워크플로 1개로 먼저 시험한 뒤 12개로 확대. 신호: 저장소가 늘거나 토큰 만료가 잦아지면 GitHub App으로 전환 |

**우선순위 판정 기준** (사장님 지시에서 뽑음): ① 사장님 손과 대기를 줄이는가 ② Claude 토큰을 줄이거나 헤르메스 활용을 늘리는가 ③ 새김 프로젝트 재시동(2026-09-20 이후)을 막는 위험을 없애는가.

| 순위 | 안건 | 이유 |
|---|---|---|
| **0 (최우선)** | **25 자동화 워크플로 push 복구(데이터 수집 중단 중, 사장님 룰셋 결정 필요)**. **24 새김 GCP 예산 80% 알림: 비용 원인(SKU)과 자동 일시중지 여부 확인, 9/30 전 결정(새김 재시동 위험, 기준 ③)**. **딥리서치의 제미나이 검증 프로세스가 실제로 작동하는지 확인(추가, 마야는 검증이 없다고 인지)**. 23 텔레그램 통일 재작업 검증. 모델 재선발 크론 첫 결과(요일은 토요일 19:00으로 확정) | 긴급 알림이 22:07 이후 멈췄다가 복구된 상태(탐 실측). 신뢰 기반이라 새 작업 전에 확인 |
| 1 (시한 2026-12-10) | 26 새김 수익 경로(유료 고객 또는 지원금)와 월 원가 허용선 확정 | 크레딧 소멸 전에 사장님이 정할 일. 원가 측정(24)과 지원금 후보 조사가 입력이 된다 |
| 1 (전달 리드타임, 탐 KPI) | 28, 29 폴러 3분 지연 단축(B안 시험, A안 마야 확대 결정) | 사장님 목표(리드타임 최소화)에 직결. 새 노출이나 새 비용 없이 기존 도구로 가능해 위험이 작다 |
| 1 | 20 CLAUDE.md 다이어트 PR2~PR4 | 매 세션 자동 로드라 모든 호출의 토큰이 줄어든다(현재 620줄). 서브에이전트가 하고 탐은 판정만 |
| 2 | 7 토큰 저잔량 모드 규칙(문서 한 장) | 지난 며칠 사장님이 토큰 제한으로 일이 멈춘 문제에 직접 닿음. 비용은 문서 한 장, 효과는 즉시 |
| 3 | 22 역할 노트(탐 포함, 80줄) | 새 세션에서 페르소나가 흔들리지 않는 안전장치. 새김 재시동 전에 있어야 함 |
| 4 | 21 텔레그램 회신 경로 | 사장님 손을 가장 크게 덜어 줌. 다만 오작동 위험이 있어 설계와 시험을 먼저 하고 서두르지 않는다 |
| 5 | 15 숏츠 모델 SSD 마무리(LTX 파일명 확인, `COMFYUI_DIR` 알림) | 내일 핏 미팅과 연결. 핏 확인이 있어야 진행 |
| 6 | 5 문서 계층 정렬과 OKR (딥리서치, 2026-09-20 이후 착수) | 방향을 정하는 큰 과제라 중요하지만, 위 1~3 결과가 입력이 된다. PR5와 의식 스킬화는 이 결과 뒤로 |
| 7 | 1 `hx` 래퍼, 3 폴러 원문 전달, 10 증거 자동화, 9 헬스체크 텔레그램 알림 | 헤르메스 활용도를 높이는 정리 작업. 급하지 않음 |
| 보류 | 11~14 구PC·SSH, 8 로컬 라우팅, 17 Auto-Memory, 4 관리자 작업 묶음, 6 야간 큐 | 신호 대기: 구PC 1주 안정, 사용량 경고 지속, 관리자 작업 주 3회 이상. 6 야간 큐는 7번 결과를 본 뒤 |

이 순위는 2026-09-20 사장님이 확정했다(0순위 추가 포함). 새김 재시동 일정과 충돌하면 사장님 판단이 우선한다.

### 2026-09-20 저녁 추가 안건 (탐 제안 순위, 사장님 확정 대기)
위 확정 순위표는 바꾸지 않았다. 아래 순위는 **제안**이며 사장님이 정하기 전에는 확정이 아니다.

| 제안 순위 | # | 안건 | 근거와 상태 |
|---|---|---|---|
| 0 (검증 프로세스와 같은 줄기) | 30 | **증거 스냅샷을 실제 열람 기록에서 추출**: 헤르메스 보고서의 `evidence_snapshot.py`가 열람 URL·실패 없음·주장 수를 코드에 직접 적어 두고 그대로 출력함(자기신고). 지시서에서 "열람한 URL은 도구 실행 기록에서 자동 추출, 스크립트에 URL 직접 기재 금지"로 고친다 | 지원사업 보고서 검수(2026-09-20): 내용은 표본 3건 원문 대조 통과, 증거 절차는 반려. 지시서 수정은 사장님 결정 대기 **사장님 승인(2026-09-21 저녁)**: 착수 가능, 지시서는 탐이 작성해 헤르메스에 위임(30번은 판정 기준을 코드로, 31번은 마무리 스크립트화) |
| 1 | 31 | **결정적 작업 실행기**: 폴러의 마무리(완료 표시, done 이동, 커밋, 알림)를 AI 대신 스크립트가 하고 AI는 실제 작업만 한다. 프로필별 병렬 실행 가능 | 2026-09-20 14:19 폴러(Solar Pro 4 무료)가 done 이동 단계를 빼먹고 27자짜리 답으로 정상 종료 → 같은 지시서 재처리 20분 낭비, 마야 후속 지시서 20분 대기(게이트웨이 로그 14:28:52 completed successfully). 사장님 승인 대기 **사장님 승인(2026-09-21 저녁)**: 착수 가능, 지시서는 탐이 작성해 헤르메스에 위임(30번은 판정 기준을 코드로, 31번은 마무리 스크립트화) |
| 1 | 32 | **작업 카드**(글 한 편 = 파일 한 장, 상태 6종, 알림 3번): 규격 [작업카드_규격_초안.md](작업카드_규격_초안.md)(PR #809), 도구 `scripts/card.py`(PR #818) | 파일럿 통과(헤르메스가 카드 도구로 직접 갱신, 감시 후 세션 깨움 7.9초). **헤르메스 내장 칸반과 겹침** → 관계 결정 대기 **결정(2026-09-20 사장님)**: 카드가 정본, 헤르메스 내장 칸반은 지금 쓰지 않는다. 조건: 업무 효율이 떨어지지 않을 것. **효율 확인 기준(탐 제안, 사장님 확정 대기)**: 카드 갱신 뒤 세션 깨움 지연이 파일럿(7.9초) 수준 유지, 카드 누락(처리됐는데 상태 미갱신) 0건, 글 한 편 대기 시간이 도입 전보다 늘지 않음. 감시 대장에 카드 정체 점검을 넣어 두 주 뒤 확인. |
| 2 | 33 | **헤르메스 병렬 운영**: 프로필당 프로세스 하나(공식 문서가 같은 프로필 두 프로세스를 금지), 필요하면 내장 칸반 | 시험 프로필 `nemo`, `ling`, `solar` 생성(2026-09-20), 고정 6문항 동시 18회 오류 0. 같은 폴더·같은 파일명 저장 충돌 1건. 시험 세트가 쉬워 모델 변별력 부족 → 어려운 시험 필요 (칸반 미사용 결정은 32번 참고) |
| 2 | 34 | **[드랍 2026-09-21 사장님: 잘 안 씀, 감시에서도 제외]** **Open WebUI 복구**: 헤르메스가 v0.11.3 업데이트 중 자기 웹UI 컨테이너를 지워 작업이 끊김(14:28:50), 3000번 포트 무응답. 채팅 기록 볼륨(`open-webui`)은 보존됨 | 새 이미지를 먼저 받고 재시작을 분리된 명령으로 하도록 다시 시켜야 함. 사장님 시험 환경이라 사장님 결정 |
| 2 | 35 | **[종결 2026-09-21: PR #818 병합, scripts/watch_done.ps1]** **세션 대기 감시 표준화**: 세션이 헤르메스 결과를 기다릴 때 폴링 대신 `scripts/watch_done.ps1` 백그라운드 감시 | 완료(PR #818). 마야 세션 적용 여부는 미확인 |
| 사장님 지시 (나중에) | 36 | **[종결 2026-09-21: PR #860 병합, check_claude_md_size.py, 폴리싱_기록.md]** **지침 문서 관리 정책**(사장님 지시 2026-09-20 "나중에 백로그로"): CLAUDE.md는 200줄 **미만**을 유지한다. 넣으려면 같은 PR에서 같은 줄 수 이상을 스킬·rule·docs로 옮기고, 199줄 초과는 병합을 막고(예외는 `크기예외` 표기와 사장님 승인), 급한 규칙은 진행상황.md 맨 위 `📢 전체 공지`에 먼저 쓰고, 격주로 폴리싱하며 정적 Golden Test로 규칙 손실을 확인한다. 임시로 넣는 줄은 `<!-- 임시 날짜 -->`를 단다 | 근거: CLAUDE.md는 8/28 약 45줄에서 9/20 604줄로 커졌고(커밋 68건, 9/8 하루 11건) 200줄 목표가 8/30부터 문서에 있었지만 강제가 없었다. 개편(PR #843) 뒤 175줄이라 여유가 24줄뿐이다. **결정 대기**: 200줄을 상한(차단)으로 할지 목표(경고)로 할지(탐 추천: 상한). 한계: 사장님 직접 커밋과 자동화는 병합 규칙을 우회한다 **결정(2026-09-20 사장님)**: 상한 250줄(CI 차단), 목표 200줄 이하는 격주 폴리싱으로 지향하고 정기적으로 돌아야 한다. **구현 완료**: `scripts/check_claude_md_size.py`(build-check), `docs/폴리싱_기록.md`(14일 주기, 감시가 기록 파일 수정일로 확인), 감시 대장 2항목(줄 수, 폴리싱 주기). |
| 사장님 지시 | 37 | **에이전트 도그푸딩 강제 적용과 개선점 발굴**(사장님 지시 2026-09-20). 정의: 고객과 같은 환경에서 고객과 똑같이 써 보며 고객보다 먼저 문제를 찾아 더 좋은 서비스로 개선. 문제점뿐 아니라 개선점을 찾아 경쟁 서비스보다 훨씬 좋아져야 한다. 안: [도그푸딩_강제_적용안.md](도그푸딩_강제_적용안.md) | 근거: 고객 경로 시험 3종이 수동 전용이고 마지막 실행이 9/13~14, 오늘 배포 뒤 미실행. 끊김을 핏 보고로 알았고 외부 고객사 1곳이 이미 있어 영향 확인이 첫 단계. **결정 대기**: 0단계(외부 고객 영향 확인)부터 착수할지, 경쟁 비교 후보를 조사로 먼저 확정할지 **시작(2026-09-20 사장님)**. **0단계 결과**: 오늘(08:16~21:16 KST) 외부 고객 요청 0건, 마지막 사용은 9/19 11:54~13:41 KST. 관측된 영향은 없으나 다음 접속 때 같은 끊김을 겪을 수 있어 40번 수정이 필요하다. 1단계(자동 시험, 합성 감시)는 감시 체계(#856, #858)로 일부 완료. |
| 사장님 지시 | 38 | **새김 구글 클라우드 사용량·비용 일일 자동 측정**(사장님 지시 2026-09-20): MCP 사용에 따른 사용량과 실제 금액을 매일 자동으로 본다. 설계는 [도그푸딩_강제_적용안.md](도그푸딩_강제_적용안.md) 8절 | **진행(2026-09-20 저녁)**: 결제 내보내기(표준 사용량 비용, 데이터세트 `billing_export`, US 다중 리전)를 사장님이 켬. 새김 저장소 PR #96 병합, 워크플로 `daily-gcp-usage.yml`(매일 09:30 KST)을 수동 실행해 **사용량 조회 성공**(권한 추가 불필요, 배포 계정이 편집자 역할). 첫 수치(24시간 구간): 요청 250~750건/일에 청구 인스턴스 시간 5,751~41,288초/일(요청당 약 20~110초, 오늘 구간 약 31초). **미완료**: 금액 열은 내보내기 표가 생겨야 채워짐(켠 뒤 몇 시간), 내일 09:30 KST 실행에서 확인. 크레딧 필드 합산은 미검증이라 나란히 표시 |
| 보안 백로그로 이동 | 39 | (2026-09-21 사장님 지시로 맨 뒤 "보안 관리 백로그"에 통합, 상세 그쪽) | |
| 탐 제안 (고객 영향) | 40 | **새김 끊김 원인 확정과 수정**: 로그로 확정(2026-09-20): 잘림 간격이 요청 제한 시간과 함께 움직임(15초 제한 때 약 18.5초 간격, 100초로 바꾼 19:11 이후 약 103.5초 간격, 지금도 지속). 핏의 오류 원문은 "session expired". 수정 후보: `json_response`와 `stateless_http` 켜기(로컬 재현에서 json_response는 POST 응답을 즉시 종료) | 미착수: 실제 서버 코드 로컬 재현(Supabase 필요) 후 PR 준비. **병합은 운영 배포라 사장님 승인**. 성공 판정: 배포 뒤 "Truncated response body" 0건 |
| 탐 제안 | 41 | **서비스 기능 점검 확장**: 다이얼로그 실사용 1건/일(무료 한도 소모 확인), 새김 세션 유지 시험(연결 후 유휴, 다시 조회)을 합성 감시에 추가 | 지금은 비밀값 없이 되는 응답 점검 4건만 있음(감시 대장 `fn:`) |
| 보안 백로그로 이동 | 42 | (2026-09-21 사장님 지시로 맨 뒤 "보안 관리 백로그"에 통합, 상세 그쪽) | |
| 보안 백로그로 이동 | 43 | (2026-09-21 사장님 지시로 맨 뒤 "보안 관리 백로그"에 통합, 상세 그쪽) | |
| 탐 제안 | 44 | **정기 위임 작업 시험**: 헤르메스(수집 4건: 지원사업, 서비스 종료·정책 공지, 경쟁 서비스, 트렌드), 로컬 LLM(가공 2건: 문서 아카이브 후보, 주간 변경 요약). 크롬 확장 대체는 공개 수치만 시험 **진척(2026-09-21)**: 로컬 LLM 가공 2건 완료(`scripts/ops/local_llm/`, 단위 시험 13건, 실행 각 11초·23초, 작업 스케줄러 `simplifier-weekly-local` 월 09:00, 감시 대장 등록·🟢). 아카이브 후보는 코드가 결정하고 LLM은 애매한 절만 분류(16개 전부 '유지'라 변별력 낮음), 변경 요약은 초안(요약 문장에 오해석이 섞임, 해시는 코드가 붙임). 헤르메스 수집 3건은 지시서로 1회 실행해 보고서 생성(지원사업은 진행 중), 인용 코드 대조 `scripts/ops/verify_collect_report.py`로 서비스 공지 7/18 일치, 경쟁 서비스 0/16(한국어 요약 인용과 자바스크립트 페이지 탓, 위조 확정 아님). 다음: 카드 인용 규칙을 '원문 그대로 복사'로 강화한 뒤 주간 크론 등록, 트렌드는 기존 파이프라인 유지 | 설계는 [주기작업_신뢰성_체계.md](주기작업_신뢰성_체계.md) 6절. 결과는 초안, 판정 금지, 감시 대장 등록 |
| 탐 제안 | 45 | **예약 발행 GitHub 누락 원인 확정**: 15분 설정이 약 2시간 간격으로만 실행됨 | 보조 시계 가동 중(#858). 노트의 답을 기다림(개편 영향인지 GitHub 쪽인지 미확인) |
| 사장님 지시 | 46 | **구PC(Linux) 활용안**: 노트북 헤르메스는 제외(27번 종결). 구PC는 감시 이중화(신PC가 꺼져도 알리는 감시), 백업, 상시 실행 스크립트 후보. 로컬 LLM은 신PC(4090)가 담당 | 구PC 사양은 미확인. Tailscale 재가입(11번) 선행 **사실 갱신(2026-09-20 사장님)**: 구PC는 무선이라 유선 WoL이 안 되고, 스마트소켓으로 전원을 넣고 빼며 BIOS의 전원 복구 시 자동 부팅으로 켠다(완벽히 됨). 리눅스 설치와 유지 설정 완료. 신PC에도 스마트소켓을 달면 신PC를 필요할 때만 깨우는 편이 효율적이므로, 구PC의 상시 가동 역할은 감시 이중화와 별도 에이전트 운영으로 좁혀 결정 대기(사양은 여전히 미확인). **결정과 1단계 완료(2026-09-20 사장님 "탐 제안대로")**: 구PC는 24시간 켜 두는 가벼운 감시·정기 작업 담당(A안), 신PC는 필요할 때만 켬. 사실 정정: 구PC는 ipTIME 공유기에 유선(enp3s0, 1Gbps)으로 연결됨. 사양 i5-3570 4코어, 메모리 15GB, 디스크 109GB(95GB 여유), Ubuntu 26.04. SSH는 신PC 전용 키(`goosolar` 별명)로 비밀번호 없이 접속. 구PC에 gh 로그인(repo, workflow 범위, 평문 저장) 후 **예약 발행 보조 시계를 구PC cron으로 이전**(2026-09-20 22:15 첫 자동 실행 확인, 신PC 작업 스케줄러는 끔, 감시는 ssh로 구PC의 kick.last를 확인). 다음: 감시 본체 분리 이전, 수집 크론 1개 시범, 전력·소음 측정, WoL 시험(MAC 94:de:80:6b:ed:bd) **감시 분리 이전 완료(2026-09-20)**: 구PC cron 30분(18항목 전부 정상 확인, 구PC에서 우편 전송 실측), 신PC는 `watcher:goosolar`로 구PC를 확인. **WoL 검증 대기(사장님 내일 귀가 뒤)**: 구PC 텔레그램 봇(`hermes-agent.service`)의 `/wol`이 신PC(MAC 74:56:3C:7B:D6:51)를 실제로 켜는지 신PC를 끄고 시험. 봇 인식 글자는 '신PC 켜' 등 정확 일치라 '신 pc 켜줘'는 AI 답변으로 빠짐(코드 확인). **봇에 허용 사용자 제한 없음**(누구나 명령 가능): 개선 후보는 허용 계정 제한, 인식 글자 추가(서비스 재시작에 sudo 필요) **2026-09-21 조치(사장님 지시 4~6번)**: 봇 개편·배포(`scripts/ops/goosolar/telegram_agent.py`): 허용 사용자 제한(닫힌 기본값), 띄어쓰기 무시 인식, 마법 패킷 2대역x2포트x3회, 보낸 뒤 최대 4분 켜짐 확인(포트 22·11434, 윈도우가 ping을 막음)해 결과 답장, `/status`, 수신·차단·결과 로그(**아침 시험 실패의 실제 원인 확정: 사장님이 7:33에 보낸 "신pc 전원 켜줘"가 옛 봇의 붙은 문구 일치("신pc 켜")에 안 걸려 AI 답변으로 빠졌고 부팅 패킷은 나가지 않았다(캡처 근거). 하드웨어 WoL 자체는 아직 미시험**, 대상 낱말+켜기 동사 방식으로 교체·시험 9문장). 재시작은 sudo 없이 프로세스 종료로(`Restart=always`). 신PC 랜카드는 드라이버 쪽 WoL(매직 패킷, 종료 상태 깨우기) 켜짐·MAC 일치·빠른 시작 꺼짐 확인, 절전형 이더넷과 Power Saving Mode는 켜져 있어 `wol_nic_setup.bat`(관리자, 바탕화면 사본)으로 끄도록 준비. **사장님 퇴근 후 실측 대기**, 실패 시 다음 후보는 메인보드 BIOS(ErP 등). 저소음: 신PC GPU 유휴 팬 0%·10W 확인, 디스크 유휴 정지 20분에서 5분, 구PC 안 쓰는 회전식 하드 2개 정지는 `quiet_setup.sh`(sudo 1회) 대기. 구솔라 직접 알림 도구 `tg_notify.py`(신PC 꺼져도 사장님 폰으로) 시험 발송 성공, 감시 알림 연결은 미결(사장님 결정). 감시 대장에 봇 서비스 점검 추가 **야간 수집 시범(2026-09-21 사장님 지시)**: 구PC에는 헤르메스 에이전트가 없고(봇은 Groq 기반) 그래서 `scripts/ops/goosolar/night_research.py`(Groq `groq/compound` 웹 검색 + 인용을 코드로 URL 재열람해 대조)를 만들어 23:30 KST 1회 cron으로 등록. 실측: compound-mini는 없는 URL·틀린 경로를 지어냄, compound는 인용 7건 중 5건 원문 일치·2건 불일치, 출력 상한 900 토큰(1800이면 413), 질문 1개 약 1.8만 토큰. 신PC가 꺼진 밤(WoL 시험)에도 구PC 단독으로 도는지가 폴백 시험. 신PC 쪽은 Studio 화면 읽기 파일럿을 헤르메스 크론 1회(23:30)로 등록 **WoL 실측 성공(2026-09-21 저녁, 사장님 확인)**: 구PC 텔레그램 봇 `/wol`로 꺼진 신PC가 켜짐(하드웨어 WoL 확정, BIOS 변경 불필요했음). 봇 로그는 systemd 저널이라 신PC에서 sudo 없이 못 봐서 증거는 사장님 관찰. **구PC 저소음 적용 확인(2026-09-21 저녁)**: 사장님이 `sudo bash ~/ops/quiet_setup.sh` 실행, 하드 2개 `standby`, 탐이 ssh로 `quiet-disks.service` enabled·active 재확인(구PC는 낮에 안 써서 사장님이 전원을 끄고 켠 것이라 재부팅해도 서비스가 유지되는지는 다음 재부팅 때 확인). 부팅 때 메모장이 뜨는 원인은 시작프로그램 폴더의 `Ollama_Then_Hermes.vbs.bak`(확장자 .bak가 메모장에 연결되어 열림, README.txt 이전 때 놓침)였고 `C:\work\_ops\startup_backup\`으로 이동함(다음 부팅에서 확인) **갱신(2026-09-22, 탐)**: ① WoL 실측 성공과 구PC 저소음 적용(하드 2개 standby, `quiet-disks.service`)은 #876·#877에 반영됨. ② 구PC 야간 조사(groq/compound)는 0/10으로 실패해 네모트론 + 매일 23:30 cron으로 방향 변경(48번, #911). ③ 구PC crontab의 1회성 `night_research.py` 2줄(9/21 23:30·00:00 실행분)을 삭제했고 원본은 `~/ops/crontab.bak_20260922`에 보관, 남은 cron은 예약 발행 보조 시계(15분)와 감시(30분) 2줄. ④ 헤르메스 수집 v2 보고서(`collect-service-notices-v2`)를 `verify_collect_report.py`로 대조: 18줄 중 **일치 9, 불일치 6, 확인불가 3**. 불일치 6줄 중 3줄은 짧은 버전 문자열(`v0.34.x`)이라 자바스크립트로 그려지는 페이지에서 원문 대조가 안 되는 오탐일 가능성이 있고(미확인), 나머지는 changelog·hermes-ai.net 인용의 번역·요약 의심. 판정은 하지 않고 수치만 기록. |
| 사장님 진행 | 20 | **CLAUDE.md 슬림화**: 사장님이 직접 리서치하고 진행(2026-09-20 지시). 1번 PR 병합(PR #816, 604→577줄), 2~7번은 사장님 판단 뒤 | 절별 배치안: 분석 결과는 대화 기록. 안전 규칙은 삭제하지 않고 포인터를 남긴다 |

### 2026-09-21 저녁 사장님 방향 지시 6건 (우선순위 표의 0~2순위 항목에 대한 답, 착수 전 확인점 포함)
| 대상 | 사장님 지시 | 착수 전 확인점 |
|---|---|---|
| 25 통계 수집 | 헤르메스가 신PC의 어사이드 브라우저 화면을 읽는 방식으로 변경 | 어사이드는 개인 SNS 로그인 브라우저(클릭 금지), 무인(크론)은 승인 벽으로 막힘(경험DB #140), 화면 읽기만 가능. API로 잘 되는 통계는 그대로 두고 API가 못 얻는 수치에만 적용할지 사장님 확인 대기. **성공 기준(사장님 2026-09-21)**: 현재 크롬 확장이 수집하는 것을 헤르메스가 대체하면 성공 |
| 24 GCP 요금 | 탐이 구글 클라우드에서 자동으로 끌어오는 방식 | 38번(daily-gcp-usage.yml 매일 09:30 KST, 결제 내보내기)이 이미 같은 방향. 금액 열 채움 여부 확인 후 탐이 읽는 경로로 연결 |
| 검증 관문 | AI 조사는 핏이 새로 만든 브라우저 컨트롤을 통한 제미나이 딥서치로 대체 | 핏의 도구 위치·범위 확인 대기. 어떤 방식이든 인용 원문 대조는 유지(헤르메스·Groq 모두 인용이 지어진 실측 있음). **성공 기준(사장님 2026-09-21)**: 현재의 "헤르메스 리서치 + 제미나이 API 검증" 딥리서치를 브라우저 컨트롤 방식으로 대체하면 성공 |
| 26 수익 경로 | 정부지원사업 또는 신설법인 설립 등의 방안을 헤르메스가 매일 조사, 매칭되는 것만 리포트 | 44번 수집 1건(지원사업)을 매일 주기로. 수집은 헤르메스, **매칭 판정은 헤르메스가 하지 않고** 조건표 + 코드 규칙 + 탐(사장님 확정 2026-09-20). 신설법인 등 전략 내용은 비공개 저장소 |
| 29 우편 배달 | 토큰이 더 소모되지 않으면 주기 단축 OK | 폴러가 Claude 토큰을 쓰는지 여부부터 실측 후 결정 |
| 21 회신 경로 | 승인 대신 "텔레그램에서 카카오톡으로 전환 테스트" 수행, 아래 문서 참고 | **참고 문서가 메시지에 첨부되지 않음**. 현행 규칙(CLAUDE.md 알림과 전달 절: 카카오톡 사용 안 함)과 충돌하므로 시험 결과를 본 뒤에만 규칙을 고침 |
| 어사이드 활용 확대 | 헤르메스 정보 수집 전반(지원사업, 서비스 공지, 경쟁 서비스 등 44번)도 어사이드 즐겨찾기에 띄워 둔 사이트에서 수집. 텔레그램 봇 확인도 탐이 어사이드의 웹 텔레그램을 브라우저 컨트롤로 직접 읽음(사장님 손 최소화) | 2026-09-21 사장님이 텔레그램을 읽으려던 탐의 화면 전환(computer-use switch_display)을 승인하지 않아 중단됨. **승인 창 문제**: 원격 승인 4회 거부 전례(경험DB #140)와 같은 벽이며 사장님이 매번 눌러야 하면 "손 최소화" 원칙과 충돌. 해결 후보: 어사이드에 수집 전용 프로필(로그인 없는 사이트만)을 만들고 그 범위만 미리 허용. 개인 SNS 로그인 탭(인스타·페이스북·링크드인)은 같은 즐겨찾기에 섞여 있어 오조작 위험(계정 정지 등 되돌리기 어려움). 사장님 확인 대기 |
| **기조 변경: 실행은 헤르메스가 기본** | 딥리서치 실행도 헤르메스에게 시킨다(제미나이 웹 화면을 서브에이전트가 조작하는 방식은 예외로 내림). 서브에이전트·탐 직접 실행은 헤르메스가 못 할 때만 | 2026-09-21 사장님 지시. 근거: 같은 날 제미나이 딥리서치가 거부 응답 3회로 실패(질문 표현이 원인일 수 있으나 미확인), 반면 헤르메스 웹 조사는 무료 엔진. 헤르메스 결과는 인용을 코드로 대조해야 함(0/16 실측) |
| 24 GCP 요금(재지시) | 사장님이 탐에게 "API로 자동으로 끌어오는 법"을 지시, 동시에 구PC로 대체하는 방안 검토 | **확인(2026-09-21)**: 새김 저장소 `simplifier-saegim`의 `daily-gcp-usage.yml`이 정상 실행 중(9/20, 9/21 성공)이고 결과가 실행 산출물로 남아 탐이 `gh run download`로 자동 수신 가능(일별 요청 수·청구 시간 확인됨). **금액 칸(cost, credits, net)은 아직 전부 비어 있음**: 결제 내보내기 표에 데이터가 안 들어왔거나 조회가 못 읽는 것으로 추정, 원인 미확인. 구PC 대체안: 구PC(gh 로그인됨)가 매일 실행·수신해 요약을 폰으로 알림 |
| 26 수익 경로(재지시) | 헤르메스가 매일 리서치해 오는 결과를 사장님이 보고 전략 검토(탐은 리포트 형식과 원문 검증만) | 결과 보고서는 매칭된 것만, 출처 원문 인용 필수 |
| 21 카카오톡 전환 시험 | **드랍(2026-09-21 사장님)**: 카카오 쪽에서 새 폰번호 등록을 요구해서 진행하지 않음. 텔레그램 유지, 알림 규칙(카카오톡 사용 안 함)은 그대로 | 뒤집을 신호: 카카오 쪽 요구사항이 바뀌거나 전용 번호가 생길 때 |
| 보안 3건(39·42·43) | 사장님이 나중에 통합해서 수립. 헤르메스 23:30 정기 작업에 포함될 가능성 | 아래 7절 보안 관리 백로그에서 대기 |

### 2026-09-22 사장님 지시 3건
| 대상 | 사장님 지시 | 처리 |
|---|---|---|
| 지투(외부 고객 1호 전담) 가동 | 사장님이 직접 진행하고, 이슈가 있으면 탐에게 문의 | **탐 백로그에서 삭제.** 탐은 문의를 받을 때만 기술 지원. 탐 몫으로 남기는 것은 고객명 금지어 자동 차단(병합 검사와 세션 훅)뿐 |
| 토큰 페이스 측정 | 탐이 놓치지 말고 챙긴다(헤르메스 일간 작업 또는 루틴) | **47번 신설**: 루틴은 `scripts/ops/pace_log.py`(세션 시작·종료 때 `get_usage` 값 기록, 페이스 판정), 감시는 `cron:pace-log`(30시간 넘게 기록 없으면 붉어짐). `get_usage`는 앱 안에서만 읽혀 헤르메스가 직접 못 읽으므로 기록은 탐 세션, 감시는 헤르메스 쪽 스크립트가 맡는다 |
| 26번 새김 수익 경로 | 헤르메스가 매일 수익경로 관련 정보를 리서치해 오는 것으로 과제 변경 | 위 26번 행에 반영. 다음: 매일 수집 규칙과 조건표(44번 수집 1건 지원사업을 매일 주기로), 리포트는 매칭된 것만 원문 인용과 함께, 매칭 판정은 헤르메스가 하지 않음 |

| # | 안건 | 근거와 상태 |
|---|---|---|
| 47 | **토큰 페이스 측정 루틴**(사장님 지시 2026-09-22): 목표는 주간 한도 초기화(토 9/26 18:00 KST)까지 탐을 쓰는 것. 페이스 = 주간 사용률 ÷ 기간 경과율 | 첫 기록 9/22 01:39 KST: 주간 47%, 경과 33.1%, **페이스 1.42(경고)**, 하루 11.3% 이내 필요. 기준 1.0(주의)/1.3(경고)은 제안값 |

### 2026-09-22 하기로 했으나 완전히 안 된 과제: 원인과 재등록 (사장님 지시, 탐이 목표를 달성할 수 있는 방식으로)
원칙: 원인은 **근거 강도**를 함께 적고, 재등록은 "탐이 혼자 끝낼 수 있는가"와 **측정 가능한 완료 기준**으로 쓴다. 사장님 결정이 필요한 것은 결정 칸에 분리한다.

| # | 과제 | 안 된 원인 (근거) | 재등록 방식 | 완료 기준 | 상태 |
|---|---|---|---|---|---|
| 48 | 구PC 야간 조사(23:30, 10문항) | ① groq/compound가 내부에서 다른 모델(분당 30,000토큰)로 검색 결과를 읽어 질문 하나가 14~20k토큰을 쓴다. 1분 안에 두 번 부르면 무조건 429(**실측 2026-09-22, 오류 원문 확보**). ② 재시도가 20/40/60초 3회뿐이라 창이 안 풀림 ③ 첫 실패에서 전체 중단 ④ 신PC 주소를 옛 유선 주소로 하드코딩해 "꺼져 있음" 오기록(주소 변경 사실 확인, 코드 위치 확인) | **패치 완료**(안내된 대기시간 + 여유로 5회 재시도, 질문 사이 75초 간격, 실패한 질문은 건너뛰고 이어하기, 신PC 주소를 Tailscale로). 시험 통과 뒤 매일 23:30 실행으로 전환(헤르메스 크론 또는 구PC cron은 사장님이 정한 쪽) | 10문항 중 8개 이상 완료, 코드 대조 표 생성, 신PC 상태 정확 기록, **3일 연속** | 패치 배포, 2문항 시험 진행 중 |
| 24·38 | 구글 클라우드 금액을 자동으로 | 결제 내보내기 표가 있으나 전체 0행(**확인**). 0행인 이유는 미확정(내보내기 활성화 이후 데이터만 쌓이거나 지연일 수 있음, **추정**). 요청 수와 과금 초는 이미 매일 자동 수집됨(**확인**) | 실제 금액이 9/24까지 채워지면 그대로 사용. 안 채워지면 **요청 수 x 과금 초 x 공개 단가로 추정 금액**을 같은 JSON에 "추정" 표시로 추가(탐이 스크립트로 끝낼 수 있음) | 매일 금액 값이 채워짐(실측 또는 추정 표시), 예산 100% 자동 일시중지 여부 확인, 9/30 전 결정 자료 제출 | 대기(9/23~24 확인) |
| 헤르메스 브라우저 | 딥리서치·수집 화면 조작 | 윈도우에서 헤르메스가 `agent-browser`를 파이프로 불러 첫 호출이 끝나지 않음(**3회 재현 + 단독 재현 + 공식 이슈 #107232 원문 확인**). real profile 잠금은 별개 문서 경고(**문서 확인, 이 PC 미시험**) | **[2026-09-22 정정: 패치 불필요]** 헤르메스 v0.21.3은 이미 파일 리다이렉트로 부르고 `example.com` 열기가 1~1.7초, `hermes chat` 경유 1분 43초에 성공(원인은 무료 모델의 느린 왕복과 짧은 시험 제한시간으로 재분류). 근거 [헤르메스_브라우저_멈춤_정정.md](헤르메스_브라우저_멈춤_정정.md). 남은 것: 로그인이 필요한 화면(구글 딥리서치)은 별도 트랙 | `example.com` 열기 30초 이내 성공(**충족**), 사고 모델 채팅 결과 4KB 이상 파일 저장 1건(미충족) | 사장님 별도 트랙 결과 대기(2~3일) |
| 44·26 | 헤르메스 수집(지원사업·경쟁·공지) 매일 | 헤르메스 수집물의 인용 대조 일치 0/16, 7/18(**측정**). 브라우저 도구 미동작으로 화면 수집 불가(위 행) | 26번 상시 과제로 통합: 수집은 헤르메스, **코드 대조 일치 행만 채택**, 매칭 판정은 하지 않음. 브라우저가 풀릴 때까지 검색 도구(브라우저 없는 웹 검색)로 수집 | 하루 1회 리포트 생성 + 대조 표 첨부, 일치 행만 리포트에 실림 | 규칙·조건표 작성 필요 |
| 라우팅 | explorer·collector 실측 | 세션 시작 때 브랜치가 main보다 뒤처져 두 에이전트가 등록되지 않음(**확인**) | 탐 시작 절차에 "최신 main 확인 + 에이전트 목록에 explorer 있는지 확인"을 추가. 새 세션에서 정책 §8의 #1(웹 수집), #2(탐색) 재측정 | §8에 재측정 값 2행 추가, 통과 시 웹 수집 행 복원 | 새 세션 대기 |
| 47 | 토큰 최적화 효과 | 효과 미확인이었음(측정값이 없었음) | 47번 페이스 루틴으로 대체(기록과 감시 있음) | 주간 페이스 1.0 이하로 복귀(현재 1.42) | 진행(첫 기록 완료) |

**48번 방향 변경(사장님 결정 2026-09-22):** Groq를 쓰지 않고 **네모트론**(헤르메스 기본 모델, OpenRouter 무료)으로 한다. 목적은 하루 1,000회 무료 호출 중 **남는 횟수를 쓰는 것**과 **실패했을 때 페일오버가 되는지 보는 것**이다.
- **남는 횟수 실측(헤르메스 기록):** 기본 프로필의 네모트론 호출이 9/20 696회, 9/21 766회, 수집 프로필 20회. 공유 한도가 1,000회가 맞다면 남는 것은 하루 약 200~230회(추정). 야간 작업은 이 안에서 상한을 둔다(예: 하루 150회, 여유 남김).
- **페일오버 체인(설정 확인):** 네모트론 -> solar-pro4(업스테이지) -> 신PC 로컬 qwen2.5-coder:14b. 시험 방법: 한도 도달 시 체인이 넘어가는 로그를 확인하거나 의도적으로 실패를 유발해 관찰.
- **Groq "막힘"은 확인하지 못했다:** 9/22 실호출이 서버에 닿아 분당 한도(429) 응답과 한도 수치를 돌려받았다(막혔다면 다른 응답이 왔을 것). 무료 한도가 낮아 실용성이 없다는 판단은 유효하다. 기존 `night_research.py`의 Groq 방식은 패치했으나 보류한다.
- **남은 설계(탐):** ① 구PC에서 브라우저 없이 쓸 웹 검색 도구가 되는지 확인(등록된 검색 제공자: brave-free, ddgs 등) ② 코드 대조는 유지(헤르메스 수집 인용 일치 0/16 실측) ③ 하루 호출 수 집계(소진 카운터) ④ **실행 위치는 구PC cron 매일 23:30(사장님 결정 2026-09-22, 추천안 나)**: 구PC에서 네모트론을 부르는 방법(구PC 헤르메스 또는 OpenRouter API 직접 호출 + 검색 도구)을 시험하고, 1회 실행 증거 뒤 매일 등록.
**결정이 필요한 것(사장님):** ① (해결: 네모트론 + 헤르메스 크론으로 결정) ② 헤르메스 호출 방식 패치 적용 승인 ③ 페이스 기준 1.0/1.3 확정.

**제미나이 API 키 배분(사장님 지시 2026-09-22):** 키 3개 중 **1개는 심플리파이어 다이얼로그용으로 마야가 쓰므로 탐·에이전트는 건드리지 않고**, 나머지 **2개를 빠른 작업**(단건 사실 확인, 표본 검증)에 투입한다. 기록상 키당 하루 무료 생성 20회(현재 값 미확인)라면 하루 약 40회가 예산이다. 무료 티어 입력은 학습에 쓰일 수 있으므로 공개 자료만 보낸다. 다이얼로그 키가 공개 사이트 방문자용인데 하루 20회 한도가 실사용에 충분한지는 확인 필요(마야 몫). 착수는 탐 단독 진행 큐 7번.

## 지금 하지 않는 것과 넘어갈 신호
- **토큰 최적화 장기 비교(2026-09-21 종료, 사장님)**: 이번 주 분석·변경은 여기서 종료. 다음 주 같은 기간의 호출·출력·캐시·도구 호출·공식 5시간/주간 사용률을 비교하는 A/B는 백로그로 유지하고, 그때까지 새 최적화 설정을 추가하지 않는다. 다음 새 세션의 시작값 기록과 `explorer`·`collector` 첫 사용 측정만 한다(근거: [토큰_최적화_보고서_2026-09-21.md](토큰_최적화_보고서_2026-09-21.md) 부록 A).
- 자동 재발주 루프(검증 실패 시 헤르메스에게 자동으로 다시 시키기): 사장님 승인 게이트를 유지한다. 신호: 자동 검증이 몇 주간 오탐 없이 돌 때.
- 공유기 외부 포트 개방: 하지 않는다. 외부 접속은 Tailscale만 쓴다.
- 구PC 상시 서버 이전(12번): 11번이 끝나고 1주 안정 가동을 확인한 뒤.
- A2A(에이전트끼리 직접 통신): 구PC가 살아난 뒤 신솔라와 구솔라가 서로 쓸 과제가 생길 때.

## 첫 주 순서
1. 오늘 밤 헤르메스: 5번 규칙 유지, 2번 스크립트 완성.
2. 리셋 후 탐: 2번 훅 연결, 15번 마무리, 16번 즉시.
3. 사장님 1회 작업: 11번(구PC Tailscale 재가입).
4. 그 뒤 헤르메스: 1번, 3번, 6번, 9번.

## 구현 현황 (2026-09-19 저녁)
- 완료: 우편함 도구와 규칙(solar-bible `mailbox/`), 세션 시작 훅 2개(헤르메스 결과 알림, 우편함 개수), 헬스체크 크론, 숏츠 모델 SSD 복사 34개와 `extra_model_paths.yaml`.
- 우편함은 위 1번(손 덜 타게) 안건의 구현입니다. 다음 단계는 헤르메스가 긴급 메시지만 사장님께 알리고 받는이별 요약본을 만드는 것입니다.

## 7. 보안 관리 백로그 (2026-09-21 사장님 지시: 39·42·43 통합, 앞으로 보안 안건은 여기에 쌓는다)

| 원번호 | 안건 | 상태와 근거 |
|---|---|---|
| 39 | **새김 배포 서비스 계정 권한 과다 점검**: `github-deploy` 계정이 편집자, 프로젝트 IAM 관리자, 보안 비밀 관리자 역할을 모두 가진 것을 IAM 화면에서 확인(2026-09-20, 보기만 함). 저장소나 워크플로가 침해되면 프로젝트 전체가 영향을 받는 구조 | 배포에 필요한 최소 역할(Cloud Run 관리자, 서비스 계정 사용자, Artifact Registry 작성자 등)로 줄이는 것을 검토. 변경은 보안 설정이라 사장님 콘솔 작업이며 배포가 깨질 수 있어 시험 배포가 필요. **결정 대기** **컴맹 설명 제공(2026-09-20), 결정 대기**: 사장님은 권한을 늘리고 싶어 하심. 이 계정은 이미 편집자, 프로젝트 IAM 관리자, 보안 비밀 관리자를 모두 가져 늘릴 권한이 거의 없다. **배포 권한 목록 조사(2026-09-20)**: 새김은 GCP 서비스 계정 `github-deploy` 하나를 GitHub 워크플로 7개(deploy-cloud-run, dogfood-smoke, dogfood-rotate-secret, daily-gcp-usage, resend-signup-emails, verify-recovery, verify-signup-flow)가 공유. 사람 쪽은 사장님 gh 로그인(repo, workflow 범위)이 워크플로 실행 권한을 가짐. 헤르메스는 .env에 GCP 자격증명 이름이 없고 gh 호출 흔적도 못 찾음(단, 같은 윈도 계정이라 gh 키링 접근 가능 여부는 미검증). 홈페이지는 main 보호 규칙, 새김·cxo-db는 무료 플랜이라 규칙 자체를 못 씀. |
| 42 | **이 PC 열린 포트 정리**: 올라마 0.0.0.0:11434(`OLLAMA_HOST=0.0.0.0` 설정 확인), Open WebUI 3000, n8n 5678, SSH 22. 사용처 확인 후 필요한 곳만 열기. SSH 키 전용(14번)과 합칠 후보 | 감시가 기준선 대비 변화를 하루 1회 확인 중. 인증 없는 올라마가 같은 네트워크에 노출된 것이 가장 큼 |
| 43 | **litellm-pool 키 파일 길목 방어**: 공개 저장소 폴더 안 키 파일이 실수로 커밋되지 않게 `.gitignore`와 로컬 변경 무시 표시 | 이력 전체 검사로 외부 유출 0건 확인. 내부 에이전트 노출은 교체 예외(규정 저장됨). 사장님 허락 대기 |

**사장님 방침(2026-09-21)**: 39·42·43은 나중에 통합해서 수립하고, 헤르메스 23:30 정기 작업에 포함시킬 가능성이 있다. 그때까지 착수하지 않는다.

**운영 규칙**: 새 보안 안건(권한, 열린 포트, 비밀값, 노출 범위)은 이 절에 행을 추가한다. 자격증명이 내부 에이전트에게만 노출된 것은 교체 사유가 아니다(규정 2026-09-20). 착수는 사장님 승인 뒤. 14번(구PC SSH 키 전용)도 같은 성격이라 여기서 함께 본다.
