# CLAUDE.md 다이어트 제안서 (읽기 전용 검토)

- 대상: `C:\work\hansunghee7.github.io\CLAUDE.md`
- 실측 현재 줄 수: **623줄** (본문 절 621줄 + 제목 2줄)
- 문서 자신이 정한 목표: 200줄 이하 (§a18e)
- 판정 기준: "이 줄을 지우면 클로드가 실수하는가?" 시나리오가 없으면 삭제, 있으면 유지 또는 스킬 이전
- 보호 절(사장님 지시, **문장 압축만** 가능): `8649` `f3a9` `c010` `22b0` `5e1a` `b7f1`

---

## 0. 먼저: 목표 200줄은 현재 규칙 아래서 도달 불가

보호 6절만 합쳐 **275줄**입니다(8649=52, f3a9=24, c010=31, 22b0=46, 5e1a=65, b7f1=57).
의미를 보존한 채 문장만 압축하면 현실적으로 **약 178줄**까지 줄어듭니다(아래 표).
나머지 22개 절을 전부 0줄로 만들어도 178줄이고, 실제로 남겨야 할 것을 남기면
**약 287줄**이 최솟값입니다. 200줄에 도달하려면 보호 절 자체를 손대야 하므로,
이건 세션이 판단할 게 아니라 **사장님 결정 사항**입니다(§5 질문 1).

| 시나리오 | 예상 줄 수 | 전제 |
|---|---|---|
| A. 이 제안서 그대로 (보호 절은 압축만) | **약 287줄** (54% 감소) | 추가 승인 불필요 |
| B. A + 아침·저녁 의식 절차 블록을 `daily-ritual` 스킬로 이전 | 약 220줄 (65% 감소) | 사장님 승인 필요 |
| C. 200줄 | 도달 불가 | 보호 절 내용 삭제가 필요 |

---

## 1. 절별 분류표

기호: **유지**=CLAUDE.md에 남김 / **압축**=내용 보존, 문장만 축약 / **스킬**=`.claude/skills/`로 이전 / **이전**=다른 문서로 / **삭제**

| id | 절 | 현재 | 분류 | 제안 후 | 지우면 늘어나는 실수 시나리오 / 스킬 트리거 |
|---|---|---:|---|---:|---|
| `8649` | 튜터 원칙 | 52 | 🔒압축 | 34 | 보호. 세션이 업계 모범사례를 그대로 이식하고 "사장님이 직접 할 절차"를 전문가 기준으로 써버림 |
| `f3a9` | DONE=VERIFIED | 24 | 🔒압축 | 16 | 보호. "완료했습니다"가 증거 없이 보고되고 사장님이 뒤늦게 미완을 발견 |
| `d78e` | 세션 운영 교리 서문 | 11 | 압축 | 4 | 원칙 1줄("기억은 파일에, 세션은 작업 하나에")은 유지. $656/$303 실측·공식문서 링크는 근거라 cxo-db로 |
| `c010` | 아침 의식 | 31 | 🔒압축 | 22 | 보호. 2026-09-08 실사고(압축 보고로 사장님이 저장 실패로 오인). 템플릿 코드블록은 축약 대상 아님 |
| `22b0` | 저녁 의식 | 46 | 🔒압축 | 32 | 보호. 인수인계 템플릿(16줄)은 그대로 두고 설명 문장만 압축 |
| `9eaf` | 세션 중 | 31 | 압축 | 12 | 5개 행동 규칙(서브에이전트 위임, 부분 읽기, Routine 바인딩 금지, 70% 셀프체크, 서브에이전트 시뮬레이션)은 유지. 하단 "2026-09-05 사용량 실측 감사" 8줄은 근거라 cxo-db로 |
| `5e1a` | 모델 고르기 | 65 | 🔒압축 | 40 | 보호. 단가표·등급 기준·검토단계 필수·페이브 지침 전부 의미 보존, 나열형 문장을 표나 한 줄로 |
| `b7f1` | Hermes 위임 | 57 | 🔒압축 | 34 | 보호. 위임 대상 10항목·제외 6항목을 각 1~2줄로 묶고 ⚠️클라우드 세션 경고는 유지 |
| `98ae` | 📢 전체 공지 | 31 | 압축 | 12 | 이 규칙이 없으면 세션이 "전달 권한이 없다"며 사장님께 중계를 부탁함(절 안에 명시된 실제 오해). 메신저=포인터 규칙도 유지 |
| `a18e` | 다이어트 기준 | 17 | 압축 | 6 | 판정 문장 + 200줄 목표 + `CLAUDE#<id>` 태그 규칙만. **"채택률 0%" 문장은 실측과 불일치라 수정 필요**(§5 질문 3) |
| `8599` | 승인 산출물 회수 | 20 | 압축 | 6 | 2026-08-30 유실 사고 재발. 4행 표는 `docs/산출물_인덱스.md` 서문으로 |
| `3cc9` | 산출물 인덱스 | 10 | 이전 | 2 | `docs/산출물_인덱스.md`로. CLAUDE.md엔 "승인 나오면 그 세션 안에 인덱스에 행 추가" 2줄만 |
| `9030` | 공개 저장소 경계 | 18 | 압축 | 8 | 지우면 사업전략·가격·고객정보가 공개 저장소에 커밋됨(되돌릴 수 없음). 최우선 유지, 표만 2줄로 |
| `1ad4` | 경험DB 기록 판단 | 14 | 압축 | 7 | 지우면 "왜 그렇게 했는지"가 안 쌓임. 이 회사가 자산이라 부르는 것 자체가 사라짐 |
| `035b` | 어떻게 남기나 | 10 | 압축 | 3 | "완성 문서 말고 `[인박스]` 이슈로" 한 줄이면 동작 동일. 마찰 설명은 근거 |
| `a55a` | 공개 커밋에 "왜" 금지 | 17 | 압축 | 6 | 지우면 판단 근거가 공개 히스토리에 박혀 삭제 불가. `stage` 요약(S0~S3)은 cxo-db README에 이미 있으므로 포인터로 |
| `9471` | 심플리파이어 웨이 포인터 | 9 | 압축 | 4 | 포인터는 유지(자동 로드 안 되는 문서라 존재 자체를 모름). 예시 나열은 삭제 |
| `7c3d` | 글쓰기 가이드 | 20 | 스킬+포인터 | 4 | 2026-09-17 실사고(주제·문체·이미지 3단계 전부 지적 후 정정). **스킬 `writing-start`** 트리거: "블로그 글/브런치/SNS 글/새 글 써줘". CLAUDE.md엔 "새 글 전 `writing-start` 스킬" 2줄 + 사장님 대면 문구 톤 규칙 2줄 |
| `6039` | UX 가이드 | 24 | 압축 | 8 | 지우면 아티팩트를 스튜디오 팔레트로 만듦(2026-09-07 탐 실사고). saegim lookup/cite + UX_GUIDE.md 경로 + 아티팩트 ⚠️만 남김 |
| `a0b4` | UX 인용 태그 절차 | 19 | 이전 | 0 | `docs/UX_GUIDE.md` 맨 앞으로. 이 절차는 UX_GUIDE를 이미 연 세션만 쓰므로 자동 로드 불필요 |
| `8f4e` | Simplifier Studio | 24 | 스킬+포인터 | 5 | **스킬 `studio-edit`** 트리거: "`insight-7b3e9f2c/` 안의 파일을 새로 만들거나 고칠 때 반드시". CLAUDE.md엔 STYLE_GUIDE 경로 + `check_studio_style.py` 완료 게이트 3줄(이게 없으면 스킬을 안 부른 채 완료 보고) |
| `d8e2` | 이미지 자산 정리 | 6 | 삭제 | 0 | 본문 전체가 스킬 포인터. `image-cleanup` 스킬 description이 이미 "이미지 정리·삭제·격리 요청이 있을 때 반드시 사용"으로 트리거를 담고 있음 |
| `a14c` | 숏폼 저장소 부착 | 14 | 스킬+포인터 | 3 | **스킬 `shorts-work`** 트리거: "대본·자막·음성·숏폼·쇼츠 요청 시 `add_repo`로 shorts-lab 먼저". ⚠️채널명 다크런칭(대외 노출 금지)은 되돌릴 수 없는 노출 리스크라 CLAUDE.md에 1줄 유지 |
| `77f6` | 여러 기기 작업 | 9 | 압축·병합 | (아래) | `git fetch origin --prune` + "브랜치 나열 fetch 금지"(조용한 실패 함정)는 유지 |
| `d1f1` | 커밋 교통정리 | 19 | 압축·병합 | 11 | `77f6`과 한 절로 병합. main 직접 push 금지·PR+auto-merge·영역 분리는 유지, 예외(bypass) 설명은 1줄로 |
| `028b` | 노출 범위 변경 | 8 | 압축 | 3 | "main 병합 = 즉시 라이브(스테이징 없음)" 사실 1줄은 유지해야 세션이 노출 판단 자체를 시작함. 절차는 `exposure-change` 스킬에 이미 있음 |
| `c198` | 시스템 구조 페이지 | 10 | 압축 | 4 | 지우면 인프라 변경 후 system-map.html이 낡음(2026-08-29 사장님이 직접 발견) |
| `d920` | 배포 사고 대응 | 5 | 삭제 | 0 | 본문 전체가 스킬 포인터. `deploy-incident` 스킬 description이 "라이브 사이트가 죽었거나 build-check/site-health-check 실패 시 반드시 사용"을 이미 담음 |

**합계**: 621줄 → **약 285줄** (제목 2줄 포함 **287줄**, 54% 감소)

- 보호 6절: 275 → 178
- 나머지 22절: 346 → 107
- 스킬로 이전: **3개 절**(7c3d, 8f4e, a14c), 신규 스킬 3개
- 다른 문서로 이전: 3건 (a0b4는 UX_GUIDE.md, 3cc9는 산출물_인덱스.md, 실측 근거 문단 2건은 cxo-db)
- 삭제: 2개 절 (d8e2, d920)

---

## 2. 실측으로 확인한 사실 (제안의 근거)

| 확인 항목 | 실측 결과 | 제안에 미친 영향 |
|---|---|---|
| `CLAUDE#<id>` 인용 태그 채택률 | **0%가 아님.** 커밋 메시지에서 11개 id, 총 26회. 상위: `5e1a`(6) `22b0`(5) `c010`(4) `a62c`(4) `f3a9`(2) `d78e`(2) `a18e`(2), 최근 1주 3건 | §a18e·§a0b4의 "채택률 0%" 문장은 **낡음**. 이걸 근거로 태그 규칙을 지우면 안 됨 |
| 인용된 적 없는 절 | `8f4e` `d8e2` `d920` `a14c` `028b` `c198` `d1f1` `77f6` `9030` `1ad4` `035b` `a55a` `9471` `6039` `a0b4` `98ae` `3cc9` `b7f1` (`7c3d`은 1회) | 다만 태그 습관이 9월 중순부터 붙기 시작한 거라 **"휴면 = 불필요"로 읽으면 안 됨**. 그래서 삭제는 스킬 포인터 2개로만 제한 |
| 존재하지 않는 id 인용 | `CLAUDE#a62c` 4회. 현재 CLAUDE.md에 없는 절(삭제되었거나 개명됨) | 절을 옮기거나 지울 때 id 이력을 남길 필요(§5 질문 3) |
| 기존 스킬 | `deploy-incident` `dom-selector-debug` `exposure-change` `image-cleanup` `multi-llm-handoff` 5개. 모두 description에 "~할 때 반드시 사용" 트리거 문구 보유 | 신규 3개도 같은 문형으로 작성 |
| `docs/진행상황.md` | 13,430줄 (상단만 확인) | 아침 의식이 "상단 우선순위만" 읽게 한 §c010 규칙의 실효성 확인 |

---

## 3. 위험 목록과 완화책

| 위험 | 왜 위험한가 | 완화책 |
|---|---|---|
| 🔴 `8f4e`(Studio) 스킬화 | 세션이 `insight-7b3e9f2c/*.html`을 고칠 때 스킬을 안 부르고 그냥 고치면 nav/head 구조·ⓘ 패턴이 깨짐. 파일 편집은 "요청 문구"가 아니라 "파일 경로"로 시작하므로 트리거가 약함 | ① 스킬 description에 경로를 그대로 넣기: "`insight-7b3e9f2c/` 안의 **어떤 파일이라도** 새로 만들거나 고치기 전에 반드시" ② CLAUDE.md에 `check_studio_style.py` **완료 게이트 3줄은 남김**. 스킬을 놓쳐도 완료 직전에 걸림 |
| 🔴 `a14c` 채널명 다크런칭 | 스킬로 통째로 옮기면 "숏폼 작업"이 아닌 맥락(사이트 카피, 강연 소개)에서 채널명을 노출할 수 있음. 되돌릴 수 없는 대외 노출 | 노출 금지 1줄만 `9030`(공개 저장소 경계) 절에 흡수. 저장소 부착 절차만 스킬로 |
| 🟡 `7c3d` 스킬화 | 이미 전역 `anthropic-skills:blog-writing`이 "블로그 글 써줘"에 트리거됨. 그쪽이 먼저 걸려 이 저장소 가이드를 건너뛸 수 있음(2026-09-17 사고와 동일 패턴) | 스킬 이름을 `writing-start`로 하고 description 첫 문장에 "이 저장소의 글은 **다른 글쓰기 스킬보다 먼저** 이 스킬을 연다"를 명시. CLAUDE.md에 2줄 포인터 유지 |
| 🟡 `d8e2`/`d920` 삭제 | 스킬 자동 트리거를 믿는 첫 사례. 안 걸리면 라이브 로고 격리 사고·배포 사고가 매뉴얼 없이 처리됨 | PR1 병합 후 다음 세션에서 1회 실사용 확인(§5 질문 2). 안 걸리면 1줄 포인터로 복구 |
| 🟡 `9eaf`·`d78e` 실측 문단 cxo-db 이전 | 비용 교리의 "왜"가 사라져 다음 세션이 70% 규칙을 임의로 완화할 수 있음 | 압축본에 근거를 한 줄로 남김: "근거: 2026-09-05 감사, 비용은 세션 길이(cache_read)와 붙어있음. 상세는 cxo-db" |
| 🟢 `a0b4`를 UX_GUIDE.md로 | UX_GUIDE를 안 여는 세션은 태그 규칙을 못 봄 | `a18e`에 남는 태그 규칙 2줄이 `UX_GUIDE#`·`WAY#`도 같이 커버하게 작성 |

---

## 4. 실행 계획 (PR 5개, 순서대로)

| PR | 범위 | 예상 감소 | 사람이 검토할 지점 |
|---|---|---:|---|
| **PR1** 저위험 정리 | `d8e2`·`d920` 삭제, `028b`·`c198` 압축, `77f6`+`d1f1` 병합 | 34줄 | 삭제한 2절의 스킬이 실제로 트리거되는지 다음 세션에서 1회 확인 |
| **PR2** 스킬 3개 신설 | `writing-start`·`studio-edit`·`shorts-work` 스킬 파일 추가 + `7c3d`·`8f4e`·`a14c` 축약 (같은 PR로. 스킬 없이 본문만 지우면 공백 발생) | 46줄 | **스킬 description 문구(트리거)**. 여기가 이 작업의 유일한 안전장치 |
| **PR3** 다른 문서로 이전 | `a0b4`는 UX_GUIDE.md, `3cc9`는 산출물_인덱스.md, 실측 근거 2문단은 cxo-db 이슈 | 30줄 | 옮긴 내용이 대상 문서에 실제로 들어갔는지(유실 방지) |
| **PR4** 비보호 절 압축 | `d78e` `9eaf` `98ae` `a18e` `8599` `9030` `1ad4` `035b` `a55a` `9471` `6039` | 118줄 | 의미 보존 여부. 절 단위로 커밋을 나눠 diff를 읽기 쉽게 |
| **PR5** 보호 6절 문장 압축 | `8649` `f3a9` `c010` `22b0` `5e1a` `b7f1` | 97줄 | **사장님이 직접 diff를 읽는 유일한 PR.** 절당 커밋 1개, 템플릿 코드블록은 무변경임을 diff로 증명 |
| (PR6, 조건부) | 아침·저녁 의식 절차를 `daily-ritual` 스킬로 | 54줄 | 사장님 승인 후에만. "하이~"/"바이~"가 스킬 트리거로 동작하는지 실측 |

순서 근거: 뒤로 갈수록 되돌리기 비용이 크므로, 앞 PR에서 "스킬 트리거가 실제로 걸리는가"를
먼저 실측하고 그 결과로 뒤 PR의 범위를 조정합니다. PR1과 PR2 사이에 최소 한 세션을 띄웁니다.

각 PR 커밋 메시지에 `CLAUDE#a18e`(다이어트 기준)를 남겨 이번 작업 자체가 태그 실측 데이터가 되게 합니다.

---

## 5. 탐(판정자)이 확인할 3가지 질문

1. **목표 줄 수를 다시 정할 것인가.** 보호 6절을 압축만 하면 178줄이라 200줄 목표는
   도달 불가입니다. (a) 목표를 **287줄**로 재설정, (b) 아침·저녁 의식 절차 블록의
   `daily-ritual` **스킬 이전을 승인**해 약 220줄, (c) 보호 절 내용 삭제를 재검토.
   셋 중 어느 쪽입니까?

2. **스킬 자동 트리거를 어디까지 믿을 것인가.** `d8e2`(이미지)·`d920`(배포사고)는
   본문 전체가 스킬 포인터라 삭제 후보지만, 삭제하면 "스킬이 안 걸리는 경우"의 안전망이
   사라집니다. **완전 삭제 / 1줄 포인터 유지** 중 어느 쪽입니까? 특히 `8f4e`(Studio)는
   파일 경로로 시작하는 작업이라 트리거가 약합니다. `check_studio_style.py` 게이트
   3줄을 본문에 남기는 안으로 충분합니까?

3. **인용 태그 체계를 유지할 것인가.** §a18e·§a0b4의 "채택률 0%"는 실측과 다릅니다
   (11개 id·26회, 최근 1주 3건). 이 문장을 **실측값으로 수정**할지 **해당 문단 자체를
   삭제**할지, 그리고 `CLAUDE#a62c`처럼 **사라진 절을 가리키는 태그**를 이번 개편에서
   어떻게 처리할지(절을 옮길 때 id를 그대로 들고 갈 것인지) 결정이 필요합니다.

---

### 사장님 결정 필요 항목 (총 4건)

1. 목표 줄 수 재설정 (200 → 287, 또는 의식 스킬화로 220)
2. 아침·저녁 의식 절차 블록의 스킬 이전 허용 여부
3. 스킬 포인터 2절(`d8e2`, `d920`) 완전 삭제 여부
4. 인용 태그 체계 유지·폐기 + 절 id 이력 처리 방침
