# 탐 KPI: 사장님 손과 에이전트 대기 시간 줄이기

> 사장님 지시(2026-09-20): 사장님이 복붙하거나 지시를 전달하는 공수를 줄이고, 에이전트 간·헤르메스 간 소통이 지연되어 대기하는 시간도 줄인다. 이것이 탐의 KPI다.
> 이 문서는 지표의 정의, 지금의 기준선(베이스라인), 측정 방법을 적는다. **목표 수치는 제안이며 사장님 확정 전이다.**
> 원칙: 측정하지 못하는 지표는 목표를 정하지 않고 측정 도구부터 만든다. 확인한 값과 추정을 나눠 적는다.

## 지표 4개

| # | 지표 | 정의 | 지금 측정 방법 | 기준선(2026-09-20) | 목표 제안(사장님 확정 대기) |
|---|---|---|---|---|---|
| K1 | 사장님 손 전달 건수 | 사장님이 에이전트 사이에서 복붙하거나 질문·지시를 옮긴 횟수(하루) | [전달 사고 대장](전달_사고_대장.md)에 발생 즉시 기록 | 이 세션 대화에서 제가 센 것: 에이전트 간 전달 3건(헤르메스 지시 1, 마야 질문 1, 마야 피드백 1). 대장 기록은 2건뿐이라 **누락이 있음**. 외부 LLM 결과를 사장님이 가져온 건도 별도로 약 3건(추정) | 같은 유형의 재발 0건. 하루 건수는 측정이 쌓인 뒤 정한다 |
| K2 | 헤르메스 발주 왕복 시간 | 발주 시점부터 헤르메스가 일을 시작해 첫 응답이 오기까지 | `hx ask` 직접 호출은 실측. `pending` 경로는 감지기 상태 파일과 크론 실행 기록 | `hx ask` 약 11~12초(2회). `pending` 경로: 푸시 후 감지 56초(1회), 감지기 실행 간격 약 2분(3회), 폴러 실행 간격 약 4분(2회). 폴러가 긴 작업을 하는 중이면 **뒤에 온 지시서가 그 작업이 끝날 때까지 대기**(시험 지시서 약 19분, 1회 실측) | `hx`로 가능한 발주는 30초 이내(달성). `pending`은 감지 2분 이내, 폴러가 바빠도 대기 없음(미달성) |
| K3 | 에이전트 간 우편 수신 지연 | 우편이 발송된 뒤 받는 에이전트가 읽기까지 걸린 시간 | **측정 불가**. 읽은 시각이 기록되지 않는다 | 미측정. 구조상 수신 측은 세션 시작 훅이나 다음 도구 호출 때만 읽으므로, 세션이 없으면 대기 시간에 상한이 없다(추정) | 측정 도구를 먼저 만든 뒤 정한다 |
| K4 | 채널 오염 | 사장님 폰에 "할 일 없는 알림"이 도착한 건수 | 사장님 보고(수동) | 2026-09-19~20 새벽에 스팸으로 판정된 건 다수(정확한 건수 미기록). 정책 개정 후 신규 발생은 확인 못 함 | 할 일 없는 알림 0건 |

## 추가 실측 (2026-09-20, 마야 대기 기록과 텔레그램 도착 시각)
| 구간 | 값 | 근거 |
|---|---|---|
| 딥리서치 지시서 푸시 → done 이동(**대부분 헤르메스의 작업 시간**, 집어 간 시각은 미측정) | 마야 건 24분(09:15 푸시, 보고서 09:38 저장, 09:39 이동), 탐 건 약 31분(10:01 푸시, 보고서 10:31 저장, 10:33 이동) | 커밋 583e21b, 저장소 커밋 시각, 신PC 파일 시각 |
| 폴러가 긴 작업 중일 때 뒤에 온 짧은 지시서 | 약 19분 대기(10:14 푸시, 10:33 처리) | 커밋 시각 |
| 보고서 파일 | 마야 건은 09:38에 **신PC에만** 저장되고 원격에는 push되지 않음(마야 확인). 탐 건도 신PC에만 있음 | 마야 보고, 신PC 파일 조회 |
| 우편함 → 그룹 도착 | 마야 우편 09:50 발송, 09:55 도착(5분) | 텔레그램 캡처 |
| 배달원 크론 실제 간격 | 약 11분(09:55:22, 10:06:21, 10:17:22 실행) | 크론 실행 기록 |
| **줄 분리 후 짧은 작업이 긴 작업과 겹칠 때** | 1분 56초(10:42:22 푸시, 10:44:18 완료). 이전에는 약 19분 | 커밋 시각 |
| **긴 작업 직접 호출(백그라운드 `hx_lite.sh ask`)** | 7분 36초 작업이 끝까지 돌고 완료 알림 자동 도착(30분 이상은 미시험) | 실행 출력 |
| **즉시 배달(`hx kick`)** | 실행 10:26:35, 신솔라와 그룹에 도착 10:26 | 텔레그램 캡처. 폴링이었다면 다음 실행이 10:28경 |
| **헤르메스가 파일 저장 → 파일 감시 프로그램이 감지**(신PC 로컬, 시험 폴더) | 12밀리초(시험 3·4, 헤르메스 경유 2건). 감시 프로그램은 윈도우 파일 이벤트를 **쌓아 두는 방식**이어야 함(호출을 반복하는 방식은 이름 바꾸기 이벤트를 놓쳐 2번 실패) | 감시 출력 원문(t0, event_time, t1) |
| **감지 → 세션 깨움**(백그라운드 명령 종료 알림) | 5.9초(시험 3·4), 10.5초(파일럿 3). 제 응답 시간 포함 상한값 | 감지 시각과 깨어난 뒤 첫 명령의 `date` |
| **파일럿: 짧은 줄 지시서 push → `tasks/done` 도착** | 1차 63초, 2차 134초, 3차 9분 47초. 3차 지연 원인은 감지기 `pending-detector`의 오류(`hermes cron run` 60초 타임아웃, exit 1, 다음 실행에서 재시도). 폴러 정기 실행(13:35:43)이 대신 처리 | 커밋 시각, 감시 출력, 크론 출력 `2026-09-20_13-33-42.md` |
| **감지기 수정 후 검증(파일럿 4차)** | 원인: `hermes cron run`이 폴러 실행이 끝날 때까지 붙잡음(빈 폴더 26초, 지시서 처리 중 60초 초과)인데 감지기가 60초에 포기해 실패로 기록. 수정: 기동만 확인하고 기다리지 않음(PR #806, 새 테스트는 옛 코드에서 실패·새 코드에서 통과). 결과: 지시서 push 14:10:42 → 감지기가 14:10:46에 폴러 기동(오류 없음) → `tasks/done` 도착 56초 | 커밋·크론 출력·감시 출력. 표본 1건이라 며칠 관찰 필요 |
| **완료 표시 일관성** | 파일럿 1차 완료본에는 `## RESULT`가 붙었고 2·3차에는 없음. 완료 신호는 "`tasks/done` 도착"만 믿을 수 있음 | 완료 폴더 파일 내용 |

지표 K5(우편 발송 → 폰·그룹 도착): 기준선 최대 약 11분, 평균 5분대(표본 1~2개). `hx kick` 사용 시 수 초. 목표 제안은 폴링 없이 발송 직후 도착.

지표 K6(살아 있는 세션 간 직접 전달): 2026-09-20 탐이 마야 세션(쉬는 상태)에 `SendMessage`로 보낸 뒤 몇 분 안에 마야가 실질 답을 보냄(정확한 지연은 미측정, 마야가 작업한 시간이 포함됨). 우편함 경로(폴링)와 비교할 기준선이다.

## 지금 측정할 수 없는 것을 측정하려면
1. **K1 누락 방지**: 사장님이 손으로 옮긴 일이 생기면 그 세션이 [전달 사고 대장](전달_사고_대장.md)에 바로 적는다(규칙은 [역할노트-탐](역할노트-탐.md)). 이번 세션에서 기록이 빠진 마야 피드백 전달은 다음 갱신에서 채운다.
2. **K3 읽음 시각 기록**: `solar-bible/mailbox/mailbox.py`가 읽음 표시를 기기별 파일에만 저장하고 시각을 남기지 않는다. 시각을 남기는 방법(로컬 파일 vs 저장소 기록)과 커밋 소음의 균형을 정해야 한다. 이 작업은 백로그에 별도 안건으로 둔다.
3. **K2 `pending` 경로**: 감지 시각은 감지기 상태 파일에 있다. 폴러 시작·종료 시각과 합쳐 주 1회 요약하면 된다. 아직 자동화하지 않았다.

## 지금 하지 않는 것
- 목표 수치를 측정 전에 확정하는 것(근거 없는 목표는 나중에 달성 여부를 판정할 수 없다).
- 지연을 줄이기 위해 신PC에 공개 진입로를 여는 것(백로그 28의 C안, 기존 원칙과 충돌).
- **다음 단계 신호**: 위 측정이 4주 쌓여도 대기 시간이 문제로 남으면 `pending` 경로의 긴 작업 분리(백로그 28-B2)를 진행한다.
