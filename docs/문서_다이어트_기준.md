# 자동 로드 문서의 다이어트 기준

> CLAUDE.md에서 옮겨 온 원문이다(2026-09-20 구조 최적화, 내용 변경 없음). 커밋 인용 태그 규칙은 CLAUDE.md에도 남아 있다.

# 자동 로드 문서의 다이어트 기준 <!-- id: a18e -->

CLAUDE.md와 역할 노트는 매 세션 자동 로드되므로 그 자체가 토큰 비용입니다. 한 줄마다
"**이 줄을 지우면 클로드가 실수하는가?**"를 물어서 아니면 지우고, 상황별 절차는
`.claude/skills/`로 옮깁니다(필요할 때만 로드됨). 목표: CLAUDE.md 200줄 이하, 역할 노트 80줄 이하.

**"감"이 아니라 실측으로 판단하세요.** 이 문서와 `simplifier-way.md`도 UX_GUIDE.md와 같은 방식으로
절별 id가 있습니다(`<!-- id: xxxx -->`). 어떤 원칙을 근거로 고쳤으면 커밋 메시지에 `CLAUDE#<id>` /
`WAY#<id>`(UX_GUIDE.md 절이면 `UX_GUIDE#<id>`)를 남기세요. `saegim.py report --doc CLAUDE.md
--tag CLAUDE`로 쓰이는 절과 휴면 절을 볼 수 있습니다.

**격주 폴리싱 때 반드시 볼 것(2026-09-23 추가, 재발방지)**: 진행상황.md 이력 절에
`[본문 반영 대기]` 표시가 남은 공지가 있는지 훑고, 있으면 이번 폴리싱에서 CLAUDE.md·
역할 노트·rules 중 해당 문구를 고치고 표시를 뗍니다. 근거: 공지는 확정됐는데 본문
(진입점 표 등)에 반영을 안 해서 옛 문구와 공존하다 실제 업무에서 혼선을 준 사고
(2026-09-23, 새김 MCP 실사용 필수 공지 vs CLAUDE.md의 "로컬 파일 지름길 허용" 잔존
문구).

**"사람이 격주로 훑는다"는 그 자체가 안 지켜질 수 있다는 지적(2026-09-23, 사장님)에 따라
기계 확인으로 바꿨습니다**: 애초에 이 문서의 "14일 지나면 감시가 알림"도 실제로는
감시 대장에 등록된 적이 없어 공수표였던 게 이번에 드러났습니다(같은 종류의 사고가 한
겹 더 있었던 것). `scripts/ops/check_claude_md_polish.py`(헤르메스 크론
`claude-md-polish-check`, 매일 08:00, no-agent — AI 판정 없이 git 날짜와 태그 문자열만
봄)가 폴리싱_기록.md 14일 초과 또는 `[본문 반영 대기]` 7일 초과를 감지하면 mailbox.py로
탐에게 직접 알립니다(정상이면 무출력). 감시 대장 `hermes:claude-md-polish-check` 등록,
1회 실행 성공 확인(job becee1dfed34, 2026-09-23). **뒤집을 신호**: 이 알림 자체가
무시되는 일이 반복되면(같은 실패가 한 겹 더 있다는 뜻), 알림 채널을 텔레그램으로
올리거나 세션 시작 훅으로 승격합니다.

**다음 단계 신호**: 의식을 지켜도 7일 사용량 경고 지속 / 동시 스레드 8개 초과 / S2(매출) 진입.
그때 모델 등급 정책과 사용량 리포트 자동화를 만듭니다.
