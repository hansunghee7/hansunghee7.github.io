# 통합 UX 가이드: 원문은 비공개로 옮겼습니다 (2026-09-24)

이 문서의 원문은 사장님 지시(2026-09-24)로 공개 저장소에서 비공개 새김 저장소(`simplifier-saegim`, `docs/guides/UX_GUIDE.md`)로 옮겼습니다.

- **평소에는 새김 MCP로 봅니다:** `mcp__saegim__lookup(doc="UX_GUIDE", query=...)`, `list_sections`, `get_section`. 반영했으면 `cite`(note는 `[페르소나명] ...`로 시작).
- **원문 열람은 대표 승인이 필요합니다.** 새김이 끊겼거나 원문 전체가 꼭 필요하면 사장님께 이유를 말씀드리고 승인을 받은 뒤 엽니다. 로컬 세션은 승인 뒤 `.claude/uxguide-approved` 파일을 만들면 훅이 통과시키고, 다 읽으면 지웁니다.
- 원문을 고치면 새김 저장소의 `reseed-guides.yml`이 자동으로 새김에 다시 심습니다.
- 커밋 메시지의 `UX_GUIDE#<id>` 인용 태그 관례는 그대로입니다.
