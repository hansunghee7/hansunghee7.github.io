source "https://rubygems.org"

# GitHub Pages가 실제로 빌드할 때 쓰는 것과 똑같은 gem 묶음.
# 이 Gemfile로 로컬/CI에서 미리 빌드해보면 GitHub Pages가 빌드에
# 실패할지 push 전에(또는 push 직후 몇 분 안에) 미리 알 수 있다.
gem "github-pages", group: :jekyll_plugins
